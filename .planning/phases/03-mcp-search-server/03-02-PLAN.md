---
phase: 03-mcp-search-server
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - apps/mcp-server/package.json
  - apps/mcp-server/src/stdio.ts
  - apps/mcp-server/src/logger.ts
  - apps/mcp-server/src/stdio.test.ts
autonomous: true
requirements:
  - MCP-01
must_haves:
  truths:
    - "MCP client can spawn kb-mcp-server bin and connect via stdio transport"
    - "stdio entrypoint uses buildMcpServer() from Plan 03-01 (transport parity)"
    - "All logging in stdio path goes to stderr only — zero stdout pollution (D-30)"
    - "Process connects StdioServerTransport and blocks until disconnect"
  artifacts:
    - path: "apps/mcp-server/src/stdio.ts"
      provides: "stdio MCP entrypoint for Cursor/local clients"
      contains: "StdioServerTransport"
    - path: "apps/mcp-server/src/logger.ts"
      provides: "stderr-only logging helper"
      contains: "stderr"
    - path: "apps/mcp-server/package.json"
      provides: "bin kb-mcp-server → stdio entrypoint"
      contains: "kb-mcp-server"
  key_links:
    - from: "apps/mcp-server/src/stdio.ts"
      to: "buildMcpServer"
      via: "createMcpServices then connect"
      pattern: "buildMcpServer"
    - from: "apps/mcp-server/src/stdio.ts"
      to: "StdioServerTransport"
      via: "server.connect(transport)"
      pattern: "StdioServerTransport"
    - from: "apps/mcp-server/package.json"
      to: "apps/mcp-server/dist/stdio.js"
      via: "bin field"
      pattern: "kb-mcp-server"
---

<objective>
Wire stdio transport entrypoint with package bin for Cursor MCP spawn, stderr-only logging, and connection to the shared buildMcpServer() factory.

Purpose: Deliver MCP-01 — local MCP clients connect via stdio and invoke search_knowledge.
Output: Runnable `kb-mcp-server` bin, stderr logger, unit tests guarding stdout pollution.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mcp-search-server/03-CONTEXT.md
@.planning/phases/03-mcp-search-server/03-RESEARCH.md
@.planning/phases/03-mcp-search-server/03-PATTERNS.md
@apps/mcp-server/src/server.ts
@apps/mcp-server/src/services.ts

<interfaces>
From Plan 03-01:
```typescript
export function buildMcpServer(searchService: SearchService): McpServer;
export async function createMcpServices(): Promise<McpServices>;
```

From MCP SDK:
```typescript
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
// await server.connect(new StdioServerTransport());
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: stderr-only logger and stdio entrypoint</name>
  <read_first>
    - .planning/phases/03-mcp-search-server/03-CONTEXT.md (D-29, D-30, D-33)
    - .planning/phases/03-mcp-search-server/03-RESEARCH.md (Pattern 2, Pitfall 1)
    - .planning/research/PITFALLS.md (stdout pollution)
    - apps/mcp-server/src/server.ts
    - apps/mcp-server/src/services.ts
  </read_first>
  <files>
    apps/mcp-server/src/logger.ts
    apps/mcp-server/src/stdio.ts
  </files>
  <acceptance_criteria>
    - grep -v '^#' apps/mcp-server/src/stdio.ts | grep -c 'StdioServerTransport' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/stdio.ts | grep -c 'buildMcpServer' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/stdio.ts | grep -c 'createMcpServices' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/stdio.ts | grep -c 'console\\.log' | awk '{exit ($1==0)?0:1}'
    - grep -v '^#' apps/mcp-server/src/logger.ts | grep -c 'stderr' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`logger.ts`** per D-30:
    - Export `logInfo`, `logError` writing exclusively to `process.stderr.write` or `console.error`
    - Document: never use console.log in stdio entrypoint chain

    **`stdio.ts`** per D-29, D-33:
    ```typescript
    async function main(): Promise<void> {
      const { searchService } = await createMcpServices();
      const server = buildMcpServer(searchService);
      const transport = new StdioServerTransport();
      await server.connect(transport);
      logInfo("kb-mcp-server running on stdio");
    }
    main().catch((err) => { logError(err); process.exit(1); });
    ```
    - Import from `./services.js`, `./server.js`, `./logger.js`
    - **No** HTTP server, **no** express
    - Shebang not required (Node bin handles execution)
  </action>
  <verify>
    <automated>pnpm exec tsc -p apps/mcp-server/tsconfig.json --noEmit 2>&1</automated>
  </verify>
  <done>stdio.ts connects buildMcpServer to StdioServerTransport with stderr-only logging</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Package bin and stdout pollution tests</name>
  <read_first>
    - .planning/phases/03-mcp-search-server/03-PATTERNS.md (stdio.ts section)
    - apps/mcp-server/package.json
  </read_first>
  <files>
    apps/mcp-server/package.json
    apps/mcp-server/src/stdio.test.ts
  </files>
  <behavior>
    - Test 1: stdio.ts source contains no console.log (static analysis or readFileSync grep)
    - Test 2: package.json bin maps kb-mcp-server to dist/stdio.js
    - Test 3: logger functions write to stderr not stdout (mock process.stderr.write)
  </behavior>
  <acceptance_criteria>
    - grep -v '^#' apps/mcp-server/package.json | grep -c 'kb-mcp-server' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/package.json | grep -c 'stdio.js' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/package.json | grep -c 'dev:stdio' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/stdio.test.ts | grep -c 'console\\.log' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Update **`package.json`**:
    - `"bin": { "kb-mcp-server": "./dist/stdio.js" }`
    - Add script `"dev:stdio": "tsx src/stdio.ts"` for local manual testing
    - Ensure `"build"` emits `dist/stdio.js` (stdio.ts included in tsconfig)

    Update **`tsconfig.json`** if needed: set `"noEmit": false` for production build OR keep noEmit true and rely on tsx for dev — for bin to work in production, build must emit dist. Match backend: use tsc with emit for CI/build; dev uses tsx.

    Adjust tsconfig: `"noEmit": false` for build script OR add separate tsconfig.build.json — executor discretion; bin must resolve to compiled JS after `pnpm --filter @kb/mcp-server build`.

    **`stdio.test.ts`**: static/source tests for stdout pollution guards and bin config documentation.

    Add brief comment in stdio.ts for Cursor MCP config example:
    `"command": "pnpm", "args": ["--filter", "@kb/mcp-server", "dev:stdio"]` or path to built bin.
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server test 2>&1 && pnpm --filter @kb/mcp-server build 2>&1</automated>
  </verify>
  <done>kb-mcp-server bin configured; stdio tests pass; build emits dist/stdio.js</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cursor/host → stdio JSON-RPC | stdin/stdout reserved for MCP protocol |
| stdio process → stderr | Logging channel (safe for operators) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | T | stdout pollution breaks MCP | mitigate | console.log banned in stdio.ts; logger uses stderr only; test grep gate (D-30) |
| T-03-06 | D | Unhandled startup exception | mitigate | main().catch → logError + exit(1) on stderr |
| T-03-07 | I | Startup logs leak secrets | mitigate | Do not log CHERRYIN_API_KEY or query text at info level |
</threat_model>

<verification>
- `pnpm --filter @kb/mcp-server build` produces dist/stdio.js
- `pnpm --filter @kb/mcp-server test` passes
- Manual smoke: `pnpm --filter @kb/mcp-server dev:stdio` starts without stdout output (only stderr startup line)
</verification>

<success_criteria>
- MCP-01: stdio transport entrypoint and bin ready for Cursor MCP configuration
- D-30: stderr-only logging enforced
- D-24: uses shared buildMcpServer() — same tools as HTTP plan
</success_criteria>

<output>
After completion, create `.planning/phases/03-mcp-search-server/03-02-SUMMARY.md`
</output>
