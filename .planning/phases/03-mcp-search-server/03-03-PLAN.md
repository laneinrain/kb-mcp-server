---
phase: 03-mcp-search-server
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - packages/config/src/env.ts
  - .env.example
  - apps/mcp-server/package.json
  - apps/mcp-server/src/http.ts
  - apps/mcp-server/src/http.test.ts
  - package.json
autonomous: false
requirements:
  - MCP-02
must_haves:
  truths:
    - "MCP client can connect via Streamable HTTP at POST /mcp and invoke search_knowledge"
    - "HTTP entrypoint uses same buildMcpServer() as stdio — identical tool schema and behavior (D-24)"
    - "Server binds MCP_HTTP_HOST (default 127.0.0.1) and MCP_HTTP_PORT (default 3100)"
    - "Root dev script starts MCP HTTP alongside backend when operator runs pnpm dev"
    - "HTTP route tests verify /mcp endpoint responds to initialize request"
  artifacts:
    - path: "apps/mcp-server/src/http.ts"
      provides: "Streamable HTTP MCP entrypoint"
      contains: "StreamableHTTPServerTransport"
    - path: "packages/config/src/env.ts"
      provides: "MCP_HTTP_HOST and MCP_HTTP_PORT config"
      contains: "MCP_HTTP_HOST"
    - path: "apps/mcp-server/src/http.test.ts"
      provides: "HTTP route unit tests"
      contains: "/mcp"
    - path: ".env.example"
      provides: "Documented MCP HTTP defaults"
      contains: "MCP_HTTP"
  key_links:
    - from: "apps/mcp-server/src/http.ts"
      to: "buildMcpServer"
      via: "session init on isInitializeRequest"
      pattern: "buildMcpServer"
    - from: "apps/mcp-server/src/http.ts"
      to: "StreamableHTTPServerTransport"
      via: "POST /mcp handleRequest"
      pattern: "StreamableHTTPServerTransport"
    - from: "apps/mcp-server/src/http.ts"
      to: "loadConfig"
      via: "MCP_HTTP_HOST MCP_HTTP_PORT bind"
      pattern: "MCP_HTTP_HOST"
---

<objective>
Add Streamable HTTP transport at POST /mcp with config env vars, dev orchestration, route tests, and human E2E verification checkpoint.

Purpose: Deliver MCP-02 — remote MCP clients invoke the same search_knowledge tool via Streamable HTTP (not legacy SSE-only).
Output: Runnable HTTP entrypoint on localhost:3100, tests, updated .env.example, human E2E checkpoint.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mcp-search-server/03-CONTEXT.md
@.planning/phases/03-mcp-search-server/03-RESEARCH.md
@.planning/phases/03-mcp-search-server/03-PATTERNS.md
@apps/mcp-server/src/server.ts
@apps/mcp-server/src/services.ts
@apps/mcp-server/src/stdio.ts
@packages/config/src/env.ts
@package.json

<interfaces>
From Plan 03-01/03-02:
```typescript
export function buildMcpServer(searchService: SearchService): McpServer;
export async function createMcpServices(): Promise<McpServices>;
```

From MCP SDK (Streamable HTTP):
```typescript
import { StreamableHTTPServerTransport } from "@modelcontextprotocol/sdk/server/streamableHttp.js";
import { isInitializeRequest } from "@modelcontextprotocol/sdk/types.js";
// transport.handleRequest(req, res, req.body)
```

New config fields (Plan 03-03):
```typescript
MCP_HTTP_HOST: string; // default "127.0.0.1"
MCP_HTTP_PORT: number; // default 3100
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config env vars and HTTP entrypoint</name>
  <read_first>
    - .planning/phases/03-mcp-search-server/03-CONTEXT.md (D-29, D-31)
    - .planning/phases/03-mcp-search-server/03-RESEARCH.md (Pattern 3)
    - packages/config/src/env.ts
    - .env.example
    - apps/mcp-server/src/services.ts
  </read_first>
  <files>
    packages/config/src/env.ts
    .env.example
    apps/mcp-server/package.json
    apps/mcp-server/src/http.ts
  </files>
  <acceptance_criteria>
    - grep -v '^#' packages/config/src/env.ts | grep -c 'MCP_HTTP_HOST' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' packages/config/src/env.ts | grep -c 'MCP_HTTP_PORT' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/http.ts | grep -c 'StreamableHTTPServerTransport' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/http.ts | grep -c 'buildMcpServer' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/http.ts | grep -c "'/mcp'" | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' .env.example | grep -c 'MCP_HTTP' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`packages/config/src/env.ts`** per D-31:
    - Add `MCP_HTTP_HOST: z.string().default("127.0.0.1")`
    - Add `MCP_HTTP_PORT: z.coerce.number().default(3100)`
    - Rebuild `@kb/config` after change

    **`.env.example`**: add section:
    ```
    # --- MCP HTTP (Streamable HTTP transport, Phase 3) ---
    MCP_HTTP_HOST=127.0.0.1
    MCP_HTTP_PORT=3100
    ```

    Add **`express@^5`** to `@kb/mcp-server` dependencies (SDK examples use express.json()).

    **`http.ts`** per D-29, D-24, RESEARCH Pattern 3:
    - `createMcpServices()` once at startup — shared `searchService` reference
    - Express app with `express.json()`
    - Session map `Record<string, StreamableHTTPServerTransport>`
    - `POST /mcp`:
      - Reuse transport when `mcp-session-id` header matches
      - On new `isInitializeRequest`: create transport, `buildMcpServer(searchService)`, `server.connect(transport)`
      - Else 400 for invalid session
      - `await transport.handleRequest(req, res, req.body)`
    - Listen on `config.MCP_HTTP_HOST` / `config.MCP_HTTP_PORT`
    - Log bind URL to **stderr** only (`console.error` or logger from Plan 03-02)
    - Do **not** implement legacy SSE-only endpoint as primary

    Update **`package.json`** scripts: `"dev": "tsx watch src/http.ts"` for MCP HTTP dev server.

    Update **root `package.json` dev script** to include MCP server:
    - Extend concurrently/turbo to run `@kb/mcp-server` dev alongside `@kb/backend` after Chroma wait
    - Example: `turbo run dev --filter=@kb/backend --filter=@kb/mcp-server`
  </action>
  <verify>
    <automated>pnpm --filter @kb/config build 2>&1 && pnpm exec tsc -p apps/mcp-server/tsconfig.json --noEmit 2>&1</automated>
  </verify>
  <done>MCP HTTP server listens on configured localhost port with Streamable HTTP transport</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HTTP route tests</name>
  <read_first>
    - .planning/phases/03-mcp-search-server/03-RESEARCH.md (Pattern 3)
    - apps/backend/src/routes/search.test.ts (inject pattern)
    - apps/mcp-server/src/http.ts
  </read_first>
  <files>
    apps/mcp-server/src/http.test.ts
    apps/mcp-server/package.json
  </files>
  <behavior>
    - Test 1: POST /mcp without session returns 400 for non-initialize body
    - Test 2: POST /mcp with valid initialize JSON-RPC body returns 200 (or SDK-expected status)
    - Test 3: http.ts uses buildMcpServer (mock createMcpServices / SearchService)
    - Test 4: No legacy SSE-only transport as sole handler (grep absence of deprecated SSE-only pattern if applicable)
  </behavior>
  <acceptance_criteria>
    - grep -v '^#' apps/mcp-server/src/http.test.ts | grep -c '/mcp' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/package.json | grep -c 'supertest' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/http.test.ts | grep -c 'initialize' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Add **`supertest`** (or `light-my-request` if preferred) as devDependency for HTTP testing.

    **`http.test.ts`**:
    - Export `createHttpApp()` or test against app factory that accepts mocked `McpServices`
    - Mock `createMcpServices` to avoid live Chroma/CherryIn in unit tests
    - Send MCP initialize request payload to POST /mcp
    - Assert response status and session header behavior per SDK

    Refactor **`http.ts`** minimally if needed to export app factory for testability (e.g. `export async function createMcpHttpApp(): Promise<express.Application>`).
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server test 2>&1</automated>
  </verify>
  <done>HTTP route tests pass with mocked services; no live network required</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: MCP stdio and HTTP end-to-end verification</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 3 Success Criteria)
    - .planning/phases/03-mcp-search-server/03-CONTEXT.md
    - scripts/fixtures/sample.txt
  </read_first>
  <files>N/A — verification only</files>
  <acceptance_criteria>
    - Ingested corpus exists (sample.txt via REST or pnpm ingest)
    - stdio: MCP client or SDK test client invokes search_knowledge and receives ranked results
    - HTTP: POST /mcp initialize + tools/call search_knowledge returns same result shape as REST /api/v1/search
    - Results include score, text, documentId, filename, chunkIndex
    - Only search_knowledge tool listed — no upload/delete/index tools
    - MCP HTTP bound to 127.0.0.1:3100 by default
  </acceptance_criteria>
  <action>
    Pause for human verification after Tasks 1–2. Operator validates full Phase 3 MCP flow against live Chroma and CherryIn.
  </action>
  <what-built>
    Complete Phase 3 MCP server: search_knowledge tool via shared buildMcpServer(), stdio bin (kb-mcp-server), Streamable HTTP at POST /mcp, config env vars, dev orchestration.
  </what-built>
  <how-to-verify>
    1. Ensure `.env` has valid `CHERRYIN_API_KEY`; ingest corpus: `curl -F "file=@scripts/fixtures/sample.txt" http://127.0.0.1:3000/api/v1/documents` (backend running) OR `pnpm ingest scripts/fixtures/sample.txt`
    2. **stdio:** Configure Cursor MCP with `pnpm --filter @kb/mcp-server dev:stdio` (or built bin). Invoke `search_knowledge` with query related to sample content → ranked results with score, text, documentId, filename, chunkIndex
    3. **HTTP:** Run `pnpm --filter @kb/mcp-server dev` → server on `http://127.0.0.1:3100/mcp`
    4. Use MCP Inspector or SDK client: initialize session on POST /mcp, call `search_knowledge` → same result fields as step 2
    5. Compare MCP results with REST: `curl -X POST http://127.0.0.1:3000/api/v1/search -H "Content-Type: application/json" -d "{\"query\":\"sample content\"}"` — scores and documentIds should match for same query
    6. List tools → confirm **only** `search_knowledge` (MCP-05)
    7. Verify `top_k` default 5 and max 10 behavior via tool call
    8. Confirm stdio startup produces no stdout lines (only stderr)
  </how-to-verify>
  <verify>
    <automated>echo "Human checkpoint — run steps in how-to-verify"</automated>
  </verify>
  <done>Operator confirms Phase 3 success criteria: stdio + Streamable HTTP search with retrieval-only tools</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network client → POST /mcp | Untrusted HTTP on localhost (no auth Phase 3) |
| MCP tool input → SearchService | Same as stdio — untrusted queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | T | MCP HTTP exposed beyond localhost | mitigate | Default MCP_HTTP_HOST=127.0.0.1 (D-31, CONF-04 parity) |
| T-03-09 | S | Unauthenticated /mcp on LAN | accept | Localhost bind Phase 3; API key deferred Phase 4 |
| T-03-10 | D | Session map memory leak | mitigate | transport.onclose deletes session from map |
| T-03-11 | T | CSRF on POST /mcp | accept | Localhost-only; no browser cookie session |
</threat_model>

<verification>
- `pnpm --filter @kb/mcp-server test` passes
- `pnpm --filter @kb/config build` passes
- MCP HTTP starts on MCP_HTTP_PORT
- Human checkpoint confirms stdio + HTTP parity with REST search
</verification>

<success_criteria>
- MCP-02: Streamable HTTP transport at POST /mcp with same tools as stdio
- Phase 3 success criteria #1–#5 satisfied after human approval
- Config documented in .env.example
</success_criteria>

<output>
After completion, create `.planning/phases/03-mcp-search-server/03-03-SUMMARY.md`
</output>
