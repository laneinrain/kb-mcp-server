---
phase: 03-mcp-search-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mcp-server/package.json
  - apps/mcp-server/tsconfig.json
  - apps/mcp-server/vitest.config.ts
  - apps/mcp-server/src/services.ts
  - apps/mcp-server/src/server.ts
  - apps/mcp-server/src/server.test.ts
autonomous: true
requirements:
  - MCP-03
  - MCP-04
  - MCP-05
  - MCP-06
must_haves:
  truths:
    - "buildMcpServer() registers exactly one tool: search_knowledge"
    - "search_knowledge delegates to SearchService.search() — no direct Chroma or embed calls"
    - "Tool results include score, text snippet, documentId, filename, chunkIndex per MCP-04"
    - "top_k defaults to 5 with max 10 enforced via SearchService (MCP-06)"
    - "createMcpServices() wires SearchService using same factory pattern as backend"
  artifacts:
    - path: "apps/mcp-server/src/server.ts"
      provides: "Transport-agnostic MCP server factory with search_knowledge tool"
      contains: "buildMcpServer"
    - path: "apps/mcp-server/src/services.ts"
      provides: "MCP service bootstrap (SearchService only)"
      contains: "createMcpServices"
    - path: "apps/mcp-server/src/server.test.ts"
      provides: "Unit tests for tool registration and delegation"
      contains: "search_knowledge"
    - path: "apps/mcp-server/package.json"
      provides: "@kb/mcp-server workspace package with MCP SDK dep"
      contains: "@modelcontextprotocol/sdk"
  key_links:
    - from: "apps/mcp-server/src/server.ts"
      to: "SearchService.search"
      via: "search_knowledge tool handler"
      pattern: "searchService\\.search"
    - from: "apps/mcp-server/src/services.ts"
      to: "SearchService.create"
      via: "bootstrap factory per D-32"
      pattern: "SearchService\\.create"
    - from: "apps/mcp-server/src/server.ts"
      to: "@modelcontextprotocol/sdk/server/mcp.js"
      via: "McpServer.registerTool"
      pattern: "registerTool"
---

<objective>
Scaffold `@kb/mcp-server` with `createMcpServices()`, `buildMcpServer()` registering the `search_knowledge` tool, and unit tests proving SearchService delegation and retrieval-only surface.

Purpose: MCP-03 through MCP-06 require a shared server factory before any transport entrypoint; this plan delivers the tool contract both stdio and HTTP will use (D-24).
Output: Tested MCP server factory and service bootstrap; no transport wiring yet.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mcp-search-server/03-CONTEXT.md
@.planning/phases/03-mcp-search-server/03-RESEARCH.md
@.planning/phases/03-mcp-search-server/03-PATTERNS.md
@apps/backend/src/services.ts
@apps/backend/src/routes/search.ts
@packages/core/src/search/search-service.ts
@packages/core/src/search/types.ts

<interfaces>
From `@kb/core` (Phase 2):
```typescript
export class SearchService {
  static create(config: AppConfig, deps: {
    vectorStore: ChromaVectorStore;
    embeddingClient: EmbeddingClient;
  }): SearchService;
  search(query: string, options?: SearchOptions): Promise<SearchResult[]>;
}

export interface SearchOptions { collection?: string; topK?: number; }
export interface SearchResult {
  score: number;
  text: string;
  documentId: string;
  filename: string;
  chunkIndex: number;
}
```

From MCP SDK v1.29:
```typescript
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
// server.registerTool(name, { description, inputSchema }, handler)
// handler returns { content: [{ type: "text", text }], structuredContent?, isError? }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/mcp-server package</name>
  <read_first>
    - .planning/phases/03-mcp-search-server/03-PATTERNS.md (package.json section)
    - apps/backend/package.json
    - apps/backend/tsconfig.json
    - apps/backend/vitest.config.ts
    - pnpm-workspace.yaml
  </read_first>
  <files>
    apps/mcp-server/package.json
    apps/mcp-server/tsconfig.json
    apps/mcp-server/vitest.config.ts
  </files>
  <acceptance_criteria>
    - grep -v '^#' apps/mcp-server/package.json | grep -c '@modelcontextprotocol/sdk' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/package.json | grep -c '@kb/core' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/package.json | grep -c '"test"' | awk '{exit ($1>=1)?0:1}'
    - test -f apps/mcp-server/tsconfig.json
  </acceptance_criteria>
  <action>
    Create **`apps/mcp-server/`** workspace package:

    **`package.json`**:
    - `"name": "@kb/mcp-server"`, `"type": "module"`, `"private": true`
    - Dependencies: `@kb/config`, `@kb/core` (workspace), `@modelcontextprotocol/sdk@1.29.0`, `zod@^4.4.3`
    - DevDependencies: `@types/node`, `tsx`, `typescript`, `vitest` (mirror backend versions)
    - Scripts: `"build": "tsc -p tsconfig.json"`, `"test": "vitest run"`
    - Do **not** add bin or express yet — Plans 03-02/03-03

    **`tsconfig.json`**: extend `../../tsconfig.base.json`, `rootDir: ./src`, `outDir: ./dist`, `noEmit: true` (match backend dev pattern with tsx)

    **`vitest.config.ts`**: `include: ["src/**/*.test.ts"]`

    Run `pnpm install` from repo root.
  </action>
  <verify>
    <automated>pnpm install 2>&1 && pnpm exec tsc -p apps/mcp-server/tsconfig.json --noEmit 2>&1</automated>
  </verify>
  <done>@kb/mcp-server package exists with MCP SDK dependency and compiles empty src</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: createMcpServices and buildMcpServer</name>
  <read_first>
    - .planning/phases/03-mcp-search-server/03-CONTEXT.md (D-23 through D-28, D-32)
    - .planning/phases/03-mcp-search-server/03-RESEARCH.md (Pattern 1, Pattern 4)
    - .planning/phases/03-mcp-search-server/03-PATTERNS.md (services.ts, server.ts)
    - apps/backend/src/services.ts
    - apps/backend/src/routes/search.ts
  </read_first>
  <files>
    apps/mcp-server/src/services.ts
    apps/mcp-server/src/server.ts
    apps/mcp-server/src/server.test.ts
  </files>
  <behavior>
    - Test 1: buildMcpServer registers only search_knowledge (listTools or grep registerTool calls)
    - Test 2: search_knowledge handler calls searchService.search with query and mapped topK from top_k
    - Test 3: handler returns structuredContent.results with score, text, documentId, filename, chunkIndex
    - Test 4: empty query rejected (Zod validation error or isError response)
    - Test 5: no registerTool for upload, delete, index, ingest (MCP-05)
    - Test 6: server.ts does not import ChromaVectorStore or EmbeddingClient directly
  </behavior>
  <acceptance_criteria>
    - grep -v '^#' apps/mcp-server/src/server.ts | grep -c 'buildMcpServer' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/server.ts | grep -c 'search_knowledge' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/server.ts | grep -c 'searchService\\.search' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/server.ts | grep -c 'ChromaVectorStore\\|embedQuery\\|EmbeddingClient' | awk '{exit ($1==0)?0:1}'
    - grep -v '^#' apps/mcp-server/src/services.ts | grep -c 'createMcpServices' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/services.ts | grep -c 'IngestionService' | awk '{exit ($1==0)?0:1}'
  </acceptance_criteria>
  <action>
    **`services.ts`** per D-32:
    - Export `McpServices { config, searchService }`
    - `createMcpServices()`: loadConfig → ChromaVectorStore + EmbeddingClient → SearchService.create
    - **No** IngestionService, DocumentRegistry, uploadsDir

    **`server.ts`** per D-23, D-24, D-26, D-28:
    ```typescript
    export function buildMcpServer(searchService: SearchService): McpServer
    ```
    - `new McpServer({ name: "kb-mcp-server", version: "0.1.0" })`
    - Register **only** `search_knowledge` per D-25:
      - `inputSchema`: query (min 1, max 2000), top_k (int 1-10 optional), collection (optional)
      - Handler: `searchService.search(query, { topK: top_k, collection })`
      - Return `{ content: [{ type: "text", text: JSON.stringify({ results }) }], structuredContent: { results } }`
      - On error: `{ content: [...], isError: true }` — do not throw
    - Export for testing if needed (e.g. `export const SEARCH_TOOL_NAME = "search_knowledge"`)

    **`server.test.ts`**: mock SearchService with vi.fn(); test tool delegation and MCP-05 negative (no other tools). Use SDK `Client` with in-memory transport if practical, or test handler via documented SDK test pattern; fallback: export and invoke handler wrapper for unit test.

    Map snake_case `top_k` → camelCase `topK` at handler boundary (D-26). Rely on SearchService for default 5 / max 10 clamp (D-27, MCP-06).
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server test 2>&1</automated>
  </verify>
  <done>buildMcpServer passes unit tests; single retrieval tool delegates exclusively to SearchService</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool input → SearchService | Untrusted query strings from MCP clients |
| SearchService → CherryIn/Chroma | Outbound to configured localhost/API endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | Accidental ingest tools on MCP | mitigate | Register search_knowledge only; test asserts no upload/delete/index tool names (D-25, MCP-05) |
| T-03-02 | D | Unbounded top_k in tool schema | mitigate | Zod max 10 on top_k + SearchService hard cap (D-27) |
| T-03-03 | I | Tool handler logs query to stdout | mitigate | No console.log in server.ts; defer logging to stderr in stdio entry (Plan 03-02) |
| T-03-04 | E | Direct Chroma bypass in MCP layer | mitigate | server.ts imports SearchService only; grep gate rejects embed/Chroma imports |
</threat_model>

<verification>
- `pnpm --filter @kb/mcp-server test` passes
- `pnpm --filter @kb/mcp-server build` or tsc --noEmit passes
- buildMcpServer is exportable and ready for stdio/HTTP entrypoints in Plans 03-02/03-03
</verification>

<success_criteria>
- MCP-03: search_knowledge tool registered and delegates to SearchService
- MCP-04: results include score, text, documentId, filename, chunkIndex
- MCP-05: retrieval-only — no write tools
- MCP-06: bounded top_k and truncated snippets via SearchService
- D-24 factory ready for both transports
</success_criteria>

<output>
After completion, create `.planning/phases/03-mcp-search-server/03-01-SUMMARY.md`
</output>
