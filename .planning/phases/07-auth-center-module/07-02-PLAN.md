---
phase: 07-auth-center-module
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - apps/backend/package.json
  - apps/backend/src/services.ts
  - apps/backend/src/index.ts
  - apps/backend/src/routes/auth.ts
  - apps/backend/src/routes/auth.test.ts
  - packages/auth/README.md
autonomous: true
requirements:
  - AUTH-03
  - AUTH-06
must_haves:
  truths:
    - "POST /api/v1/auth/login accepts employeeId+password; calls provider.login only (D-06, D-21)"
    - "Mock CAS: valid 工号+非空密码 returns 200 + accessToken (D-22)"
    - "No POST /api/v1/auth/register on default AUTH_PROVIDER=cas path (D-05)"
    - "Auth routes public — no API_KEY preHandler (D-11 coexistence)"
    - "README documents MockCasAuthProvider → CasAuthProvider swap, CAS_MOCK, CAS_SERVER_URL (D-16, D-17, D-25, AUTH-06)"
    - "USER_AUTH_ENABLED=false → auth login route 404 (D-12)"
    - "Login password min 1 char enforced in Zod (D-07)"
    - "AuthProvider.login(employeeId,password) stable contract; CasAuthProvider stub for real CAS (D-18)"
    - "CAS maps to employee_id for Phase 8 user scope (D-19)"
    - "Login page stays 工号+password; no redirect-only CAS (D-20 superseded by D-24)"
  artifacts:
    - path: "apps/backend/src/routes/auth.ts"
      provides: "CAS login endpoint"
      contains: "/api/v1/auth/login"
    - path: "packages/auth/README.md"
      provides: "CAS swap documentation"
      contains: "MockCasAuthProvider"
  key_links:
    - from: "apps/backend/src/services.ts"
      to: "packages/auth/src/factory.ts"
      via: "createAuthProvider"
      pattern: "createAuthProvider"
---

<objective>
Wire **MockCasAuthProvider** into backend and expose **`POST /api/v1/auth/login`** (AUTH-03, AUTH-06). Document production CAS swap in README.

Output: Login API + integration tests; document routes unchanged (API_KEY only).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auth-center-module/07-CONTEXT.md
@apps/backend/src/index.ts
@apps/backend/src/routes/documents.ts
@packages/auth/src/index.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Password logged | High | Do not log request body on auth routes |
| Mock left on in prod | Medium | README warns CAS_MOCK=false + real CasAuthProvider |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire createAuthProvider in services</name>
  <read_first>
    - apps/backend/src/services.ts
    - packages/auth/src/factory.ts
    - packages/config/src/env.ts
  </read_first>
  <files>
    apps/backend/package.json
    apps/backend/src/services.ts
  </files>
  <acceptance_criteria>
    - grep -c '@kb/auth' apps/backend/package.json | awk '{exit ($1>=1)?0:1}'
    - grep -c 'authProvider' apps/backend/src/services.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'createAuthProvider' apps/backend/src/services.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Add `@kb/auth` workspace dep. Extend `AppServices`:
    - `authProvider: AuthProvider | null` — null when `!config.USER_AUTH_ENABLED`
    - Else `createAuthProvider({ dbPath: AUTH_SQLITE_PATH, jwtSecret, jwtExpiresInSeconds, authProvider: AUTH_PROVIDER, casMock: CAS_MOCK, casServerUrl: CAS_SERVER_URL })`
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend build 2>&1</automated>
  </verify>
  <done>Backend constructs MockCasAuthProvider when USER_AUTH_ENABLED + CAS_MOCK</done>
</task>

<task type="auto">
  <name>Task 2: Login route and index registration</name>
  <read_first>
    - .planning/phases/07-auth-center-module/07-CONTEXT.md (D-06, D-08, D-12)
    - apps/backend/src/routes/documents.ts
  </read_first>
  <files>
    apps/backend/src/routes/auth.ts
    apps/backend/src/index.ts
  </files>
  <acceptance_criteria>
    - grep -c '/api/v1/auth/login' apps/backend/src/routes/auth.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'employeeId' apps/backend/src/routes/auth.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'registerAuthRoutes' apps/backend/src/index.ts | awk '{exit ($1>=1)?0:1}'
    - grep -cv '/api/v1/auth/register' apps/backend/src/routes/auth.ts | awk '{exit ($1>=0)?0:1}'
  </acceptance_criteria>
  <action>
    **`routes/auth.ts`**: `registerAuthRoutes(app, { authProvider })`.

    **`POST /api/v1/auth/login`** (public, no routeOpts):
    - Zod body: `employeeId` string, `password` string min 1
    - Call `authProvider.login({ employeeId, password })`
    - Validation errors → 400 `"工号须为 4–10 位数字"` or empty password message
    - Success 200: `{ accessToken, tokenType: 'Bearer', expiresIn, user: { id, employeeId, email } }`

    **No register route** in default cas path (D-05).

    **`index.ts`**: call `registerAuthRoutes` after services; before bearer auth. When authProvider null, routes omitted or return 404.

    Do **not** modify document/search `routeOpts`.
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend build 2>&1</automated>
  </verify>
  <done>Login endpoint live when USER_AUTH_ENABLED</done>
</task>

<task type="auto">
  <name>Task 3: Auth route tests and README</name>
  <read_first>
    - apps/backend/src/routes/documents.test.ts
    - .planning/phases/07-auth-center-module/07-CONTEXT.md (D-16–D-19, D-25)
  </read_first>
  <files>
    apps/backend/src/routes/auth.test.ts
    packages/auth/README.md
  </files>
  <acceptance_criteria>
    - grep -c 'accessToken' apps/backend/src/routes/auth.test.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c '12345678' apps/backend/src/routes/auth.test.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'MockCasAuthProvider' packages/auth/README.md | awk '{exit ($1>=1)?0:1}'
    - grep -c 'CAS_MOCK' packages/auth/README.md | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`auth.test.ts`**: temp AUTH_SQLITE_PATH; USER_AUTH_ENABLED mock:
    1. POST login `{ employeeId: '12345678', password: 'any' }` → 200 + accessToken
    2. POST login invalid employeeId `123` → 400
    3. POST login empty password → 400
    4. Second login same employeeId → same user id (JIT idempotent)

    **`README.md`**: AuthProvider contract; MockCasAuthProvider default; swap to CasAuthProvider + CAS_SERVER_URL; env table; createJwtPreHandler for Phase 8; production 统一 CAS note (D-16, D-19); AUTH_PROVIDER=local optional future register only.
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend test 2>&1</automated>
  </verify>
  <done>Login API tested; README complete for AUTH-06</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @kb/backend test`
2. curl login with USER_AUTH_ENABLED=true returns JWT
</verification>

## PLAN COMPLETE
