---
phase: 07-auth-center-module
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - apps/backend/package.json
  - apps/backend/src/services.ts
  - apps/backend/src/index.ts
  - apps/backend/src/routes/auth.ts
  - apps/backend/src/routes/auth.test.ts
  - packages/auth/README.md
autonomous: true
requirements:
  - AUTH-03
  - AUTH-06
must_haves:
  truths:
    - "POST /api/v1/auth/register creates user and returns accessToken when USER_AUTH_ENABLED (AUTH-03)"
    - "POST /api/v1/auth/login returns JWT for valid credentials; 401 for invalid (AUTH-03)"
    - "Auth routes are public (no API_KEY bearer required) even when AUTH_ENABLED=true"
    - "packages/auth/README.md documents AuthProvider swap, env vars, and createJwtPreHandler hook (AUTH-06)"
    - "Response shape includes accessToken, tokenType Bearer, expiresIn, and user { id, username, email }"
    - "Existing API_KEY bearer on /api/v1/documents unchanged in this phase"
  artifacts:
    - path: "apps/backend/src/routes/auth.ts"
      provides: "Register and login REST endpoints"
      contains: "/api/v1/auth/register"
    - path: "apps/backend/src/routes/auth.test.ts"
      provides: "Auth route integration tests"
      contains: "auth/register"
    - path: "packages/auth/README.md"
      provides: "Swap documentation for production IdP"
      contains: "AuthProvider"
  key_links:
    - from: "apps/backend/src/index.ts"
      to: "apps/backend/src/routes/auth.ts"
      via: "registerAuthRoutes when USER_AUTH_ENABLED"
      pattern: "registerAuthRoutes"
    - from: "apps/backend/src/routes/auth.ts"
      to: "@kb/auth LocalAuthProvider"
      via: "provider.register / provider.login"
      pattern: "LocalAuthProvider"
---

<objective>
Mount public register/login REST endpoints on the backend and document `@kb/auth` swap points (AUTH-03, AUTH-06).

Purpose: Operators and Phase 8 web UI can obtain user JWTs; auth center remains a replaceable module.
Output: Working `/api/v1/auth/*` routes, backend tests, README.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auth-center-module/07-RESEARCH.md
@.planning/phases/07-auth-center-module/07-PATTERNS.md
@apps/backend/src/index.ts
@apps/backend/src/services.ts
@apps/backend/src/routes/documents.ts
@packages/auth/src/index.ts

<interfaces>
Register request body:
```typescript
{ username: string; password: string; email?: string }
```

Login request body:
```typescript
{ username: string; password: string }
```

Success response (both register 201 and login 200):
```typescript
{
  accessToken: string;
  tokenType: "Bearer";
  expiresIn: number;
  user: { id: string; username: string; email: string | null };
}
```
</interfaces>
</context>

<threat_model>
| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| Credential stuffing | Medium | Document rate-limit as production hardening in README; generic 401 message on login |
| Mass account creation | Medium | README notes admin deployment should gate register in production |
| JWT leakage in logs | High | Fastify logger must not log request bodies on auth routes |
| Auth bypass on doc routes | High | Document routes still use API_KEY only — user JWT enforcement is Phase 8 (explicit in README) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire LocalAuthProvider in backend services</name>
  <read_first>
    - apps/backend/src/services.ts
    - packages/auth/src/local-auth-provider.ts
    - packages/config/src/env.ts
  </read_first>
  <files>
    apps/backend/package.json
    apps/backend/src/services.ts
  </files>
  <acceptance_criteria>
    - grep -c '@kb/auth' apps/backend/package.json | awk '{exit ($1>=1)?0:1}'
    - grep -c 'LocalAuthProvider' apps/backend/src/services.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'authProvider' apps/backend/src/services.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Add **`@kb/auth": "workspace:*"`** to `apps/backend/package.json`.

    Extend **`AppServices`** in `services.ts`:
    - `authProvider: LocalAuthProvider | null` — null when `!config.USER_AUTH_ENABLED`
    - When enabled: `new LocalAuthProvider({ dbPath: config.AUTH_SQLITE_PATH, jwtSecret: config.JWT_SECRET!, jwtExpiresInSeconds: config.JWT_EXPIRES_IN })`

    Export authProvider from `createAppServices()` return value.
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend build 2>&1</automated>
  </verify>
  <done>Backend constructs LocalAuthProvider when USER_AUTH_ENABLED</done>
</task>

<task type="auto">
  <name>Task 2: Implement auth REST routes and register in index</name>
  <read_first>
    - apps/backend/src/routes/documents.ts
    - apps/backend/src/routes/search.ts
    - apps/backend/src/index.ts
    - .planning/phases/07-auth-center-module/07-RESEARCH.md
  </read_first>
  <files>
    apps/backend/src/routes/auth.ts
    apps/backend/src/index.ts
  </files>
  <acceptance_criteria>
    - grep -c '/api/v1/auth/register' apps/backend/src/routes/auth.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c '/api/v1/auth/login' apps/backend/src/routes/auth.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'registerAuthRoutes' apps/backend/src/index.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'z.object' apps/backend/src/routes/auth.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Create **`apps/backend/src/routes/auth.ts`**:

    `registerAuthRoutes(app, { authProvider })` — no-op if authProvider null.

    **`POST /api/v1/auth/register`** (public, no routeOpts preHandler):
    - Zod body: username min 3 max 32, password min 8, optional email
    - Call `authProvider.register`
    - Duplicate user → 409 `{ error: "conflict", message: "Username already exists" }`
    - Success 201 with tokens + user (call login flow or return tokens from register)

    **`POST /api/v1/auth/login`** (public):
    - Invalid credentials → 401 `{ error: "unauthorized", message: "Invalid username or password" }`
    - Success 200 with tokens + user

    Wire in **`index.ts`**: after `createAppServices`, call `registerAuthRoutes(app, { authProvider: services.authProvider })` **before** bearer auth registration. Auth routes must NOT use `apiRouteOpts`.

    Do **not** change document/search routeOpts in this plan.
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend build 2>&1</automated>
  </verify>
  <done>Register and login endpoints mounted when USER_AUTH_ENABLED</done>
</task>

<task type="auto">
  <name>Task 3: Auth route tests and @kb/auth README</name>
  <read_first>
    - apps/backend/src/routes/documents.test.ts
    - apps/backend/src/routes/auth.ts
    - .planning/phases/07-auth-center-module/07-RESEARCH.md
    - packages/core/README.md
  </read_first>
  <files>
    apps/backend/src/routes/auth.test.ts
    packages/auth/README.md
  </files>
  <acceptance_criteria>
    - grep -c 'auth/register' apps/backend/src/routes/auth.test.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'accessToken' apps/backend/src/routes/auth.test.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'AuthProvider' packages/auth/README.md | awk '{exit ($1>=1)?0:1}'
    - grep -c 'createJwtPreHandler' packages/auth/README.md | awk '{exit ($1>=1)?0:1}'
    - grep -c 'JWT_SECRET' packages/auth/README.md | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`auth.test.ts`**: Build Fastify app with USER_AUTH_ENABLED mock config and temp AUTH_SQLITE_PATH. Tests:
    1. POST register → 201, body has accessToken and user.id
    2. POST register duplicate username → 409
    3. POST login valid → 200 + accessToken
    4. POST login wrong password → 401
    5. When USER_AUTH_ENABLED false, register route returns 404 or 503 (document chosen behavior in route — prefer 404 "User auth disabled")

    **`packages/auth/README.md`** (AUTH-06):
    - Overview: independent auth center module
    - **AuthProvider contract** — implement for OAuth/OIDC/LDAP
    - **LocalAuthProvider** — default SQLite + bcrypt + JWT
    - **Env vars**: USER_AUTH_ENABLED, JWT_SECRET, JWT_EXPIRES_IN, AUTH_SQLITE_PATH
    - **Backend integration**: `createJwtPreHandler(provider)` for Phase 8 route protection
    - **Production swap steps**: (1) implement AuthProvider, (2) replace factory in services.ts, (3) configure env
    - Note: register endpoint should be disabled/gated in production deployments
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend test 2>&1</automated>
  </verify>
  <done>Auth routes tested; README documents swap points</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @kb/backend test` — auth tests pass
2. Manual: `USER_AUTH_ENABLED=true JWT_SECRET=...` — curl register/login returns JWT
3. Document routes still work without user JWT (API_KEY behavior unchanged)
</verification>

## PLAN COMPLETE
