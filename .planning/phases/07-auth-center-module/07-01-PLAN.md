---
phase: 07-auth-center-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/auth/package.json
  - packages/auth/tsconfig.json
  - packages/auth/src/schema.sql
  - packages/auth/src/types.ts
  - packages/auth/src/user-store.ts
  - packages/auth/src/jwt.ts
  - packages/auth/src/mock-cas-auth-provider.ts
  - packages/auth/src/cas-auth-provider.ts
  - packages/auth/src/factory.ts
  - packages/auth/src/fastify.ts
  - packages/auth/src/index.ts
  - packages/auth/src/mock-cas-auth-provider.test.ts
  - packages/config/src/env.ts
  - packages/config/src/env.test.ts
  - .env.example
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "AuthProvider + MockCasAuthProvider exported; login via CAS adapter not bcrypt (D-14, D-21, D-22)"
    - "employee_id 4-10 digits; password_hash nullable; auth_source column (D-01, D-02, D-03, D-23)"
    - "Mock login succeeds for valid employeeId + non-empty password; JIT creates user (D-22, D-23)"
    - "JWT sub=user UUID; 7-day default via JWT_EXPIRES_IN (D-09)"
    - "Config USER_AUTH_ENABLED, JWT_SECRET, AUTH_PROVIDER=cas, CAS_MOCK, CAS_SERVER_URL, AUTH_SQLITE_PATH (D-13, D-26)"
    - "email column nullable on users; no register route in default path (D-04, D-05)"
    - "jose HS256 JWT; no bcrypt on CAS login path (D-15)"
  artifacts:
    - path: "packages/auth/src/mock-cas-auth-provider.ts"
      provides: "Mock CAS login"
      contains: "class MockCasAuthProvider"
    - path: "packages/auth/src/schema.sql"
      provides: "users with employee_id"
      contains: "employee_id"
    - path: "packages/config/src/env.ts"
      provides: "CAS_MOCK env"
      contains: "CAS_MOCK"
  key_links:
    - from: "packages/auth/src/factory.ts"
      to: "packages/auth/src/mock-cas-auth-provider.ts"
      via: "createAuthProvider(config)"
      pattern: "CAS_MOCK"
---

<objective>
Scaffold `@kb/auth` with **MockCasAuthProvider**, employeeId user store, JWT helpers, and config env vars per CONTEXT D-01–D-03, D-13–D-15, D-21–D-23, D-26 (AUTH-01, AUTH-02).

Output: Buildable package + config tests; no backend routes yet.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auth-center-module/07-CONTEXT.md
@.planning/phases/07-auth-center-module/07-RESEARCH.md
@packages/core/src/registry/document-registry.ts
@packages/config/src/env.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Plaintext passwords in DB | High | CAS path stores no password; password_hash nullable |
| Weak JWT secret | High | JWT_SECRET min 32 chars when USER_AUTH_ENABLED |
| Mock CAS in production | Medium | README + CAS_MOCK default true only for scaffold; document CAS_MOCK=false for prod |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: @kb/auth package and employeeId user store</name>
  <read_first>
    - .planning/phases/07-auth-center-module/07-CONTEXT.md (D-01–D-03, D-23)
    - packages/core/package.json
    - packages/core/src/registry/document-registry.ts
  </read_first>
  <files>
    packages/auth/package.json
    packages/auth/tsconfig.json
    packages/auth/src/schema.sql
    packages/auth/src/types.ts
    packages/auth/src/user-store.ts
    packages/auth/src/index.ts
  </files>
  <acceptance_criteria>
    - grep -c 'employee_id' packages/auth/src/schema.sql | awk '{exit ($1>=1)?0:1}'
    - grep -c 'auth_source' packages/auth/src/schema.sql | awk '{exit ($1>=1)?0:1}'
    - grep -c 'export interface AuthProvider' packages/auth/src/types.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'employeeId' packages/auth/src/types.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'findByEmployeeId' packages/auth/src/user-store.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Create **`packages/auth`** (`@kb/auth`): deps `better-sqlite3`, `jose`, `@kb/config`; build copies `schema.sql` to dist.

    **`schema.sql`**: users(id, employee_id UNIQUE, email nullable, password_hash nullable, auth_source TEXT NOT NULL DEFAULT 'cas', created_at).

    **`types.ts`**: `AuthUser`, `LoginInput { employeeId, password }`, `AuthTokens`, `AuthProvider` with `login`, `validateAccessToken`, `getUserById`.

    **`user-store.ts`**: `openAuthDatabase`, `upsertCasUser({ employeeId, authSource: 'cas' })`, `findByEmployeeId`, `findById`.

    Export store helpers from **`index.ts`** (providers added in Task 2).
  </action>
  <verify>
    <automated>pnpm --filter @kb/auth build 2>&1</automated>
  </verify>
  <done>User store keyed by employee_id with nullable password_hash</done>
</task>

<task type="auto">
  <name>Task 2: MockCasAuthProvider, JWT, factory, CasAuthProvider stub</name>
  <read_first>
    - .planning/phases/07-auth-center-module/07-CONTEXT.md (D-21, D-22, D-09, D-25)
    - packages/auth/src/user-store.ts
  </read_first>
  <files>
    packages/auth/src/jwt.ts
    packages/auth/src/mock-cas-auth-provider.ts
    packages/auth/src/cas-auth-provider.ts
    packages/auth/src/factory.ts
    packages/auth/src/fastify.ts
    packages/auth/src/index.ts
    packages/auth/src/mock-cas-auth-provider.test.ts
  </files>
  <acceptance_criteria>
    - grep -c 'class MockCasAuthProvider' packages/auth/src/mock-cas-auth-provider.test.ts | awk '{exit ($1>=0)?0:1}'
    - grep -c '\\d{4,10}' packages/auth/src/mock-cas-auth-provider.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'createAuthProvider' packages/auth/src/factory.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'createJwtPreHandler' packages/auth/src/fastify.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`jwt.ts`**: signAccessToken / verifyAccessToken (jose HS256, payload sub + employeeId claim optional).

    **`MockCasAuthProvider`**: `login({ employeeId, password })` — reject if !/^\d{4,10}$/.test(employeeId) or !password.trim(); else upsert user auth_source='cas', return JWT (expiresIn from config).

    **`CasAuthProvider`**: stub class — when instantiated without mock, `login` throws `Error('Real CAS not implemented — set CAS_MOCK=true or implement CasAuthProvider')` (D-25 placeholder).

    **`factory.ts`**: `createAuthProvider({ dbPath, jwtSecret, jwtExpiresInSeconds, authProvider, casMock, casServerUrl })` returns MockCasAuthProvider when `authProvider==='cas' && casMock`, else CasAuthProvider stub when casMock false (requires casServerUrl in config validation).

    **`fastify.ts`**: `createJwtPreHandler(provider)`.

    **Tests**: mock success JIT user; invalid employeeId fails; empty password fails; validateAccessToken roundtrip.
  </action>
  <verify>
    <automated>pnpm --filter @kb/auth test 2>&1</automated>
  </verify>
  <done>MockCasAuthProvider tested; factory selects mock by default</done>
</task>

<task type="auto">
  <name>Task 3: @kb/config auth + CAS env vars</name>
  <read_first>
    - packages/config/src/env.ts
    - .planning/phases/07-auth-center-module/07-CONTEXT.md (D-11, D-13, D-26)
  </read_first>
  <files>
    packages/config/src/env.ts
    packages/config/src/env.test.ts
    .env.example
  </files>
  <acceptance_criteria>
    - grep -c 'USER_AUTH_ENABLED' packages/config/src/env.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'CAS_MOCK' packages/config/src/env.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'AUTH_PROVIDER' packages/config/src/env.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'CAS_MOCK' .env.example | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Add env fields:
    - `USER_AUTH_ENABLED` (default false)
    - `JWT_SECRET` (min 32, required when USER_AUTH_ENABLED)
    - `JWT_EXPIRES_IN` (default 604800)
    - `AUTH_SQLITE_PATH` (resolve repo-relative)
    - `AUTH_PROVIDER` enum `cas|local` default `cas`
    - `CAS_MOCK` boolean default `true`
    - `CAS_SERVER_URL` optional URL; superRefine: when USER_AUTH_ENABLED && !CAS_MOCK && !CAS_SERVER_URL → issue

    Add `JWT_SECRET` to SECRET_KEYS. Update tests and `.env.example` with User auth + CAS mock section (D-11 separate from AUTH_ENABLED).
  </action>
  <verify>
    <automated>pnpm --filter @kb/config test 2>&1</automated>
  </verify>
  <done>Config supports CAS mock auth env vars</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @kb/auth build && pnpm --filter @kb/auth test`
2. `pnpm --filter @kb/config test`
</verification>

## PLAN COMPLETE
