---
phase: 07-auth-center-module
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/pages/LoginPage.tsx
  - apps/web/src/api/auth.ts
  - apps/web/src/lib/auth-token.ts
  - apps/web/src/api/client.ts
  - apps/web/src/App.css
autonomous: true
requirements:
  - WEB-01
must_haves:
  truths:
    - "Login page 简体中文 labels 工号 and 密码 (D-08)"
    - "Submit calls POST /api/v1/auth/login with employeeId+password (D-06, D-24)"
    - "Success stores localStorage kb_access_token and navigates to / (D-10, D-24)"
    - "Unauthenticated admin entry redirects to login view (minimal WEB-03, D-24)"
    - "Password input required non-empty before submit (D-07)"
  artifacts:
    - path: "apps/web/src/pages/LoginPage.tsx"
      provides: "Login form UI"
      contains: "工号"
    - path: "apps/web/src/lib/auth-token.ts"
      provides: "JWT storage"
      contains: "kb_access_token"
    - path: "apps/web/src/api/auth.ts"
      provides: "login API client"
      contains: "/api/v1/auth/login"
  key_links:
    - from: "apps/web/src/pages/LoginPage.tsx"
      to: "apps/web/src/api/auth.ts"
      via: "login()"
      pattern: "employeeId"
    - from: "apps/web/src/main.tsx"
      to: "apps/web/src/pages/LoginPage.tsx"
      via: "pathname /login or missing token"
      pattern: "LoginPage"
---

<objective>
Deliver **简体中文登录页** (WEB-01 accelerated): 工号 + 密码 → mock CAS login API → JWT in localStorage → admin access (D-08, D-10, D-24).

Output: Login UX end-to-end with backend; ApiKeyModal remains fallback when no JWT and AUTH_ENABLED API key mode.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-auth-center-module/07-CONTEXT.md
@apps/web/src/api/client.ts
@apps/web/src/components/ApiKeyModal.tsx
@apps/web/src/App.tsx
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| JWT in localStorage XSS | Medium | Same as existing API key pattern; document Phase 8 hardening |
| Token logged | High | Do not console.log tokens |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Auth token helpers and login API client</name>
  <read_first>
    - apps/web/src/api/client.ts
    - apps/web/src/api/documents.ts
  </read_first>
  <files>
    apps/web/src/lib/auth-token.ts
    apps/web/src/api/auth.ts
  </files>
  <acceptance_criteria>
    - grep -c 'kb_access_token' apps/web/src/lib/auth-token.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'employeeId' apps/web/src/api/auth.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c '/api/v1/auth/login' apps/web/src/api/auth.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`lib/auth-token.ts`**: `getAccessToken()`, `setAccessToken(token)`, `clearAccessToken()`, `hasAccessToken()` using localStorage key **`kb_access_token`**.

    **`api/auth.ts`**: `login(employeeId: string, password: string)` POST `/api/v1/auth/login` JSON body; parse `{ accessToken, user }`; throw ApiError on failure with server message.
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>Token helpers and login client ready</done>
</task>

<task type="auto">
  <name>Task 2: LoginPage component</name>
  <read_first>
    - .planning/phases/07-auth-center-module/07-CONTEXT.md (D-08, D-22)
    - apps/web/src/components/ApiKeyModal.tsx
    - apps/web/src/App.css
  </read_first>
  <files>
    apps/web/src/pages/LoginPage.tsx
    apps/web/src/App.css
  </files>
  <acceptance_criteria>
    - grep -c '工号' apps/web/src/pages/LoginPage.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c '密码' apps/web/src/pages/LoginPage.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c '12345678' apps/web/src/pages/LoginPage.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c 'login(' apps/web/src/pages/LoginPage.tsx | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`LoginPage.tsx`**: centered card (reuse App.css modal/panel patterns):
    - Title: **登录**
    - Fields: **工号** (input type text, pattern `[0-9]*`, placeholder `12345678`), **密码** (type password)
    - Submit button **登录**; on submit call `login()`, `setAccessToken`, `window.location.href = '/'`
    - Show error message from ApiError in 简体中文
    - Helper text: 开发环境 CAS 模拟鉴权，合法工号即可登录

    Add `.login-page` styles in **App.css** (match existing admin aesthetic).
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>Login page UI complete</done>
</task>

<task type="auto">
  <name>Task 3: Route gate and client Bearer JWT</name>
  <read_first>
    - apps/web/src/main.tsx
    - apps/web/src/App.tsx
    - apps/web/src/api/client.ts
  </read_first>
  <files>
    apps/web/src/main.tsx
    apps/web/src/App.tsx
    apps/web/src/api/client.ts
  </files>
  <acceptance_criteria>
    - grep -c 'LoginPage' apps/web/src/main.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c 'getAccessToken' apps/web/src/api/client.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'kb_access_token' apps/web/src/api/client.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`main.tsx`**: If `window.location.pathname === '/login'` OR `!hasAccessToken()` (when `import.meta.env.VITE_USER_AUTH !== 'false'`, default require login for admin), render `<LoginPage />`; else render `<App />`. Add optional `VITE_USER_AUTH=false` to skip gate in dev without user auth.

    **`client.ts`**: In `authHeaders()`, if `getAccessToken()` present, return `Authorization: Bearer ${token}`; else fall back to sessionStorage API key (D-10 coexistence).

    **`App.tsx`**: Keep ApiKeyModal for 401 when JWT missing/invalid and AUTH_ENABLED API key path still active.

    Document in code comment: full JWT-only mode completes in Phase 8 (AUTH-04).
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>Login gate + JWT Bearer on API requests when token present</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @kb/web build`
2. Manual: USER_AUTH_ENABLED=true, open web → login → enter 12345678 + password → lands on admin with token in localStorage
</verification>

## PLAN COMPLETE
