---
phase: 09-filename-content-hash-dedup
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - apps/backend/src/routes/documents.ts
  - apps/backend/src/routes/documents.test.ts
  - apps/backend/src/routes/documents.dedup.test.ts
  - apps/web/src/api/documents.ts
  - apps/web/src/types.ts
  - apps/web/src/components/UploadPanel.tsx
  - apps/cli/src/api-client.ts
  - apps/cli/src/commands/ingest.ts
  - apps/cli/src/commands/ingest.test.ts
autonomous: true
requirements:
  - INGE-13
must_haves:
  truths:
    - "POST /api/v1/documents returns outcome in JSON (INGE-13, D-09)"
    - "201 for created, 200 for unchanged/replaced (D-09)"
    - "Web UploadPanel shows 简体中文 message per outcome (D-10)"
    - "CLI logs outcome in ingest output (D-11)"
  artifacts:
    - path: "apps/backend/src/routes/documents.dedup.test.ts"
      provides: "HTTP outcome integration tests"
      contains: "unchanged"
    - path: "apps/web/src/components/UploadPanel.tsx"
      provides: "outcome-aware success copy"
      contains: "内容未变"
  key_links:
    - from: "apps/backend/src/routes/documents.ts"
      to: "packages/core/src/ingestion/ingestion-service.ts"
      via: "ingest result outcome"
      pattern: "outcome"
---

<objective>
Surface **`outcome`** through REST, Web, and CLI (INGE-13, D-09–D-11).

Depends on 09-02 `IngestResult.outcome`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filename-content-hash-dedup/09-CONTEXT.md
@apps/backend/src/routes/documents.ts
@apps/web/src/components/UploadPanel.tsx
@apps/cli/src/commands/ingest.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Web treats unchanged as error | Low | Explicit success copy for unchanged |
| CLI scripts miss outcome | Low | Log JSON field in stderr |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Backend POST documents outcome + status codes</name>
  <read_first>
    - apps/backend/src/routes/documents.ts
    - apps/backend/src/routes/documents.test.ts
    - .planning/phases/09-filename-content-hash-dedup/09-CONTEXT.md (D-09)
  </read_first>
  <files>
    apps/backend/src/routes/documents.ts
    apps/backend/src/routes/documents.test.ts
    apps/backend/src/routes/documents.dedup.test.ts
  </files>
  <acceptance_criteria>
    - grep -c "outcome" apps/backend/src/routes/documents.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/backend test -- dedup exits 0
  </acceptance_criteria>
  <action>
    Map **`ingestionService.ingest`** result to response body including **`outcome`**.

    HTTP status: **201** if `created`, **200** if `unchanged` or `replaced`.

    Add **`documents.dedup.test.ts`**: mock ingestionService returning each outcome; assert status + body.

    Update existing documents.test.ts if response shape changed.
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend test 2>&1</automated>
  </verify>
  <done>REST API exposes outcome</done>
</task>

<task type="auto">
  <name>Task 2: Web upload UX for outcomes</name>
  <read_first>
    - apps/web/src/components/UploadPanel.tsx
    - apps/web/src/api/documents.ts
    - .planning/phases/09-filename-content-hash-dedup/09-CONTEXT.md (D-10)
  </read_first>
  <files>
    apps/web/src/api/documents.ts
    apps/web/src/types.ts
    apps/web/src/components/UploadPanel.tsx
  </files>
  <acceptance_criteria>
    - grep -c "内容未变" apps/web/src/components/UploadPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c "outcome" apps/web/src/api/documents.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/web build exits 0
  </acceptance_criteria>
  <action>
    Extend **`UploadResult`** with `outcome`.

    **`UploadPanel`**: onSuccess switch on outcome:
    - `created` → 已上传 {filename}
    - `unchanged` → 内容未变，跳过索引（{filename}）
    - `replaced` → 内容已更新并重新索引（{filename}）

    Still invalidate documents query on all success paths.
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>Web shows operator-friendly dedup messages</done>
</task>

<task type="auto">
  <name>Task 3: CLI outcome logging</name>
  <read_first>
    - apps/cli/src/commands/ingest.ts
    - apps/cli/src/api-client.ts
    - apps/cli/src/commands/ingest.test.ts
  </read_first>
  <files>
    apps/cli/src/api-client.ts
    apps/cli/src/commands/ingest.ts
    apps/cli/src/commands/ingest.test.ts
  </files>
  <acceptance_criteria>
    - grep -c "outcome" apps/cli/src/commands/ingest.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/cli test exits 0
  </acceptance_criteria>
  <action>
    Parse **`outcome`** from REST upload response in api-client.

    Include **`outcome`** in CLI success log JSON alongside documentId/chunkCount.

    Update ingest tests for new response field.
  </action>
  <verify>
    <automated>pnpm --filter @kb/cli test 2>&1</automated>
  </verify>
  <done>CLI operators see dedup outcome</done>
</task>

</tasks>

<verification>
- [ ] Re-upload same file via Web shows 内容未变
- [ ] Edit file re-upload shows 内容已更新
- [ ] `pnpm test` passes for @kb/core, @kb/backend, @kb/cli, @kb/web build
</verification>
