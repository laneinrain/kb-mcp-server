---
phase: 09-filename-content-hash-dedup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/registry/schema.sql
  - packages/core/src/registry/types.ts
  - packages/core/src/registry/migrations.ts
  - packages/core/src/registry/document-registry.ts
  - packages/core/src/registry/document-registry.test.ts
  - packages/core/src/ingestion/content-hash.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - INGE-10
must_haves:
  truths:
    - "documents.content_hash column exists (INGE-10, D-12)"
    - "findByUserAndFilename(userId, filename) returns latest row (D-03)"
    - "computeContentHash is deterministic on normalized text (D-01, D-02)"
    - "deriveDocumentIdForUserFile(userId, filename) stable (D-04)"
  artifacts:
    - path: "packages/core/src/ingestion/content-hash.ts"
      provides: "hash + normalize helpers"
      contains: "computeContentHash"
    - path: "packages/core/src/registry/document-registry.ts"
      provides: "lookup by user+filename"
      contains: "findByUserAndFilename"
  key_links:
    - from: "packages/core/src/registry/migrations.ts"
      to: "documents.content_hash"
      via: "ALTER TABLE"
      pattern: "content_hash"
---

<objective>
Add **`content_hash`** column, migration, registry lookup by **`(user_id, filename)`**, and shared hash helpers (INGE-10).

Output: `@kb/core` builds with registry tests; no ingest branching yet.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filename-content-hash-dedup/09-CONTEXT.md
@.planning/phases/09-filename-content-hash-dedup/09-RESEARCH.md
@packages/core/src/registry/document-registry.ts
@packages/core/src/registry/migrations.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Duplicate legacy rows block unique index | Medium | Probe before CREATE UNIQUE INDEX; skip + log (D-13) |
| Hash non-determinism | High | Pure string normalize; unit tests with fixtures |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: content_hash column and migration</name>
  <read_first>
    - packages/core/src/registry/schema.sql
    - packages/core/src/registry/migrations.ts
    - .planning/phases/09-filename-content-hash-dedup/09-CONTEXT.md (D-12, D-13)
  </read_first>
  <files>
    packages/core/src/registry/schema.sql
    packages/core/src/registry/migrations.ts
    packages/core/src/registry/types.ts
  </files>
  <acceptance_criteria>
    - grep -c "content_hash" packages/core/src/registry/schema.sql | awk '{exit ($1>=1)?0:1}'
    - grep -c "content_hash" packages/core/src/registry/migrations.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/core build exits 0
  </acceptance_criteria>
  <action>
    Add **`content_hash TEXT`** to `documents` in schema.sql.

    Extend **`runRegistryMigrations`**: ADD COLUMN if missing; attempt unique index on `(user_id, filename)` only when no duplicate pairs exist (COUNT query); log skipped index if duplicates.

    Add **`contentHash?: string | null`** to `DocumentRecord` / `RegisterDocumentInput`.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core build 2>&1</automated>
  </verify>
  <done>Registry schema supports content_hash</done>
</task>

<task type="auto">
  <name>Task 2: Hash helpers and stable document id</name>
  <read_first>
    - packages/core/src/ingestion/ingestion-service.ts
    - .planning/phases/09-filename-content-hash-dedup/09-CONTEXT.md (D-01, D-04)
  </read_first>
  <files>
    packages/core/src/ingestion/content-hash.ts
    packages/core/src/index.ts
  </files>
  <acceptance_criteria>
    - grep -c "computeContentHash" packages/core/src/ingestion/content-hash.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "deriveDocumentIdForUserFile" packages/core/src/ingestion/content-hash.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/core test -- content-hash exits 0
  </acceptance_criteria>
  <action>
    Create **`content-hash.ts`**:
    - `normalizeParsedText(text: string): string` — `\r\n` → `\n`, `trim()`
    - `computeContentHash(text: string): string` — sha256 hex of normalized text
    - `deriveDocumentIdForUserFile(userId: string, filename: string): string` — sha256(userId + '\0' + filename)

    Export from **`packages/core/src/index.ts`**.

    Add unit tests for normalize + hash stability.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test 2>&1</automated>
  </verify>
  <done>Hash helpers tested and exported</done>
</task>

<task type="auto">
  <name>Task 3: findByUserAndFilename registry API</name>
  <read_first>
    - packages/core/src/registry/document-registry.ts
    - packages/core/src/registry/document-registry.test.ts
  </read_first>
  <files>
    packages/core/src/registry/document-registry.ts
    packages/core/src/registry/document-registry.test.ts
  </files>
  <acceptance_criteria>
    - grep -c "findByUserAndFilename" packages/core/src/registry/document-registry.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/core test -- document-registry exits 0
  </acceptance_criteria>
  <action>
    Add **`findByUserAndFilename(userId, filename)`** — SELECT … WHERE user_id = ? AND filename = ? ORDER BY updated_at DESC LIMIT 1.

    Update **`registerDocument`** / upsert to persist `content_hash` when provided.

    Tests: insert two rows same user+filename (if index skipped), lookup returns latest; hash round-trip on register.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test 2>&1</automated>
  </verify>
  <done>Registry lookup by user+filename ready for ingest</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @kb/core build` passes
- [ ] Migration idempotent on existing dev DB
</verification>
