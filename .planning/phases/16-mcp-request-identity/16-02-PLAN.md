---
phase: 16-mcp-request-identity
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-03
files_modified:
  - apps/mcp-server/src/auth/mcp-request-context.ts
  - apps/mcp-server/src/http.ts
  - apps/mcp-server/src/http.test.ts
autonomous: true
requirements:
  - PLAT-05
  - PLAT-09
must_haves:
  truths:
    - "POST /mcp returns 401 without Bearer when USER_AUTH_ENABLED=true (D-11, D-09)"
    - "POST /mcp with valid JWT initializes session successfully (D-11)"
    - "Each handleRequest wrapped in AsyncLocalStorage with McpCallerContext (D-13)"
    - "GET/DELETE /mcp require Bearer when user auth active (D-12)"
  artifacts:
    - path: "apps/mcp-server/src/auth/mcp-request-context.ts"
      provides: "mcpCallerStorage, getMcpCallerContext"
      contains: "AsyncLocalStorage"
    - path: "apps/mcp-server/src/http.ts"
      provides: "HTTP auth gate"
      contains: "resolve"
  key_links:
    - from: "apps/mcp-server/src/http.ts"
      to: "apps/mcp-server/src/auth/mcp-auth-resolver.ts"
      via: "authResolver.resolve"
      pattern: "authResolver"
    - from: "apps/mcp-server/src/http.ts"
      to: "apps/mcp-server/src/auth/mcp-request-context.ts"
      via: "mcpCallerStorage.run"
      pattern: "mcpCallerStorage"
---

<objective>
Wire **HTTP `/mcp` auth middleware** — validate Bearer on every request, 401 on failure, expose context via AsyncLocalStorage (PLAT-05, PLAT-09).

Depends on 16-01 resolver and 16-03 `createMcpServices` auth deps.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-mcp-request-identity/16-CONTEXT.md
@.planning/phases/16-mcp-request-identity/16-RESEARCH.md
@apps/mcp-server/src/http.ts
@apps/mcp-server/src/http.test.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Unauthenticated session init | High | Auth before creating transport / handleRequest |
| ALS context leak across requests | Medium | `storage.run` per request only |
| Breaking existing tests | Medium | mockServices with USER_AUTH_ENABLED=false default |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: AsyncLocalStorage request context module</name>
  <read_first>
    - .planning/phases/16-mcp-request-identity/16-CONTEXT.md (D-13)
  </read_first>
  <files>
    apps/mcp-server/src/auth/mcp-request-context.ts
  </files>
  <acceptance_criteria>
    - grep -c "AsyncLocalStorage" apps/mcp-server/src/auth/mcp-request-context.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "getMcpCallerContext" apps/mcp-server/src/auth/mcp-request-context.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
  **`mcp-request-context.ts`**:
  ```typescript
  export const mcpCallerStorage = new AsyncLocalStorage<McpCallerContext>();
  export function getMcpCallerContext(): McpCallerContext {
    const ctx = mcpCallerStorage.getStore();
    if (!ctx) throw new Error("MCP caller context not available");
    return ctx;
  }
  export function runWithMcpCallerContext<T>(ctx: McpCallerContext, fn: () => T): T {
    return mcpCallerStorage.run(ctx, fn);
  }
  ```
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server build 2>&1</automated>
  </verify>
  <done>ALS helper ready for HTTP and Phase 17 tools</done>
</task>

<task type="auto">
  <name>Task 2: HTTP auth gate on all /mcp handlers</name>
  <read_first>
    - apps/mcp-server/src/http.ts
    - apps/mcp-server/src/services.ts (McpServices shape from 16-03)
  </read_first>
  <files>
    apps/mcp-server/src/http.ts
    apps/mcp-server/src/http.test.ts
  </files>
  <acceptance_criteria>
    - grep -c "authResolver" apps/mcp-server/src/http.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "runWithMcpCallerContext" apps/mcp-server/src/http.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "401" apps/mcp-server/src/http.test.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/mcp-server test -- http exits 0
  </acceptance_criteria>
  <action>
  **`http.ts`** changes:

  1. `createMcpHttpApp(services)` uses `services.authResolver` from extended `McpServices`.

  2. Helper `extractBearerToken(req): string | undefined` from `Authorization` header.

  3. Helper `async function authenticateRequest(req, res): Promise<McpCallerContext | null>`:
     - calls `authResolver.resolve(extractBearerToken(req))`
     - on `McpAuthError` → `res.status(401).json({ error: "unauthorized", message })` return null

  4. **`handleMcpPost`**: authenticate first; wrap `transport.handleRequest` in `runWithMcpCallerContext(ctx, () => ...)`.

  5. **`handleMcpGet`** / **`handleMcpDelete`**: authenticate before session lookup when `USER_AUTH_ENABLED`.

  6. Session init path: auth runs before `new StreamableHTTPServerTransport` + `buildMcpServer`.

  **`http.test.ts`** additions:
  - Mock `McpServices` with `USER_AUTH_ENABLED: true`, mock `authResolver` that throws without token
  - Test: POST initialize without Bearer → 401
  - Test: POST initialize with mock valid context → 200 + session (existing flow)
  - Existing tests pass with `USER_AUTH_ENABLED: false` mock config
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server test -- http 2>&1</automated>
  </verify>
  <done>HTTP MCP enforces Bearer auth per request</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @kb/mcp-server test -- http`
2. All prior HTTP session tests still pass with auth disabled mock
3. New 401 test passes with auth enabled mock
</verification>

## PLAN COMPLETE
