---
phase: 16-mcp-request-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/auth/src/bearer-resolver.ts
  - packages/auth/src/bearer-resolver.test.ts
  - packages/auth/src/index.ts
  - apps/backend/src/auth.ts
  - apps/mcp-server/package.json
  - apps/mcp-server/src/auth/types.ts
  - apps/mcp-server/src/auth/mcp-auth-resolver.ts
  - apps/mcp-server/src/auth/mcp-auth-resolver.test.ts
autonomous: true
requirements:
  - PLAT-04
  - PLAT-07
  - PLAT-08
  - PLAT-09
must_haves:
  truths:
    - "resolveBearerToken in @kb/auth handles JWT, API_KEY, and USER_AUTH_ENABLED=false (D-01)"
    - "Backend createProtectedRouteOpts uses shared resolver without behavior change (D-02)"
    - "McpCallerContext includes authMode, optional authUser, optional allowedDocumentIds (D-04–D-07)"
    - "McpAuthError thrown on missing/invalid token when user auth active (D-08)"
  artifacts:
    - path: "packages/auth/src/bearer-resolver.ts"
      provides: "resolveBearerToken"
      contains: "resolveBearerToken"
    - path: "apps/mcp-server/src/auth/mcp-auth-resolver.ts"
      provides: "McpAuthResolver.resolve"
      contains: "McpCallerContext"
    - path: "apps/mcp-server/src/auth/types.ts"
      provides: "McpAuthMode"
      contains: "allowedDocumentIds"
  key_links:
    - from: "apps/mcp-server/src/auth/mcp-auth-resolver.ts"
      to: "packages/auth/src/bearer-resolver.ts"
      via: "resolveBearerToken"
      pattern: "resolveBearerToken"
    - from: "apps/mcp-server/src/auth/mcp-auth-resolver.ts"
      to: "packages/core"
      via: "listDocumentsForUser"
      pattern: "listDocumentsForUser"
---

<objective>
Extract shared **`resolveBearerToken`** to `@kb/auth` and implement **`McpAuthResolver`** + **`McpCallerContext`** in `@kb/mcp-server` (PLAT-04 auth layer, PLAT-07/08/09).

Output: resolver unit-tested; backend auth refactored; MCP package depends on `@kb/auth`. No HTTP/stdio wiring yet.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-mcp-request-identity/16-CONTEXT.md
@.planning/phases/16-mcp-request-identity/16-RESEARCH.md
@apps/backend/src/auth.ts
@apps/backend/src/routes/search.ts
@packages/auth/src/mock-cas-auth-provider.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Backend auth regression | High | Same 401 messages; run backend tests |
| Token logged | High | Never log raw Bearer token |
| ACL set computed wrong | Medium | Mirror search.ts listDocumentsForUser exactly |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: resolveBearerToken in @kb/auth</name>
  <read_first>
    - apps/backend/src/auth.ts (createProtectedRouteOpts logic)
    - .planning/phases/16-mcp-request-identity/16-RESEARCH.md
  </read_first>
  <files>
    packages/auth/src/bearer-resolver.ts
    packages/auth/src/bearer-resolver.test.ts
    packages/auth/src/index.ts
    apps/backend/src/auth.ts
  </files>
  <acceptance_criteria>
    - grep -c "resolveBearerToken" packages/auth/src/bearer-resolver.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "resolveBearerToken" apps/backend/src/auth.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/auth test exits 0
  </acceptance_criteria>
  <action>
  **`bearer-resolver.ts`**: Export `BearerAuthError` (statusCode 401), `BearerAuthMode`, `BearerAuthResult`, `resolveBearerToken(token, deps)`.

  Logic per D-01 / research table:
  - `userAuthEnabled=false` → `{ mode: "none" }` (ignore token)
  - missing token when user auth → throw `BearerAuthError("Missing Bearer token")`
  - try `authProvider.validateAccessToken(token)` → `{ mode: "user", user }`
  - catch → if `authEnabled && apiKey && token === apiKey` → `{ mode: "service" }`
  - else throw `BearerAuthError("Invalid or expired token")`

  **`auth.ts`**: Refactor `createProtectedRouteOpts` preHandler to call `resolveBearerToken` and map to `request.authUser` / `request.authMode`.

  **`bearer-resolver.test.ts`**: JWT roundtrip, API_KEY service, missing token, invalid token, userAuth disabled.

  Export from **`index.ts`**.
  </action>
  <verify>
    <automated>pnpm --filter @kb/auth test && pnpm --filter @kb/backend test 2>&1</automated>
  </verify>
  <done>Shared bearer resolution exported and backend refactored</done>
</task>

<task type="auto">
  <name>Task 2: McpCallerContext types and McpAuthResolver</name>
  <read_first>
    - .planning/phases/16-mcp-request-identity/16-CONTEXT.md (D-04–D-07)
    - apps/backend/src/routes/search.ts (allowedDocumentIds)
  </read_first>
  <files>
    apps/mcp-server/package.json
    apps/mcp-server/src/auth/types.ts
    apps/mcp-server/src/auth/mcp-auth-resolver.ts
    apps/mcp-server/src/auth/mcp-auth-resolver.test.ts
  </files>
  <acceptance_criteria>
    - grep -c "@kb/auth" apps/mcp-server/package.json | awk '{exit ($1>=1)?0:1}'
    - grep -c "McpCallerContext" apps/mcp-server/src/auth/types.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "listDocumentsForUser" apps/mcp-server/src/auth/mcp-auth-resolver.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/mcp-server test -- mcp-auth-resolver exits 0
  </acceptance_criteria>
  <action>
  Add **`"@kb/auth": "workspace:*"`** to mcp-server dependencies; `pnpm install`.

  **`types.ts`**: `McpAuthMode`, `McpCallerContext` per D-04.

  **`mcp-auth-resolver.ts`**:
  ```typescript
  export class McpAuthError extends Error { statusCode = 401; }

  export interface McpAuthResolverDeps {
    config: AppConfig;
    authProvider: AuthProvider | null;
    registry: DocumentRegistry;
    systemUserId: string | null;
  }

  export class McpAuthResolver {
    constructor(private deps: McpAuthResolverDeps) {}
    async resolve(token: string | undefined): Promise<McpCallerContext>
  }
  ```

  - Call `resolveBearerToken` with deps from config
  - `mode: "none"` → `{ authMode: "global" }`
  - `mode: "service"` → `{ authMode: "service" }`
  - `mode: "user"` → `{ authMode: "user", authUser, allowedDocumentIds: Set from listDocumentsForUser }`
  - Map `BearerAuthError` → `McpAuthError` same message

  **`mcp-auth-resolver.test.ts`**: mock registry + MockCasAuthProvider; global mode, user ACL set, service global, missing token throws.
  </action>
  <verify>
    <automated>pnpm install && pnpm --filter @kb/mcp-server test -- mcp-auth-resolver 2>&1</automated>
  </verify>
  <done>McpAuthResolver maps bearer → caller context with ACL prep</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @kb/auth test`
2. `pnpm --filter @kb/backend test`
3. `pnpm --filter @kb/mcp-server test -- mcp-auth-resolver`
</verification>

## PLAN COMPLETE
