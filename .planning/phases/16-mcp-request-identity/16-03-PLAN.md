---
phase: 16-mcp-request-identity
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - apps/mcp-server/src/services.ts
  - apps/mcp-server/src/stdio.ts
  - apps/mcp-server/src/stdio.test.ts
autonomous: true
requirements:
  - PLAT-06
  - PLAT-08
  - PLAT-09
must_haves:
  truths:
    - "createMcpServices bootstraps authProvider and systemUserId when USER_AUTH_ENABLED (D-18)"
    - "McpServices exports authResolver, registry, systemUserId (D-20)"
    - "stdio resolves MCP_USER_TOKEN at startup when user auth enabled (D-15, D-16)"
    - "stdio exits on missing/invalid token when USER_AUTH_ENABLED=true (D-10)"
    - "USER_AUTH_ENABLED=false ignores MCP_USER_TOKEN, global context (D-17)"
  artifacts:
    - path: "apps/mcp-server/src/services.ts"
      provides: "extended McpServices"
      contains: "authResolver"
    - path: "apps/mcp-server/src/stdio.ts"
      provides: "stdio token bootstrap"
      contains: "MCP_USER_TOKEN"
  key_links:
    - from: "apps/mcp-server/src/services.ts"
      to: "apps/backend/src/services.ts"
      via: "createAuthProvider pattern"
      pattern: "createAuthProvider"
    - from: "apps/mcp-server/src/stdio.ts"
      to: "apps/mcp-server/src/auth/mcp-auth-resolver.ts"
      via: "authResolver.resolve"
      pattern: "MCP_USER_TOKEN"
---

<objective>
Extend **`createMcpServices`** with auth/registry wiring and bootstrap **stdio** with `MCP_USER_TOKEN` (PLAT-06, PLAT-08, PLAT-09).

Depends on 16-01 `McpAuthResolver`. Enables 16-02 HTTP wiring.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-mcp-request-identity/16-CONTEXT.md
@.planning/phases/16-mcp-request-identity/16-RESEARCH.md
@apps/mcp-server/src/services.ts
@apps/mcp-server/src/stdio.ts
@apps/backend/src/services.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| stdio starts without auth in prod | High | exit(1) on resolve failure |
| Duplicate auth DB open logic bugs | Medium | Mirror backend services.ts exactly |
| MCP_USER_TOKEN in logs | High | Never log token value |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend createMcpServices with auth deps</name>
  <read_first>
    - apps/backend/src/services.ts
    - apps/mcp-server/src/services.ts
  </read_first>
  <files>
    apps/mcp-server/src/services.ts
  </files>
  <acceptance_criteria>
    - grep -c "authResolver" apps/mcp-server/src/services.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "createAuthProvider" apps/mcp-server/src/services.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "systemUserId" apps/mcp-server/src/services.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/mcp-server build exits 0
  </acceptance_criteria>
  <action>
  Extend **`McpServices`**:
  ```typescript
  export interface McpServices {
    config: AppConfig;
    searchService: SearchService;
    contextService: ContextService;
    registry: DocumentRegistry;
    authProvider: AuthProvider | null;
    systemUserId: string | null;
    authResolver: McpAuthResolver;
  }
  ```

  **`createMcpServices()`** — mirror backend auth bootstrap block:
  - When `USER_AUTH_ENABLED`: open auth DB, `ensureSystemUser`, optional admin seed if `CAS_MOCK`, `createAuthProvider`
  - `initSettingsStore(config, systemUserId ? { systemUserId } : undefined)`
  - `registry = getDocumentRegistry(settingsStore.db)`
  - `authResolver = new McpAuthResolver({ config, authProvider, registry, systemUserId })`

  Export registry from services (needed by resolver, already internal).
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server build 2>&1</automated>
  </verify>
  <done>McpServices includes authResolver and registry</done>
</task>

<task type="auto">
  <name>Task 2: stdio MCP_USER_TOKEN bootstrap</name>
  <read_first>
    - apps/mcp-server/src/stdio.ts
    - .planning/phases/16-mcp-request-identity/16-CONTEXT.md (D-15–D-17)
  </read_first>
  <files>
    apps/mcp-server/src/stdio.ts
    apps/mcp-server/src/stdio.test.ts
  </files>
  <acceptance_criteria>
    - grep -c "MCP_USER_TOKEN" apps/mcp-server/src/stdio.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "authResolver" apps/mcp-server/src/stdio.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/mcp-server test -- stdio exits 0
  </acceptance_criteria>
  <action>
  **`stdio.ts`** `main()`:

  1. `const services = await createMcpServices()`
  2. When `services.config.USER_AUTH_ENABLED`:
     - `const token = process.env.MCP_USER_TOKEN`
     - `const ctx = await services.authResolver.resolve(token)` — catch `McpAuthError`, log, `process.exit(1)`
  3. When auth disabled: `ctx = { authMode: "global" }`
  4. Store `stdioCallerContext = ctx` module-level
  5. Wrap `server.connect(transport)` tool invocations: export `getStdioCallerContext()` or seed ALS — for Phase 17, set `mcpCallerStorage.enterWith(ctx)` before connect OR wrap server build to use getter

  Minimal Phase 16: call `mcpCallerStorage.enterWith(ctx)` once before `server.connect` (stdio single-user).

  Update comment block with `MCP_USER_TOKEN` env example.

  **`stdio.test.ts`**: test helper that calls resolver logic — mock `createMcpServices`; verify missing token path throws/exit (use vi.spyOn process.exit).
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server test -- stdio 2>&1</automated>
  </verify>
  <done>stdio binds single user via MCP_USER_TOKEN</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @kb/mcp-server build`
2. `pnpm --filter @kb/mcp-server test -- stdio`
3. Manual: `USER_AUTH_ENABLED=false` stdio still starts without token
</verification>

## PLAN COMPLETE
