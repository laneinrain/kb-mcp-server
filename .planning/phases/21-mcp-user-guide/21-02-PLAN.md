---
phase: 21-mcp-user-guide
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - apps/web/src/components/HelpPanel.tsx
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03
  - DOCS-05
must_haves:
  truths:
    - "Cursor HTTP + stdio JWT recipes present (D-03, D-04)"
    - "CodeBuddy equivalent HTTP Bearer recipe present (D-06, D-07)"
    - "Auth matrix table in Help (D-11 / DOCS-05)"
  artifacts:
    - path: "apps/web/src/components/HelpPanel.tsx"
      provides: "Cursor and CodeBuddy MCP recipes"
      contains: "Authorization"
  key_links:
    - from: "apps/web/src/components/HelpPanel.tsx"
      to: "README.md MCP section"
      via: "ported examples"
      pattern: "3100/mcp"
---

<objective>
Add Cursor + CodeBuddy MCP JWT configuration blocks and the auth matrix to HelpPanel (DOCS-02, DOCS-03, DOCS-05).

Output: Web build green; no secrets in examples.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-mcp-user-guide/21-CONTEXT.md
@.planning/phases/21-mcp-user-guide/21-RESEARCH.md
@apps/web/src/components/HelpPanel.tsx
@README.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| User pastes JWT into committed mcp.json | Medium | Explicit warning in copy near examples |
| Wrong CodeBuddy field names | Low | Describe semantics not proprietary UI only |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Client recipes + auth matrix</name>
  <read_first>
    - apps/web/src/components/HelpPanel.tsx
    - README.md
  </read_first>
  <files>
    apps/web/src/components/HelpPanel.tsx
  </files>
  <acceptance_criteria>
    - grep -c "Cursor" apps/web/src/components/HelpPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c "CodeBuddy" apps/web/src/components/HelpPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c "MCP_USER_TOKEN" apps/web/src/components/HelpPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c "API_KEY" apps/web/src/components/HelpPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/web build exits 0
  </acceptance_criteria>
  <action>
  Fill Cursor HTTP/stdio sections (D-03–D-05), CodeBuddy parity section (D-06–D-07), and auth matrix table (D-11). Use `<pre>` JSON with `<jwt>` placeholders.
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>Client JWT recipes documented in Help</done>
</task>

</tasks>

<verification>
  <automated>pnpm --filter @kb/web build 2>&1</automated>
</verification>

<success_criteria>
- Operator can configure Cursor/CodeBuddy MCP with JWT from Help alone
</success_criteria>

<output>
After completion, create `.planning/phases/21-mcp-user-guide/21-02-SUMMARY.md`
</output>
