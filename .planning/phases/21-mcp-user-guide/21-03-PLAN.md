---
phase: 21-mcp-user-guide
plan: 03
type: execute
wave: 3
depends_on:
  - 21-01
files_modified:
  - apps/web/src/components/HelpPanel.tsx
  - apps/web/src/App.css
autonomous: true
requirements:
  - DOCS-04
must_haves:
  truths:
    - "Copy current JWT button uses getAccessToken (D-08)"
    - "Success/error status and security notes shown (D-09, D-10)"
    - "pnpm --filter @kb/web build succeeds"
  artifacts:
    - path: "apps/web/src/components/HelpPanel.tsx"
      provides: "copy JWT control"
      contains: "getAccessToken"
  key_links:
    - from: "apps/web/src/components/HelpPanel.tsx"
      to: "apps/web/src/lib/auth-token.ts"
      via: "getAccessToken"
      pattern: "getAccessToken"
---

<objective>
Add「复制当前登录 JWT」control with security/expiry notes (DOCS-04).

Output: Web build green; HelpPanel is a client component with useState for copy status.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-mcp-user-guide/21-CONTEXT.md
@.planning/phases/21-mcp-user-guide/21-RESEARCH.md
@apps/web/src/components/HelpPanel.tsx
@apps/web/src/lib/auth-token.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Token leaked via UI | Medium | Warn; do not display full token in DOM by default |
| Clipboard fails on insecure origin | Low | Fallback + error message |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Copy JWT button + notes</name>
  <read_first>
    - apps/web/src/components/HelpPanel.tsx
    - apps/web/src/lib/auth-token.ts
  </read_first>
  <files>
    apps/web/src/components/HelpPanel.tsx
    apps/web/src/App.css
  </files>
  <acceptance_criteria>
    - grep -c "getAccessToken" apps/web/src/components/HelpPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c "复制当前登录 JWT" apps/web/src/components/HelpPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c "勿提交" apps/web/src/components/HelpPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/web build exits 0
  </acceptance_criteria>
  <action>
  Convert HelpPanel to use client state; implement copy button (D-08–D-10); never render raw JWT in the page body (clipboard only). Security notes in 简体中文.
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>Users can copy JWT into MCP client config</done>
</task>

</tasks>

<verification>
  <automated>pnpm --filter @kb/web build 2>&1</automated>
</verification>

<success_criteria>
- Copy JWT works from Help without exposing token on screen
</success_criteria>

<output>
After completion, create `.planning/phases/21-mcp-user-guide/21-03-SUMMARY.md`
</output>
