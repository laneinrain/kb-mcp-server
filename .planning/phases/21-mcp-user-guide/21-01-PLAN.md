---
phase: 21-mcp-user-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/HelpPanel.tsx
  - apps/web/src/App.css
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "HelpPanel has structured sections: 快速开始, 鉴权, MCP 工具 (D-01)"
    - "pnpm --filter @kb/web build succeeds"
  artifacts:
    - path: "apps/web/src/components/HelpPanel.tsx"
      provides: "User Guide structure"
      contains: "快速开始"
  key_links:
    - from: "apps/web/src/components/HelpPanel.tsx"
      to: "apps/web/src/App.css"
      via: "help-section styles"
      pattern: "help-panel"
---

<objective>
Restructure HelpPanel into a multi-section 中文 User Guide skeleton (DOCS-01). Cursor/CodeBuddy/JWT copy can be stub headings filled in 21-02/21-03, or fully drafted here if cleaner — prefer complete 快速开始 + 鉴权 + 工具 in this plan; leave client JSON to 21-02.

Output: Web build green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-mcp-user-guide/21-CONTEXT.md
@.planning/phases/21-mcp-user-guide/21-RESEARCH.md
@apps/web/src/components/HelpPanel.tsx
@apps/web/src/App.css
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Outdated ports/commands | Low | Match README :3000/:3100/:5173 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Restructure HelpPanel sections</name>
  <read_first>
    - apps/web/src/components/HelpPanel.tsx
    - apps/web/src/App.css
    - README.md
  </read_first>
  <files>
    apps/web/src/components/HelpPanel.tsx
    apps/web/src/App.css
  </files>
  <acceptance_criteria>
    - grep -c "快速开始" apps/web/src/components/HelpPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c "search_knowledge" apps/web/src/components/HelpPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c "read_around" apps/web/src/components/HelpPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/web build exits 0
  </acceptance_criteria>
  <action>
  Replace the short numbered list with sectioned guide (D-01): 快速开始（上传/搜索）、鉴权矩阵摘要（can be brief stub if 21-02 expands）、MCP 工具三表。Add light `.help-section` / `pre.mono` CSS as needed (D-02).
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>Help is a real User Guide skeleton</done>
</task>

</tasks>

<verification>
  <automated>pnpm --filter @kb/web build 2>&1</automated>
</verification>

<success_criteria>
- Users see structured help beyond the old 4-step list
</success_criteria>

<output>
After completion, create `.planning/phases/21-mcp-user-guide/21-01-SUMMARY.md`
</output>
