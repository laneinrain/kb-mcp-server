---
phase: 19-model-settings-core
plan: 03
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - packages/core/src/embeddings/embedding-client.ts
  - packages/core/src/embeddings/embedding-client.test.ts
  - packages/core/src/search/search-service.ts
  - packages/core/src/search/search-service.test.ts
  - apps/backend/src/services.ts
  - apps/backend/src/routes/health.ts
  - apps/mcp-server/src/services.ts
autonomous: true
requirements:
  - CONF-06
must_haves:
  truths:
    - "EmbeddingClient uses runtime model getter / config.EMBEDDING_MODEL (D-11)"
    - "SearchService.search reads settingsStore.getModelConfig when provided (D-12)"
    - "Backend + MCP createApp/McpServices pass settingsStore into SearchService + EmbeddingClient (D-14)"
    - "Health embeddings reports settings embeddingModel (D-15)"
  artifacts:
    - path: "packages/core/src/search/search-service.ts"
      provides: "runtime model config"
      contains: "getModelConfig"
    - path: "packages/core/src/embeddings/embedding-client.ts"
      provides: "dynamic embedding model"
      contains: "getEmbeddingModel"
  key_links:
    - from: "apps/backend/src/services.ts"
      to: "packages/core/src/search/search-service.ts"
      via: "settingsStore"
      pattern: "settingsStore"
    - from: "apps/mcp-server/src/services.ts"
      to: "packages/core/src/search/search-service.ts"
      via: "settingsStore"
      pattern: "settingsStore"
---

<objective>
Wire embedding + search/rerank to **runtime settings** (CONF-06) so PATCH `/settings/models` affects subsequent searches in-process without restart.

Output: core + backend + mcp-related unit tests green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-model-settings-core/19-CONTEXT.md
@.planning/phases/19-model-settings-core/19-RESEARCH.md
@packages/core/src/embeddings/embedding-client.ts
@packages/core/src/search/search-service.ts
@apps/backend/src/services.ts
@apps/mcp-server/src/services.ts
@apps/backend/src/routes/health.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Silent use of wrong embedding model after PATCH | High | Per-call getter; tests assert model arg |
| MCP process stale if only Backend patched | Medium | Both processes read same SQLite; document restart MCP if long-lived cache (none expected) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: EmbeddingClient dynamic model</name>
  <read_first>
    - packages/core/src/embeddings/embedding-client.ts
    - packages/core/src/embeddings/embedding-client.test.ts
  </read_first>
  <files>
    packages/core/src/embeddings/embedding-client.ts
    packages/core/src/embeddings/embedding-client.test.ts
  </files>
  <acceptance_criteria>
    - grep -c "EMBEDDING_MODEL" packages/core/src/embeddings/embedding-client.ts | awk '{exit ($1==0)?0:1}' || true
    - File no longer hardcodes local const model string as sole source — uses getter or config.EMBEDDING_MODEL
    - pnpm --filter @kb/core test -- src/embeddings/embedding-client.test.ts exits 0
  </acceptance_criteria>
  <action>
  Implement D-11: remove hardcoded model constant; accept `getEmbeddingModel?: () => string` defaulting to `() => config.EMBEDDING_MODEL`. Pass `model: this.getEmbeddingModel()` into `embeddings.create`. Update tests to assert the model string used.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test -- src/embeddings/embedding-client.test.ts 2>&1</automated>
  </verify>
  <done>Embedding model is runtime-selectable</done>
</task>

<task type="auto">
  <name>Task 2: SearchService reads settings per search</name>
  <read_first>
    - packages/core/src/search/search-service.ts
    - packages/core/src/search/search-service.test.ts
  </read_first>
  <files>
    packages/core/src/search/search-service.ts
    packages/core/src/search/search-service.test.ts
  </files>
  <acceptance_criteria>
    - grep -c "getModelConfig" packages/core/src/search/search-service.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/core test -- src/search/search-service.test.ts exits 0
    - Test proves toggling mock settingsStore.rerankEnabled changes whether rerank client is invoked
  </acceptance_criteria>
  <action>
  Implement D-12/D-13: `SearchService.create` accepts optional `settingsStore`; `search()` resolves model config each call; fallback to config env when store absent. Keep constructor backward compatible for existing tests.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test -- src/search/search-service.test.ts 2>&1</automated>
  </verify>
  <done>Rerank settings hot-reload in-process</done>
</task>

<task type="auto">
  <name>Task 3: Wire Backend + MCP services + health</name>
  <read_first>
    - apps/backend/src/services.ts
    - apps/mcp-server/src/services.ts
    - apps/backend/src/routes/health.ts
  </read_first>
  <files>
    apps/backend/src/services.ts
    apps/mcp-server/src/services.ts
    apps/backend/src/routes/health.ts
  </files>
  <acceptance_criteria>
    - grep -n "settingsStore" apps/backend/src/services.ts apps/mcp-server/src/services.ts
    - grep -c "getModelConfig" apps/backend/src/routes/health.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/core test 2>&1 exits 0
    - pnpm --filter @kb/backend test -- src/routes/settings.test.ts 2>&1 exits 0
  </acceptance_criteria>
  <action>
  D-14/D-15: Pass settingsStore into SearchService.create; EmbeddingClient getter from settings; health embeddings uses getModelConfig().embeddingModel. Keep types compiling across apps.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test 2>&1 && pnpm --filter @kb/backend test -- src/routes/health.ts src/routes/settings.test.ts 2>&1</automated>
  </verify>
  <done>CONF-06 end-to-end in services</done>
</task>

</tasks>

<verification>
  <automated>pnpm --filter @kb/core test 2>&1 && pnpm --filter @kb/backend test 2>&1</automated>
</verification>

<success_criteria>
- Changing models via SettingsStore affects subsequent embed/search without process restart
- MCP and Backend both wired
</success_criteria>

<output>
After completion, create `.planning/phases/19-model-settings-core/19-03-SUMMARY.md`
</output>
