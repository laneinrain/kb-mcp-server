---
phase: 19-model-settings-core
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - apps/backend/src/routes/settings.ts
  - apps/backend/src/routes/settings.test.ts
autonomous: true
requirements:
  - CONF-05
must_haves:
  truths:
    - "GET /api/v1/settings includes models + embeddingDimensions (D-07)"
    - "PATCH /api/v1/settings/models updates SettingsStore and returns models (D-08–D-10)"
    - "Invalid rerankCandidates returns 400"
  artifacts:
    - path: "apps/backend/src/routes/settings.ts"
      provides: "PATCH /api/v1/settings/models"
      contains: "settings/models"
  key_links:
    - from: "apps/backend/src/routes/settings.ts"
      to: "packages/core/src/registry/settings-store.ts"
      via: "updateModelConfig"
      pattern: "updateModelConfig"
---

<objective>
Expose model settings on REST (CONF-05): extend GET; add PATCH `/api/v1/settings/models` with Zod validation + tests.

Output: backend settings tests green. Web UI deferred to Phase 20.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-model-settings-core/19-CONTEXT.md
@.planning/phases/19-model-settings-core/19-RESEARCH.md
@apps/backend/src/routes/settings.ts
@apps/backend/src/routes/settings.test.ts
@apps/backend/src/index.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Unauthenticated settings change | Medium | Reuse existing `routeOpts` auth on settings routes |
| Clients assume old GET shape only | Low | Additive `models` field |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Zod schemas + GET/PATCH routes</name>
  <read_first>
    - apps/backend/src/routes/settings.ts
    - apps/backend/src/index.ts
    - packages/core/src/registry/types.ts
  </read_first>
  <files>
    apps/backend/src/routes/settings.ts
  </files>
  <acceptance_criteria>
    - grep -c "settings/models" apps/backend/src/routes/settings.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "embeddingDimensions" apps/backend/src/routes/settings.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "getModelConfig" apps/backend/src/routes/settings.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
  1. Add `ModelSettingsSchema` (embeddingModel min 1, rerankEnabled bool, rerankModel min 1, rerankCandidates 1–50).
  2. Extend GET response schema with `models` + `embeddingDimensions` (number from deps — inject `embeddingDimensions: number` on SettingsDeps from `config.EMBEDDING_DIMENSIONS` in index.ts).
  3. PATCH `/api/v1/settings/models` → `updateModelConfig` → `{ models }`.
  4. Map store validation errors to 400 via existing error helper or small extension.
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend exec tsc -p tsconfig.json --noEmit 2>&1</automated>
  </verify>
  <done>Routes compile</done>
</task>

<task type="auto">
  <name>Task 2: settings route tests</name>
  <read_first>
    - apps/backend/src/routes/settings.test.ts
  </read_first>
  <files>
    apps/backend/src/routes/settings.test.ts
    apps/backend/src/index.ts
  </files>
  <acceptance_criteria>
    - pnpm --filter @kb/backend test -- src/routes/settings.test.ts exits 0
    - GET asserts models + embeddingDimensions present
    - PATCH success + invalid candidates covered
  </acceptance_criteria>
  <action>
  Extend mock `settingsStore` with `getModelConfig` / `updateModelConfig`. Wire `embeddingDimensions` in `registerSettingsRoutes` call sites (index + test helper).
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend test -- src/routes/settings.test.ts 2>&1</automated>
  </verify>
  <done>CONF-05 REST covered</done>
</task>

</tasks>

<verification>
  <automated>pnpm --filter @kb/backend test -- src/routes/settings.test.ts 2>&1</automated>
</verification>

<success_criteria>
- Operators can read/update model settings via REST without restart
</success_criteria>

<output>
After completion, create `.planning/phases/19-model-settings-core/19-02-SUMMARY.md`
</output>
