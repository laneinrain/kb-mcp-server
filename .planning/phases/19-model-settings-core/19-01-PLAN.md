---
phase: 19-model-settings-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/registry/types.ts
  - packages/core/src/registry/schema.sql
  - packages/core/src/registry/settings-store.ts
  - packages/core/src/registry/settings-store.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - CONF-04
must_haves:
  truths:
    - "ModelConfig type exists with embeddingModel, rerankEnabled, rerankModel, rerankCandidates (D-01)"
    - "Settings row migrates/seeds model columns from AppConfig (D-02–D-04)"
    - "getModelConfig / updateModelConfig round-trip; candidates 1–50 validated (D-05, D-06)"
  artifacts:
    - path: "packages/core/src/registry/types.ts"
      provides: "ModelConfig"
      contains: "embeddingModel"
    - path: "packages/core/src/registry/settings-store.ts"
      provides: "getModelConfig"
      contains: "updateModelConfig"
  key_links:
    - from: "packages/core/src/registry/settings-store.ts"
      to: "packages/config"
      via: "seed from AppConfig"
      pattern: "EMBEDDING_MODEL"
---

<objective>
Add **ModelConfig** persistence to `SettingsStore` (CONF-04): schema migration, env seed, get/update APIs + unit tests.

Output: `@kb/core` tests green; no REST or SearchService wiring yet.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-model-settings-core/19-CONTEXT.md
@.planning/phases/19-model-settings-core/19-RESEARCH.md
@packages/core/src/registry/settings-store.ts
@packages/core/src/registry/types.ts
@packages/core/src/registry/schema.sql
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Migration breaks old DBs | High | ALTER ADD COLUMN with defaults; tests on legacy schema |
| Empty model string persisted | Medium | trim + min length validation on update |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: ModelConfig type + schema columns</name>
  <read_first>
    - packages/core/src/registry/types.ts
    - packages/core/src/registry/schema.sql
    - packages/core/src/registry/settings-store.ts
  </read_first>
  <files>
    packages/core/src/registry/types.ts
    packages/core/src/registry/schema.sql
    packages/core/src/registry/settings-store.ts
  </files>
  <acceptance_criteria>
    - grep -c "ModelConfig" packages/core/src/registry/types.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "embedding_model" packages/core/src/registry/schema.sql | awk '{exit ($1>=1)?0:1}'
    - grep -c "getModelConfig" packages/core/src/registry/settings-store.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
  1. Add `ModelConfig` interface (D-01).
  2. Extend `schema.sql` CREATE TABLE settings with the four model columns (for fresh installs).
  3. Extend `migrateSettingsColumns` to accept `AppConfig` and ADD COLUMN for existing DBs with defaults from config (D-02, D-04).
  4. Update `seedSettingsIfMissing` INSERT to include model fields (D-03).
  5. Implement `getModelConfig` / `updateModelConfig` on SettingsStore (D-05, D-06). Map SQLite 0/1 ↔ boolean for `rerank_enabled`.
  6. Export `ModelConfig` from `packages/core/src/index.ts` if other packages need it.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core exec tsc -p tsconfig.json --noEmit 2>&1</automated>
  </verify>
  <done>Store API compiles with ModelConfig</done>
</task>

<task type="auto">
  <name>Task 2: settings-store unit tests</name>
  <read_first>
    - packages/core/src/registry/settings-store.test.ts
  </read_first>
  <files>
    packages/core/src/registry/settings-store.test.ts
  </files>
  <acceptance_criteria>
    - pnpm --filter @kb/core test -- src/registry/settings-store.test.ts exits 0
    - Test covers seed from env EMBEDDING_MODEL / RERANK_*
    - Test covers updateModelConfig + reject candidates outside 1–50
    - Test covers migrate from legacy settings table without model columns
  </acceptance_criteria>
  <action>
  Extend `settings-store.test.ts` mirroring context tests: seed, update, validation error, legacy ALTER path.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test -- src/registry/settings-store.test.ts 2>&1</automated>
  </verify>
  <done>CONF-04 covered by unit tests</done>
</task>

</tasks>

<verification>
  <automated>pnpm --filter @kb/core test -- src/registry/settings-store.test.ts 2>&1</automated>
</verification>

<success_criteria>
- Model settings persist in SQLite and survive reopen
- Legacy DB migrates without data loss
</success_criteria>

<output>
After completion, create `.planning/phases/19-model-settings-core/19-01-SUMMARY.md`
</output>
