---
phase: 02-rest-backend-search
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - apps/backend/src/index.ts
  - apps/backend/src/routes/documents.ts
  - apps/backend/src/routes/search.ts
  - apps/backend/src/lib/errors.ts
  - apps/backend/src/routes/documents.test.ts
  - apps/backend/src/routes/search.test.ts
  - apps/backend/vitest.config.ts
  - apps/backend/package.json
autonomous: false
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
must_haves:
  truths:
    - "Operator can POST multipart file to /api/v1/documents and receive 201 with documentId, chunkCount, collection, status indexed (D-11)"
    - "Operator can GET /api/v1/documents and see full list without sourcePath (D-17)"
    - "Operator can GET /api/v1/documents/:id for single document detail; 404 when missing"
    - "Operator can DELETE /api/v1/documents/:id; Chroma vectors removed then registry row; 404 when missing (D-18)"
    - "Operator can POST /api/v1/search with query and receive ranked results via SearchService only (D-13, API-04)"
    - "Parser and dependency failures return { error, message } JSON with appropriate 4xx/5xx (D-12)"
  artifacts:
    - path: "apps/backend/src/routes/documents.ts"
      provides: "Document upload, list, get, delete REST API"
      contains: "/api/v1/documents"
    - path: "apps/backend/src/routes/search.ts"
      provides: "Test semantic search REST API"
      contains: "/api/v1/search"
    - path: "apps/backend/src/lib/errors.ts"
      provides: "HTTP error mapping from core exceptions"
      contains: "mapIngestError"
  key_links:
    - from: "apps/backend/src/routes/documents.ts"
      to: "IngestionService.ingest"
      via: "multipart temp file path"
      pattern: "ingestionService\\.ingest"
    - from: "apps/backend/src/routes/search.ts"
      to: "SearchService.search"
      via: "POST body query delegation"
      pattern: "searchService\\.search"
    - from: "apps/backend/src/routes/documents.ts"
      to: "ChromaVectorStore.deleteByDocumentId"
      via: "delete before registry row"
      pattern: "deleteByDocumentId"
---

<objective>
Implement Phase 2 REST routes: document upload/list/get/delete and semantic search test endpoint, all under `/api/v1/`, delegating to `@kb/core` services with Zod validation and consistent error responses.

Purpose: Deliver API-01 through API-04 and Phase 2 success criteria — operators manage corpus and test search via REST using the same SearchService MCP will use.
Output: Working `/api/v1/documents`, `/api/v1/search`, route unit tests, and human E2E verification checkpoint.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-rest-backend-search/02-CONTEXT.md
@.planning/phases/02-rest-backend-search/02-RESEARCH.md
@.planning/phases/02-rest-backend-search/02-PATTERNS.md
@apps/backend/src/services.ts
@apps/backend/src/routes/health.ts
@packages/core/src/ingestion/parsers/index.ts

<interfaces>
From Plan 02-02 bootstrap:
```typescript
export interface AppServices {
  config: AppConfig;
  registry: DocumentRegistry;
  vectorStore: ChromaVectorStore;
  ingestionService: IngestionService;
  searchService: SearchService;
  uploadsDir: string;
}

export async function createAppServices(): Promise<AppServices>;
```

From `@kb/core`:
```typescript
export const INSUFFICIENT_TEXT_ERROR: string;
export interface IngestResult { documentId: string; chunkCount: number; collection: string; }
export interface DocumentRecord { id; filename; status; chunkCount; collection; createdAt; updatedAt; sourcePath; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Document routes and error mapper</name>
  <read_first>
    - .planning/phases/02-rest-backend-search/02-CONTEXT.md (D-08 through D-12, D-17 through D-19)
    - .planning/phases/02-rest-backend-search/02-RESEARCH.md (Pattern 4 multipart, Pattern 5 delete, Error mapping)
    - .planning/phases/02-rest-backend-search/02-PATTERNS.md (documents route section)
    - apps/backend/src/services.ts
  </read_first>
  <files>
    apps/backend/src/lib/errors.ts
    apps/backend/src/routes/documents.ts
    apps/backend/src/routes/documents.test.ts
    apps/backend/vitest.config.ts
    apps/backend/package.json
    apps/backend/src/index.ts
  </files>
  <behavior>
    - Test 1: POST /api/v1/documents without file returns 400 { error, message }
    - Test 2: GET /api/v1/documents returns list omitting sourcePath from each item
    - Test 3: GET /api/v1/documents/:id returns 404 for unknown id
    - Test 4: DELETE /api/v1/documents/:id calls deleteByDocumentId before registry.deleteDocument; 404 when missing
    - Test 5: mapIngestError maps INSUFFICIENT_TEXT_ERROR to 422
  </behavior>
  <acceptance_criteria>
    - grep -v '^#' apps/backend/src/routes/documents.ts | grep -c '/api/v1/documents' | awk '{exit ($1>=3)?0:1}'
    - grep -v '^#' apps/backend/src/routes/documents.ts | grep -c 'req\\.parts' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/routes/documents.ts | grep -c 'deleteByDocumentId' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/routes/documents.ts | grep -c 'sourcePath' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/package.json | grep -c 'vitest' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`apps/backend/src/lib/errors.ts`** per D-12 and RESEARCH error table:
    - `mapIngestError(error): { statusCode; body: { error; message } }`
    - Map: unsupported extension/MIME → 415; INSUFFICIENT_TEXT_ERROR → 422; file too large → 413; path guard → 400; CherryIn/Chroma ECONNREFUSED → 503; default → 500
    - Export `notFound(documentId)` helper → 404 `{ error: "not_found", message }`

    **`apps/backend/src/routes/documents.ts`** per D-08, D-09, D-10, D-11, D-17, D-18, D-19, D-20, D-22:

    `DocumentsDeps`: `{ ingestionService, registry, vectorStore, uploadsDir, defaultCollection }`

    **`POST /api/v1/documents`** (API-01):
    - Parse `req.parts()` iterator (D-08) — read `collection` field when `part.type === 'field'`
    - Single file: allow MIME `text/plain`, `text/markdown`, `application/pdf` and extensions `.txt`, `.md`, `.markdown`, `.pdf` (D-09)
    - Save to `join(uploadsDir, \`${randomUUID()}-${basename(part.filename)}\`)` via `pipeline(part.file, createWriteStream(tempPath))`
    - Call `ingestionService.ingest(tempPath, { collection: collection ?? defaultCollection })` (D-10)
    - On success: `unlink(tempPath)` (cleanup policy per research discretion); return `201` `{ documentId, chunkCount, collection, status: "indexed" }` (D-11)
    - On failure: map via `mapIngestError`; retain temp file for debug

    **`GET /api/v1/documents`** (API-02, D-17):
    - `registry.listDocuments()` mapped to public DTO — omit `sourcePath`

    **`GET /api/v1/documents/:documentId`** (research recommends add):
    - `registry.getDocument(id)` → 404 or public DTO without sourcePath

    **`DELETE /api/v1/documents/:documentId`** (API-03, D-18):
    - Lookup doc → 404 if missing
    - `await vectorStore.deleteByDocumentId(documentId, doc.collection)` **first**
    - `registry.deleteDocument(documentId)` second
    - Return `{ status: "deleted", documentId }`

    Add Zod response schemas where practical with `fastify-type-provider-zod`.

    **`vitest.config.ts`** + `"test": "vitest run"` in backend package.json.

    **`documents.test.ts`**: Fastify `app.inject()` with mocked services (mirror core vitest mock style).

    Register in `index.ts`: `await registerDocumentRoutes(app, { ...services, defaultCollection: services.config.DEFAULT_COLLECTION })`.
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend test -- documents 2>&1</automated>
  </verify>
  <done>Document CRUD routes pass unit tests; POST upload delegates to IngestionService; DELETE ordering correct</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Search route</name>
  <read_first>
    - .planning/phases/02-rest-backend-search/02-CONTEXT.md (D-13, D-14, D-16)
    - .planning/phases/02-rest-backend-search/02-RESEARCH.md (Zod search route example)
    - packages/core/src/search/search-service.ts
  </read_first>
  <files>
    apps/backend/src/routes/search.ts
    apps/backend/src/routes/search.test.ts
    apps/backend/src/index.ts
  </files>
  <behavior>
    - Test 1: POST /api/v1/search with valid body delegates to searchService.search with query, topK, collection
    - Test 2: Empty query rejected by Zod validation (400)
    - Test 3: topK above 10 rejected by Zod validation (400 Bad Request)
    - Test 4: Response shape { results: SearchResult[] } matches D-16 fields
  </behavior>
  <acceptance_criteria>
    - grep -v '^#' apps/backend/src/routes/search.ts | grep -c '/api/v1/search' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/routes/search.ts | grep -c 'searchService\\.search' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/routes/search.ts | grep -c 'z\\.object' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/routes/search.ts | grep -c 'ChromaVectorStore\\|embedQuery' | awk '{exit ($1==0)?0:1}'
  </acceptance_criteria>
  <action>
    **`apps/backend/src/routes/search.ts`** per D-13, D-14, D-16, D-20, D-22, API-04:

    Import `z` from `zod/v4`.

    Zod schemas (co-located in route file per D-22):
    ```typescript
    const SearchBodySchema = z.object({
      query: z.string().min(1).max(2000),
      topK: z.number().int().min(1).max(10).optional(),
      collection: z.string().optional(),
    });
    const SearchResponseSchema = z.object({
      results: z.array(z.object({
        score: z.number(),
        text: z.string(),
        documentId: z.string(),
        filename: z.string(),
        chunkIndex: z.number(),
      })),
    });
    ```

    **`POST /api/v1/search`**:
    - Use `app.withTypeProvider<ZodTypeProvider>().post(...)` with schema body/response
    - Handler: `const results = await searchService.search(req.body.query, { topK: req.body.topK, collection: req.body.collection })`
    - Return `{ results }`
    - **Must not** call EmbeddingClient or ChromaVectorStore directly — SearchService only (Phase 2 success criterion #4)

    Catch search failures → 503 if embedding/Chroma unavailable; 500 otherwise with `{ error, message }`.

    Register in `index.ts`. Add `search.test.ts` with inject + mocked SearchService.

    Wire global or route-level error handler if not done in 02-02 for consistent `{ error, message }` on validation failures.
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend test 2>&1</automated>
  </verify>
  <done>Search route validates input with Zod and delegates exclusively to SearchService.search()</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: REST API end-to-end verification</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 Success Criteria)
    - scripts/fixtures/sample.txt
    - apps/backend/src/routes/documents.ts
    - apps/backend/src/routes/search.ts
  </read_first>
  <files>N/A — verification only</files>
  <acceptance_criteria>
    - curl POST multipart upload of sample.txt returns 201 with documentId and chunkCount greater than 0
    - curl GET /api/v1/documents lists uploaded document without sourcePath field
    - curl POST /api/v1/search with query related to sample content returns results with score, text, documentId, filename, chunkIndex
    - curl DELETE /api/v1/documents/:id returns deleted status; subsequent GET returns 404
    - GET /docs loads OpenAPI UI with document and search endpoints documented
    - GET /health* endpoints still respond ok
  </acceptance_criteria>
  <action>
    Pause for human verification of full Phase 2 REST flow after Tasks 1–2 complete. No code changes unless issues found — operator runs live upload/search/delete against CherryIn and Chroma.
  </action>
  <what-built>
    Complete Phase 2 REST API: multipart upload ingest, document list/get/delete, semantic search test endpoint, OpenAPI at /docs, health routes unchanged.
  </what-built>
  <how-to-verify>
    1. Ensure `.env` has valid `CHERRYIN_API_KEY`; run `pnpm dev` from repo root
    2. `curl http://127.0.0.1:3000/health` → `{ "status": "ok" }`
    3. Upload: `curl -F "file=@scripts/fixtures/sample.txt" http://127.0.0.1:3000/api/v1/documents` → `201` with `documentId`, `chunkCount`, `status: "indexed"`
    4. List: `curl http://127.0.0.1:3000/api/v1/documents` → array includes uploaded doc; no `sourcePath` field
    5. Detail: `curl http://127.0.0.1:3000/api/v1/documents/{documentId}` → single document
    6. Search: `curl -X POST http://127.0.0.1:3000/api/v1/search -H "Content-Type: application/json" -d "{\"query\":\"sample content\"}"` → `results` array with score, text snippet, documentId, filename, chunkIndex
    7. Delete: `curl -X DELETE http://127.0.0.1:3000/api/v1/documents/{documentId}` → `{ "status": "deleted" }`
    8. Confirm GET detail returns 404 after delete
    9. Open `http://127.0.0.1:3000/docs` — verify document and search endpoints appear
    10. Optional: upload scanned PDF → expect 422 with clear error (INGE-04 via REST)
  </how-to-verify>
  <verify>
    <automated>echo "Human checkpoint — run steps in how-to-verify"</automated>
  </verify>
  <done>Operator confirms all Phase 2 success criteria pass live with CherryIn API key and Chroma sidecar running</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /api/v1/documents | Untrusted multipart uploads |
| HTTP client → /api/v1/search | Untrusted JSON query strings |
| Routes → @kb/core services | Validated inputs only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | T | POST /documents unrestricted upload | mitigate | MIME allowlist (D-09); 50MB limit; single file; UUID temp paths |
| T-02-09 | T | Filename path traversal | mitigate | basename + UUID prefix under uploadsDir only |
| T-02-10 | D | Oversized search query | mitigate | Zod query.max(2000) on POST /search |
| T-02-11 | I | Error responses leak stack traces | mitigate | Return { error, message }; log full error server-side via Fastify logger |
| T-02-12 | E | Delete registry before Chroma | mitigate | deleteByDocumentId before registry.deleteDocument (D-18, RESEARCH Pattern 5) |
</threat_model>

<verification>
- All @kb/backend tests pass: `pnpm --filter @kb/backend test`
- Backend build/typecheck passes
- Human checkpoint confirms upload → list → search → delete cycle
- Search route grep confirms no direct Chroma/embed imports
</verification>

<success_criteria>
- API-01: multipart upload ingest via REST with sync 201 response
- API-02: list indexed documents via GET /api/v1/documents
- API-03: delete document and vectors via DELETE /api/v1/documents/:id
- API-04: test semantic search via POST /api/v1/search using SearchService only
- Phase 2 success criteria #1–#4 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-rest-backend-search/02-03-SUMMARY.md`
</output>
