---
phase: 18-config-docs-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - README.md
  - .env.example
autonomous: true
requirements:
  - PLAT-15
must_haves:
  truths:
    - "README documents HTTP Authorization headers for MCP (D-08)"
    - "README documents stdio MCP_USER_TOKEN (D-08)"
    - "README auth matrix MCP row reflects user isolation (D-09)"
    - "README lists search_knowledge, read_around, read_file (D-11)"
  artifacts:
    - path: "README.md"
      provides: "MCP auth operator docs"
      contains: "MCP_USER_TOKEN"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "MCP_AUTH_REQUIRED"
      pattern: "MCP_AUTH_REQUIRED"
---

<objective>
Update **README** and finalize **`.env.example`** for MCP auth / Cursor config (PLAT-15).

Depends on 18-01 env flag existing.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-config-docs-verification/18-CONTEXT.md
@.planning/phases/18-config-docs-verification/18-RESEARCH.md
@README.md
@.env.example
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Docs show real secrets | High | Placeholders only |
| Operators miss fail-closed default | Medium | Call out MCP_AUTH_REQUIRED default true |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: README MCP auth + tool surface</name>
  <read_first>
    - README.md (MCP 配置, 鉴权矩阵, env table)
    - .planning/phases/18-config-docs-verification/18-RESEARCH.md
  </read_first>
  <files>
    README.md
    .env.example
  </files>
  <acceptance_criteria>
    - grep -c "MCP_USER_TOKEN" README.md | awk '{exit ($1>=1)?0:1}'
    - grep -c "Authorization" README.md | awk '{exit ($1>=1)?0:1}'
    - grep -c "MCP_AUTH_REQUIRED" README.md | awk '{exit ($1>=1)?0:1}'
    - grep -c "read_around" README.md | awk '{exit ($1>=1)?0:1}'
    - grep -c "read_file" README.md | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
  Update **README.md**:

  1. MCP section: document three tools (`search_knowledge`, `read_around`, `read_file`).
  2. stdio example: add `MCP_USER_TOKEN` in `env` when user auth on.
  3. HTTP example: add `headers.Authorization` with Bearer placeholder token.
  4. New short subsection **MCP 用户隔离（v1.5）**: JWT vs API_KEY vs `MCP_AUTH_REQUIRED=false`.
  5. Env table: `MCP_AUTH_REQUIRED`, note `MCP_USER_TOKEN` (stdio process env, not always in AppConfig).
  6. Auth matrix: replace MCP “无 / 不受隔离” with scoped behavior.

  Ensure **`.env.example`** comments match README (from 18-01; tweak if needed).
  </action>
  <verify>
    <automated>grep -E "MCP_USER_TOKEN|MCP_AUTH_REQUIRED|read_around" README.md | head -20</automated>
  </verify>
  <done>Operator docs cover MCP auth paths</done>
</task>

</tasks>

<verification>
1. README contains HTTP headers + stdio token examples
2. Auth matrix no longer claims MCP is unscoped
</verification>

## PLAN COMPLETE
