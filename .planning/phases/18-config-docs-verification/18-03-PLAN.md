---
phase: 18-config-docs-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - apps/mcp-server/src/mcp-user-isolation.test.ts
  - apps/mcp-server/src/http.test.ts
autonomous: true
requirements:
  - PLAT-16
must_haves:
  truths:
    - "Two JWT users with disjoint docs — user A cannot read user B via MCP tools (D-13–D-15)"
    - "User A search receives only A's allowedDocumentIds (D-14)"
    - "MCP_AUTH_REQUIRED=false allows unauthenticated global context (D-17)"
  artifacts:
    - path: "apps/mcp-server/src/mcp-user-isolation.test.ts"
      provides: "two-user MCP isolation suite"
      contains: "document_not_found"
  key_links:
    - from: "apps/mcp-server/src/mcp-user-isolation.test.ts"
      to: "apps/mcp-server/src/auth/mcp-auth-resolver.ts"
      via: "resolve(token)"
      pattern: "McpAuthResolver"
    - from: "apps/mcp-server/src/mcp-user-isolation.test.ts"
      to: "apps/mcp-server/src/server.ts"
      via: "buildMcpServer + InMemory"
      pattern: "callTool"
---

<objective>
Add automated **two-user MCP isolation** tests proving cross-user search/read denial (PLAT-16).

Depends on 18-01 gate (for MCP_AUTH_REQUIRED=false case).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-config-docs-verification/18-CONTEXT.md
@.planning/phases/18-config-docs-verification/18-RESEARCH.md
@apps/mcp-server/src/server.test.ts
@apps/mcp-server/src/auth/mcp-auth-resolver.test.ts
@packages/core/src/context/context-service.test.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Test mocks away ACL and false-pass | High | Use real ContextService for read denial |
| Live Chroma dependency | Medium | Mock vectorStore.getByIds |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Two-user MCP isolation integration tests</name>
  <read_first>
    - apps/mcp-server/src/server.test.ts
    - apps/mcp-server/src/auth/mcp-auth-resolver.ts
    - packages/core/src/context/context-service.ts
  </read_first>
  <files>
    apps/mcp-server/src/mcp-user-isolation.test.ts
  </files>
  <acceptance_criteria>
    - test -f apps/mcp-server/src/mcp-user-isolation.test.ts
    - grep -c "McpAuthResolver" apps/mcp-server/src/mcp-user-isolation.test.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "read_file\|READ_FILE" apps/mcp-server/src/mcp-user-isolation.test.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/mcp-server test -- mcp-user-isolation exits 0
  </acceptance_criteria>
  <action>
  Create **`mcp-user-isolation.test.ts`**:

  Setup:
  - MockCasAuthProvider → login user A (`11111111`) and user B (`22222222`)
  - Registry mock: `doc-a` owned by A, `doc-b` owned by B; `listDocumentsForUser` filters; `getDocument` / `getChunkIds` for both
  - Real `ContextService` with mocked settingsStore + vectorStore (`getByIds` returns chunks for requested ids)
  - Spy/mock `SearchService.search` to capture options
  - `McpAuthResolver` with `USER_AUTH_ENABLED=true`, `MCP_AUTH_REQUIRED=true`, `systemUserId`

  Cases:
  1. Resolve tokenA → `runWithMcpCallerContext` → `search_knowledge` → `allowedDocumentIds` has `doc-a`, not `doc-b`
  2. Same context → `read_file` on `doc-b` → `isError` / not found
  3. Same context → `read_file` or `read_around` on `doc-a` → success
  4. Resolve tokenB → search ACL is B's set
  5. Config `MCP_AUTH_REQUIRED=false` → `resolve(undefined)` → `authMode: "global"`
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server test -- mcp-user-isolation 2>&1</automated>
  </verify>
  <done>Two-user isolation proven through MCP tools</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @kb/mcp-server test`
2. Isolation file covers search ACL + cross-user read deny
</verification>

## PLAN COMPLETE
