---
phase: 10-mock-local-auth-admin-bootstrap
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - apps/backend/src/services.ts
  - apps/backend/src/routes/auth.ts
  - apps/backend/src/routes/auth.test.ts
  - packages/auth/README.md
  - .env.example
  - README.md
autonomous: true
requirements:
  - ADMIN-01
  - AUTH-07
  - AUTH-10
must_haves:
  truths:
    - "createAppServices bootstraps admin 00000 when USER_AUTH_ENABLED && CAS_MOCK (D-16)"
    - "POST /api/v1/auth/register creates local user when CAS_MOCK=true (AUTH-07)"
    - "Register returns 404 when CAS_MOCK=false or USER_AUTH_ENABLED=false (AUTH-10, D-13)"
    - "Register duplicate returns 409; validation 400 (D-14, D-15)"
    - "Login response includes user.role for Web (Phase 12 prep)"
    - "README documents 00000/admin123 scaffold warning (D-19)"
  artifacts:
    - path: "apps/backend/src/routes/auth.ts"
      provides: "register route"
      contains: "/api/v1/auth/register"
    - path: "apps/backend/src/services.ts"
      provides: "admin bootstrap"
      contains: "ensureAdminUser"
    - path: "apps/backend/src/routes/auth.test.ts"
      provides: "register integration tests"
      contains: "register"
  key_links:
    - from: "apps/backend/src/services.ts"
      to: "packages/auth/src/user-store.ts"
      via: "ensureAdminUser"
      pattern: "CAS_MOCK"
---

<objective>
Wire **admin bootstrap** on startup and **`POST /api/v1/auth/register`** route with mock-mode guards (ADMIN-01, AUTH-07, AUTH-10).

Depends on 10-02 provider register + local login.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-mock-local-auth-admin-bootstrap/10-CONTEXT.md
@.planning/phases/10-mock-local-auth-admin-bootstrap/10-RESEARCH.md
@apps/backend/src/routes/auth.ts
@apps/backend/src/services.ts
@packages/config/src/env.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Open register on production | High | 404 when CAS_MOCK=false (AUTH-10) |
| Admin bootstrap on production | High | Guarded by CAS_MOCK in services.ts |
| Register spam | Medium | Mock mode only; document network exposure warning |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Admin bootstrap in createAppServices</name>
  <read_first>
    - apps/backend/src/services.ts
    - packages/auth/src/user-store.ts
    - packages/auth/src/password.ts
    - packages/auth/src/constants.ts
  </read_first>
  <files>
    apps/backend/src/services.ts
  </files>
  <acceptance_criteria>
    - grep -c "ensureAdminUser" apps/backend/src/services.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "CAS_MOCK" apps/backend/src/services.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    In **`createAppServices()`** when `USER_AUTH_ENABLED`:
    1. After `ensureSystemUser()`, if `config.CAS_MOCK`:
       - Open auth DB
       - `hashPassword(ADMIN_DEFAULT_PASSWORD)`
       - `new UserStore(db).ensureAdminUser(hash)`
       - Close DB

    Pass `casMock` awareness to auth routes via deps if needed.
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend build 2>&1</automated>
  </verify>
  <done>Admin 00000 bootstrapped on mock-mode startup</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/v1/auth/register route</name>
  <read_first>
    - apps/backend/src/routes/auth.ts
    - packages/auth/src/mock-cas-auth-provider.ts
  </read_first>
  <files>
    apps/backend/src/routes/auth.ts
    apps/backend/src/routes/auth.test.ts
  </files>
  <acceptance_criteria>
    - grep -c "/api/v1/auth/register" apps/backend/src/routes/auth.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "register" apps/backend/src/routes/auth.test.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Extend **`AuthRoutesDeps`**: `casMock: boolean`, `authProvider`.

    Add **`POST /api/v1/auth/register`**:
    - Body Zod: `{ employeeId: string, password: string.min(8) }`
    - If `!authProvider` or `!casMock` → **404** `"User auth is disabled"` / mock register unavailable
    - Call `authProvider.register()` (type-narrow to MockCasAuthProvider or check method exists)
    - Success **201**: `{ user: { id, employeeId, email, role } }`
    - `AuthValidationError` → 400
    - Duplicate → 409 `{ error: "conflict", message: "工号已存在" }`

    Update **`POST /api/v1/auth/login`** response user object to include **`role`**.

    **`index.ts`**: pass `config.CAS_MOCK` to `registerAuthRoutes`.
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend test -- auth 2>&1</automated>
  </verify>
  <done>Register route with mock guards and tests</done>
</task>

<task type="auto">
  <name>Task 3: Documentation and env example</name>
  <read_first>
    - packages/auth/README.md
    - .env.example
    - README.md
  </read_first>
  <files>
    packages/auth/README.md
    .env.example
    README.md
  </files>
  <acceptance_criteria>
    - grep -c "00000" packages/auth/README.md | awk '{exit ($1>=1)?0:1}'
    - grep -c "admin123" .env.example | awk '{exit ($1>=1)?0:1}'
    - grep -c "register" README.md | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`packages/auth/README.md`**: Document mock-mode admin `00000`/`admin123`, register API, role in JWT; warn scaffold-only.

    **`.env.example`**: Comment block for mock admin + register when `CAS_MOCK=true`.

    **`README.md`**: Update 鉴权矩阵 — mock mode adds register + admin account; note Phase 11 admin API coming.

    Run full test suite for auth + backend.
  </action>
  <verify>
    <automated>pnpm --filter @kb/auth test && pnpm --filter @kb/backend test 2>&1</automated>
  </verify>
  <done>Docs updated; tests green</done>
</task>

</tasks>

<verification>
1. Manual: `USER_AUTH_ENABLED=true CAS_MOCK=true pnpm dev` → login `00000`/`admin123` works
2. `curl -X POST /api/v1/auth/register` creates user; duplicate returns 409
3. `CAS_MOCK=false` → register 404
</verification>

## PLAN COMPLETE
