---
phase: 10-mock-local-auth-admin-bootstrap
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - packages/auth/src/jwt.ts
  - packages/auth/src/types.ts
  - packages/auth/src/mock-cas-auth-provider.ts
  - packages/auth/src/mock-cas-auth-provider.test.ts
  - packages/auth/src/errors.ts
  - packages/auth/src/index.ts
autonomous: true
requirements:
  - AUTH-08
  - AUTH-09
must_haves:
  truths:
    - "JWT payload includes role; verifyAccessToken returns role on AuthUser (D-05)"
    - "local users login via bcrypt verify; wrong password fails (D-11)"
    - "cas JIT users keep any-non-empty-password behavior (D-11)"
    - "system auth_source users cannot login (D-11)"
    - "admin 00000 logs in with admin123 via bcrypt (D-12)"
    - "MockCasAuthProvider.register() creates local user (D-07, D-08)"
  artifacts:
    - path: "packages/auth/src/jwt.ts"
      provides: "role in JWT"
      contains: "role"
    - path: "packages/auth/src/mock-cas-auth-provider.ts"
      provides: "local bcrypt login"
      contains: "verifyPassword"
  key_links:
    - from: "packages/auth/src/mock-cas-auth-provider.ts"
      to: "packages/auth/src/user-store.ts"
      via: "registerLocalUser"
      pattern: "auth_source"
---

<objective>
Extend **`MockCasAuthProvider`** with bcrypt local login, register method, and JWT **`role`** claim (AUTH-08, AUTH-09).

Depends on 10-01 UserStore + password helpers.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-mock-local-auth-admin-bootstrap/10-CONTEXT.md
@.planning/phases/10-mock-local-auth-admin-bootstrap/10-RESEARCH.md
@packages/auth/src/mock-cas-auth-provider.ts
@packages/auth/src/jwt.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Timing attack on bcrypt | Low | bcryptjs constant-time compare |
| JWT role tampering | High | HS256 signed claim; verify on each request |
| cas path bypasses password | Medium | By design for JIT mock; local path strict |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: JWT role claim</name>
  <read_first>
    - packages/auth/src/jwt.ts
    - .planning/phases/10-mock-local-auth-admin-bootstrap/10-CONTEXT.md (D-05)
  </read_first>
  <files>
    packages/auth/src/jwt.ts
    packages/auth/src/mock-cas-auth-provider.test.ts
  </files>
  <acceptance_criteria>
    - grep -c "role" packages/auth/src/jwt.ts | awk '{exit ($1>=2)?0:1}'
    - pnpm --filter @kb/auth test -- jwt exits 0
  </acceptance_criteria>
  <action>
    Extend **`AccessTokenPayload`** with `role: "admin" | "user"`.

    **`signAccessToken`**: accept `role` param; include in JWT body.

    **`verifyAccessToken`**: return role; default `"user"` if claim missing (dev backward compat).

    Add jwt unit test: sign/verify roundtrip with role=admin.
  </action>
  <verify>
    <automated>pnpm --filter @kb/auth test 2>&1</automated>
  </verify>
  <done>JWT carries role claim</done>
</task>

<task type="auto">
  <name>Task 2: MockCasAuthProvider local login + register</name>
  <read_first>
    - packages/auth/src/mock-cas-auth-provider.ts
    - packages/auth/src/user-store.ts
    - .planning/phases/10-mock-local-auth-admin-bootstrap/10-CONTEXT.md (D-11, D-12)
  </read_first>
  <files>
    packages/auth/src/mock-cas-auth-provider.ts
    packages/auth/src/errors.ts
    packages/auth/src/types.ts
    packages/auth/src/index.ts
  </files>
  <acceptance_criteria>
    - grep -c "verifyPassword" packages/auth/src/mock-cas-auth-provider.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "register" packages/auth/src/mock-cas-auth-provider.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "auth_source" packages/auth/src/mock-cas-auth-provider.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Refactor **`login()`**:
    1. Validate employeeId format + non-empty password
    2. Block `auth_source=system` users → `AuthValidationError`
    3. If user exists with `auth_source=local` → bcrypt verify; fail with `"工号或密码错误"` on mismatch
    4. If user exists with `auth_source=cas` OR new user → existing JIT upsertCasUser path (any password)
    5. Issue JWT with `role` from user row

    Add **`register({ employeeId, password })`**:
    - Validate format; password min 8 chars
    - Reject reserved IDs
    - `hashPassword` → `registerLocalUser`
    - Return `AuthUser` (no token)

    Add **`AuthConflictError`** for duplicate register (extends or separate from AuthValidationError).

    Optional: add `register` to `AuthProvider` interface as optional method.
  </action>
  <verify>
    <automated>pnpm --filter @kb/auth build 2>&1</automated>
  </verify>
  <done>Provider supports local login and register</done>
</task>

<task type="auto">
  <name>Task 3: Provider tests — admin, local, cas paths</name>
  <read_first>
    - packages/auth/src/mock-cas-auth-provider.test.ts
    - packages/auth/src/constants.ts
  </read_first>
  <files>
    packages/auth/src/mock-cas-auth-provider.test.ts
    packages/auth/src/user-store.ts
  </files>
  <acceptance_criteria>
    - pnpm --filter @kb/auth test -- mock-cas-auth-provider exits 0
  </acceptance_criteria>
  <action>
    **Tests**:
    - Bootstrap admin via `ensureAdminUser` + hash `admin123`; login `00000`/`admin123` → JWT with role=admin
    - Wrong admin password fails
    - Register new user `12345` + password; login with bcrypt succeeds
    - Wrong password on local user fails
    - JIT cas user `12345678` + any password still works
    - `00000000` login blocked
    - Register reserved `00000` fails
    - `validateAccessToken` returns role on AuthUser
  </action>
  <verify>
    <automated>pnpm --filter @kb/auth test 2>&1</automated>
  </verify>
  <done>All auth paths covered by unit tests</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @kb/auth test` passes
- [ ] Existing Phase 7 login tests still pass for JIT cas users
</verification>

## PLAN COMPLETE
