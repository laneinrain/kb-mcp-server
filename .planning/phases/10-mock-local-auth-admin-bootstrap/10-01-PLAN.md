---
phase: 10-mock-local-auth-admin-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/auth/package.json
  - packages/auth/src/constants.ts
  - packages/auth/src/schema.sql
  - packages/auth/src/types.ts
  - packages/auth/src/user-store.ts
  - packages/auth/src/password.ts
  - packages/auth/src/migrations.ts
  - packages/auth/src/index.ts
  - packages/auth/src/user-store.test.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - AUTH-09
must_haves:
  truths:
    - "users.role column exists with default 'user' (D-04)"
    - "ADMIN_EMPLOYEE_ID='00000' constant; SYSTEM_EMPLOYEE_ID='00000000' unchanged (D-01, D-02)"
    - "ensureAdminUser() creates local admin with role=admin if missing (D-01, D-16)"
    - "registerLocalUser() stores bcrypt password_hash and auth_source=local (D-07)"
    - "RESERVED_EMPLOYEE_IDS blocks 00000 and 00000000 on register (D-03)"
    - "AuthUser includes role field (D-05)"
  artifacts:
    - path: "packages/auth/src/constants.ts"
      provides: "ADMIN_EMPLOYEE_ID"
      contains: "00000"
    - path: "packages/auth/src/password.ts"
      provides: "bcrypt helpers"
      contains: "hashPassword"
    - path: "packages/auth/src/user-store.ts"
      provides: "ensureAdminUser"
      contains: "ensureAdminUser"
  key_links:
    - from: "packages/auth/src/migrations.ts"
      to: "users.role"
      via: "ALTER TABLE"
      pattern: "role"
---

<objective>
Add **`role`** column, reserved ID constants, bcrypt password helpers, and **`UserStore`** methods for admin bootstrap + local registration (ADMIN-01, ADMIN-02, AUTH-09 schema/types foundation).

Output: `@kb/auth` builds with user-store tests; no provider/route changes yet.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-mock-local-auth-admin-bootstrap/10-CONTEXT.md
@.planning/phases/10-mock-local-auth-admin-bootstrap/10-RESEARCH.md
@packages/auth/src/user-store.ts
@packages/auth/src/constants.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Plaintext passwords in DB | High | bcrypt hash only for local users |
| Admin password in constants | Medium | Scaffold-only comment; README warning (D-19) |
| Role column missing on existing DB | Medium | Idempotent migration in openAuthDatabase |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Constants, types, schema role column</name>
  <read_first>
    - .planning/phases/10-mock-local-auth-admin-bootstrap/10-CONTEXT.md (D-01–D-06)
    - packages/auth/src/types.ts
    - packages/auth/src/constants.ts
  </read_first>
  <files>
    packages/auth/src/constants.ts
    packages/auth/src/types.ts
    packages/auth/src/schema.sql
  </files>
  <acceptance_criteria>
    - grep -c "ADMIN_EMPLOYEE_ID" packages/auth/src/constants.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "00000" packages/auth/src/constants.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "role" packages/auth/src/types.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "role" packages/auth/src/schema.sql | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`constants.ts`**: Add `ADMIN_EMPLOYEE_ID = "00000"`, `ADMIN_DEFAULT_PASSWORD = "admin123"` (scaffold-only comment), `RESERVED_EMPLOYEE_IDS = new Set([ADMIN_EMPLOYEE_ID, SYSTEM_EMPLOYEE_ID])`.

    **`types.ts`**: Add `role: "admin" | "user"` to `AuthUser`; add `RegisterInput` alias or reuse `LoginInput` for register.

    **`schema.sql`**: Add `role TEXT NOT NULL DEFAULT 'user'` to users CREATE TABLE for fresh DBs.
  </action>
  <verify>
    <automated>pnpm --filter @kb/auth build 2>&1</automated>
  </verify>
  <done>Types and schema include role</done>
</task>

<task type="auto">
  <name>Task 2: bcrypt helpers and auth DB migration</name>
  <read_first>
    - .planning/phases/10-mock-local-auth-admin-bootstrap/10-RESEARCH.md
    - packages/auth/src/user-store.ts
  </read_first>
  <files>
    packages/auth/package.json
    packages/auth/src/password.ts
    packages/auth/src/migrations.ts
    packages/auth/src/user-store.ts
    packages/auth/src/index.ts
  </files>
  <acceptance_criteria>
    - grep -c "bcryptjs" packages/auth/package.json | awk '{exit ($1>=1)?0:1}'
    - grep -c "hashPassword" packages/auth/src/password.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "runAuthMigrations" packages/auth/src/migrations.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Add dependency **`bcryptjs`** + **`@types/bcryptjs`**.

    **`password.ts`**: `hashPassword(plain: string): Promise<string>`, `verifyPassword(plain, hash): Promise<boolean>` (cost factor 10).

    **`migrations.ts`**: `runAuthMigrations(db)` — if `role` column missing, `ALTER TABLE users ADD COLUMN role TEXT NOT NULL DEFAULT 'user'`.

    Update **`openAuthDatabase`** in user-store.ts to call `runAuthMigrations` after schema exec.

    Export password helpers from index.ts.
  </action>
  <verify>
    <automated>pnpm --filter @kb/auth build 2>&1</automated>
  </verify>
  <done>bcrypt helpers and migration ready</done>
</task>

<task type="auto">
  <name>Task 3: UserStore ensureAdminUser + registerLocalUser</name>
  <read_first>
    - packages/auth/src/user-store.ts
    - packages/auth/src/constants.ts
  </read_first>
  <files>
    packages/auth/src/user-store.ts
    packages/auth/src/user-store.test.ts
    packages/auth/src/index.ts
  </files>
  <acceptance_criteria>
    - grep -c "ensureAdminUser" packages/auth/src/user-store.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "registerLocalUser" packages/auth/src/user-store.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/auth test -- user-store exits 0
  </acceptance_criteria>
  <action>
  Extend **`UserStore`**:
  - `mapRow` includes `role`
  - `ensureAdminUser(passwordHash: string)` — insert admin `00000` if missing (`auth_source=local`, `role=admin`); idempotent if exists (do not overwrite password)
  - `registerLocalUser({ employeeId, passwordHash })` — reject reserved IDs; reject duplicate; insert `auth_source=local`, `role=user`
  - `isReservedEmployeeId(id: string): boolean` helper

  **Tests**: ensureAdminUser creates once; registerLocalUser succeeds; duplicate throws; reserved IDs throw; bcrypt round-trip via password helpers.
  </action>
  <verify>
    <automated>pnpm --filter @kb/auth test 2>&1</automated>
  </verify>
  <done>UserStore admin + register methods tested</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @kb/auth build && pnpm --filter @kb/auth test`
2. Migration idempotent on existing dev auth.db
</verification>

## PLAN COMPLETE
