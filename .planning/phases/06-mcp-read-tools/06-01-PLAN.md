---
phase: 06-mcp-read-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mcp-server/src/services.ts
  - apps/mcp-server/src/server.ts
  - apps/mcp-server/src/server.test.ts
  - apps/mcp-server/src/stdio.ts
  - apps/mcp-server/src/http.ts
autonomous: true
requirements:
  - MCP-07
  - MCP-08
  - MCP-09
  - MCP-11
must_haves:
  truths:
    - "D-01: Tool descriptions match search_knowledge verbosity — one concise English sentence; params in Zod schema"
    - "D-02: read_around description includes inline one-line example from search_knowledge hit documentId/chunkIndex"
    - "D-03: read_around mentions search_knowledge only via inline example — no multi-step tutorial prose"
    - "D-04: read_file description stays D-01 length; optional one-line contrast with read_around at planner discretion"
    - "D-05: Tool names are exactly read_around and read_file (MCP-07, MCP-08)"
    - "D-06: MCP inputs use snake_case document_id, chunk_index, window, collection"
    - "D-07: Responses use content JSON + structuredContent with full ContextService metadata (MCP-09)"
    - "D-08: ContextError returns isError true with message — no partial chunks on failure"
    - "D-09: createMcpServices wires registry + settingsStore + ContextService via same SQLite as backend"
    - "D-10: buildMcpServer registers search_knowledge, read_around, read_file only — no write tools (MCP-11)"
    - "stdio and http call buildMcpServer(searchService, contextService) — shared factory (MCP-10 prep)"
  artifacts:
    - path: "apps/mcp-server/src/server.ts"
      provides: "read_around and read_file MCP tool registration"
      contains: "read_around"
    - path: "apps/mcp-server/src/services.ts"
      provides: "McpServices with contextService"
      contains: "contextService"
    - path: "apps/mcp-server/src/server.test.ts"
      provides: "tool list and delegation tests"
      contains: "read_around"
  key_links:
    - from: "apps/mcp-server/src/server.ts"
      to: "packages/core/src/context/context-service.ts"
      via: "contextService.readAround / readFile"
      pattern: "readAround"
    - from: "apps/mcp-server/src/services.ts"
      to: "apps/backend/src/services.ts"
      via: "initSettingsStore + ContextService.create"
      pattern: "initSettingsStore"
    - from: "apps/mcp-server/src/stdio.ts"
      to: "buildMcpServer"
      via: "pass searchService and contextService"
      pattern: "buildMcpServer\\("
---

<objective>
Register `read_around` and `read_file` on the shared `buildMcpServer()` factory, wire `ContextService` into `createMcpServices()`, and cover tool behavior with unit tests.

Purpose: Deliver MCP-07, MCP-08, MCP-09, MCP-11 — core read tools delegating to Phase 5 ContextService with locked CONTEXT decisions D-01–D-10.
Output: Extended server.ts, services.ts, stdio/http entrypoints, server.test.ts passing.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-mcp-read-tools/06-CONTEXT.md
@.planning/phases/06-mcp-read-tools/06-PATTERNS.md
@.planning/phases/05-context-retrieval-core/05-CONTEXT.md
@.planning/phases/03-mcp-search-server/03-CONTEXT.md
@apps/mcp-server/src/server.ts
@apps/mcp-server/src/services.ts
@apps/backend/src/services.ts
@packages/core/src/context/context-service.ts
@packages/core/src/context/types.ts
@packages/core/src/context/errors.ts

<interfaces>
From `context-service.ts`:
```typescript
async readAround(documentId: string, chunkIndex: number, options?: ReadAroundOptions): Promise<ReadAroundResult>
async readFile(documentId: string, options?: ReadFileOptions): Promise<ReadFileResult>
```

From `errors.ts`:
```typescript
export class ContextError extends Error { code: ContextErrorCode; }
```

From existing `server.ts`:
```typescript
export function buildMcpServer(searchService: SearchService): McpServer
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ContextService in createMcpServices</name>
  <read_first>
    - .planning/phases/06-mcp-read-tools/06-CONTEXT.md (D-09)
    - .planning/phases/06-mcp-read-tools/06-PATTERNS.md (services.ts section)
    - apps/backend/src/services.ts
    - apps/mcp-server/src/services.ts
    - packages/core/src/index.ts
  </read_first>
  <files>
    apps/mcp-server/src/services.ts
  </files>
  <acceptance_criteria>
    - grep -v '^#' apps/mcp-server/src/services.ts | grep -c 'contextService' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/services.ts | grep -c 'initSettingsStore' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/services.ts | grep -c 'getDocumentRegistry' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/services.ts | grep -c 'ContextService\\.create' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/services.ts | grep -c 'ChromaVectorStore' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Extend **`McpServices`** interface with `contextService: ContextService`.

    Update **`createMcpServices()`** to mirror backend context wiring (05-03 handoff):
    - `settingsStore = initSettingsStore(config)`
    - `registry = getDocumentRegistry(settingsStore.db)`
    - `vectorStore = new ChromaVectorStore(config)`
    - Keep `embeddingClient` + `SearchService.create` for search_knowledge
    - `contextService = ContextService.create(config, { registry, vectorStore, settingsStore })`
    - Return `{ config, searchService, contextService }`

    Do NOT add IngestionService. Do NOT duplicate ContextService logic.
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server exec tsc --noEmit 2>&1</automated>
  </verify>
  <done>McpServices exposes contextService sharing backend SQLite settings path</done>
</task>

<task type="auto">
  <name>Task 2: Register read_around and read_file tools</name>
  <read_first>
    - .planning/phases/06-mcp-read-tools/06-CONTEXT.md (D-01 through D-08, D-05, D-06, D-07)
    - apps/mcp-server/src/server.ts
    - packages/core/src/context/errors.ts
    - packages/core/src/context/types.ts
  </read_first>
  <files>
    apps/mcp-server/src/server.ts
  </files>
  <acceptance_criteria>
    - grep -v '^#' apps/mcp-server/src/server.ts | grep -c 'read_around' | awk '{exit ($1>=2)?0:1}'
    - grep -v '^#' apps/mcp-server/src/server.ts | grep -c 'read_file' | awk '{exit ($1>=2)?0:1}'
    - grep -v '^#' apps/mcp-server/src/server.ts | grep -c 'document_id' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/server.ts | grep -c 'chunk_index' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/server.ts | grep -c 'readAround' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/server.ts | grep -c 'readFile' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/server.ts | grep -c 'documentId.*chunkIndex\|chunkIndex.*documentId' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Change **`buildMcpServer`** signature to:
    `buildMcpServer(searchService: SearchService, contextService: ContextService): McpServer`

    Export constants: `READ_AROUND_TOOL_NAME = "read_around"`, `READ_FILE_TOOL_NAME = "read_file"`.

    **`read_around` tool** (MCP-07):
    - Zod input: `document_id` (string min 1), `chunk_index` (int min 0), optional `window` (int min 0 max 10), optional `collection`
    - Description (D-01, D-02): one concise English sentence + inline example, e.g. "Expand neighboring chunks around a document position. Example: after search_knowledge returns documentId \"abc\" and chunkIndex 3, call with document_id=\"abc\", chunk_index=3."
    - Handler: `contextService.readAround(document_id, chunk_index, { window, collection })`
    - Success: `{ content: [{ type: "text", text: JSON.stringify(result, null, 2) }], structuredContent: result }` — pass full ReadAroundResult including windowRequested, chunkRange, truncated, isCenter on chunks (D-07)
    - Error: catch `ContextError` and generic Error; return `{ content: [{ type: "text", text: message }], isError: true }` (D-08)

    **`read_file` tool** (MCP-08):
    - Zod input: `document_id` (string min 1), optional `collection`
    - Description (D-01): one concise English sentence — bounded full-document read in chunk order
    - Handler: `contextService.readFile(document_id, { collection })`
    - Same response/error pattern as read_around with ReadFileResult in structuredContent

    Keep existing **`search_knowledge`** unchanged except factory signature update.

    Import `ContextService` and `ContextError` from `@kb/core`.
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server exec tsc --noEmit 2>&1</automated>
  </verify>
  <done>read_around and read_file registered with locked schemas and descriptions</done>
</task>

<task type="auto">
  <name>Task 3: Update stdio and HTTP entrypoints</name>
  <read_first>
    - apps/mcp-server/src/stdio.ts
    - apps/mcp-server/src/http.ts
    - .planning/phases/06-mcp-read-tools/06-CONTEXT.md (D-09, MCP-10)
  </read_first>
  <files>
    apps/mcp-server/src/stdio.ts
    apps/mcp-server/src/http.ts
  </files>
  <acceptance_criteria>
    - grep -v '^#' apps/mcp-server/src/stdio.ts | grep -c 'contextService' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/http.ts | grep -c 'contextService' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/stdio.ts | grep -c 'buildMcpServer\\(searchService, contextService\\)' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/http.ts | grep -c 'buildMcpServer\\(searchService, contextService\\)' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`stdio.ts`:** Destructure `{ searchService, contextService }` from `createMcpServices()`; call `buildMcpServer(searchService, contextService)`.

    **`http.ts`:** In `createMcpHttpApp`, destructure `contextService` from resolved services; pass both to `buildMcpServer` inside initialize branch (line ~44).

    Do not change HTTP session transport logic.
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server exec tsc --noEmit 2>&1</automated>
  </verify>
  <done>Both transports use the same buildMcpServer factory with ContextService</done>
</task>

<task type="auto">
  <name>Task 4: Unit tests for tool registration and delegation</name>
  <read_first>
    - apps/mcp-server/src/server.test.ts
    - .planning/phases/06-mcp-read-tools/06-CONTEXT.md (D-10, MCP-11)
    - .planning/phases/03-mcp-search-server/03-01-PLAN.md (forbidden tool tests)
  </read_first>
  <files>
    apps/mcp-server/src/server.test.ts
  </files>
  <acceptance_criteria>
    - pnpm --filter @kb/mcp-server test -- server.test 2>&1 exits 0
    - grep -v '^#' apps/mcp-server/src/server.test.ts | grep -c 'read_around' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/server.test.ts | grep -c 'read_file' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/server.test.ts | grep -c 'toHaveLength(3)' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Update **`connectTestClient`** helper to accept mocked `searchService` and `contextService`; call `buildMcpServer(searchService, contextService)`.

    **Tests to add/update:**
    1. `listTools` returns exactly 3 tools: search_knowledge, read_around, read_file (update from length 1)
    2. `read_around` delegates to `contextService.readAround` with mapped args `{ window, collection }`
    3. `read_around` returns structuredContent matching mock ReadAroundResult (includes chunks array)
    4. `read_around` on ContextError returns `isError: true` and does not include chunks in structuredContent
    5. `read_file` delegates to `contextService.readFile`
    6. Forbidden ingest/write tool grep test still passes (MCP-11)
    7. server.ts still does not import ChromaVectorStore directly

    Mock ContextService with `vi.fn()` for readAround/readFile.
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server test -- server.test 2>&1</automated>
  </verify>
  <done>All server unit tests pass; three retrieval tools only</done>
</task>

</tasks>

<verification>
- `pnpm --filter @kb/mcp-server test` passes
- `pnpm --filter @kb/mcp-server exec tsc --noEmit` passes
- MCP-07, MCP-08, MCP-09, MCP-11 covered by tests and registration
</verification>

<success_criteria>
- MCP client can invoke read_around and read_file via InMemoryTransport unit tests
- ContextService wired with shared SQLite settings store
- Tool descriptions satisfy D-01 and D-02
</success_criteria>

<output>
After completion, create `.planning/phases/06-mcp-read-tools/06-01-SUMMARY.md`
</output>
