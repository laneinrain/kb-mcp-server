---
phase: 06-mcp-read-tools
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - apps/mcp-server/src/http.test.ts
  - scripts/uat-read-around.ts
  - package.json
autonomous: true
requirements:
  - MCP-10
must_haves:
  truths:
    - "HTTP tools/list returns read_around and read_file with same names as stdio unit tests (MCP-10)"
    - "UAT script runs search_knowledge then read_around using first hit documentId and chunkIndex on live stdio stack"
    - "UAT asserts read_around response includes chunks with filename and chunkIndex metadata (MCP-09)"
    - "Phase 6 human checkpoint documented in SUMMARY with pass/fail criteria"
  artifacts:
    - path: "apps/mcp-server/src/http.test.ts"
      provides: "HTTP tool list parity test"
      contains: "read_around"
    - path: "scripts/uat-read-around.ts"
      provides: "search → read_around UAT script"
      contains: "read_around"
  key_links:
    - from: "scripts/uat-read-around.ts"
      to: "apps/mcp-server/dist/stdio.js"
      via: "StdioClientTransport spawn"
      pattern: "StdioClientTransport"
    - from: "apps/mcp-server/src/http.test.ts"
      to: "buildMcpServer"
      via: "mock McpServices with contextService"
      pattern: "tools/list"
---

<objective>
Verify stdio/HTTP transport parity for the expanded tool list and deliver a human UAT checkpoint for the search → read_around workflow on the live stack.

Purpose: Close MCP-10 and ROADMAP success criterion #5 (search → read_around UAT).
Output: http.test.ts parity coverage, `scripts/uat-read-around.ts`, root npm script, 06-02-SUMMARY with UAT results.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-mcp-read-tools/06-CONTEXT.md
@.planning/phases/06-mcp-read-tools/06-PATTERNS.md
@.planning/phases/06-mcp-read-tools/06-01-SUMMARY.md
@apps/mcp-server/src/http.test.ts
@scripts/uat-search-knowledge.ts
@.planning/ROADMAP.md

<interfaces>
From 06-01:
```typescript
export function buildMcpServer(searchService: SearchService, contextService: ContextService): McpServer
export interface McpServices { config; searchService; contextService; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTTP tools/list parity tests</name>
  <read_first>
    - apps/mcp-server/src/http.test.ts
    - apps/mcp-server/src/http.ts
    - .planning/phases/06-mcp-read-tools/06-CONTEXT.md (MCP-10)
    - .planning/phases/03-mcp-search-server/03-03-PLAN.md (HTTP test patterns)
  </read_first>
  <files>
    apps/mcp-server/src/http.test.ts
  </files>
  <acceptance_criteria>
    - pnpm --filter @kb/mcp-server test -- http.test 2>&1 exits 0
    - grep -v '^#' apps/mcp-server/src/http.test.ts | grep -c 'read_around' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/http.test.ts | grep -c 'read_file' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/mcp-server/src/http.test.ts | grep -c 'contextService' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Update **`mockServices()`** in http.test.ts to include mocked `contextService` with `readAround` and `readFile` vi.fn stubs returning minimal valid results.

    Add test: after initialize + session header, POST `tools/list` returns tools array containing exactly three names: `search_knowledge`, `read_around`, `read_file` (order not asserted).

    Add test: POST `tools/call` for `read_around` with `{ document_id, chunk_index }` returns 200 JSON-RPC body without transport error (mock returns sample chunks).

    Ensure tests use updated `createMcpHttpApp(mockServices())` signature compatible with extended McpServices.

    Do not add upload/delete tools to mocks.
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server test -- http.test 2>&1</automated>
  </verify>
  <done>HTTP transport exposes same three retrieval tools as stdio factory</done>
</task>

<task type="auto">
  <name>Task 2: UAT script search → read_around</name>
  <read_first>
    - scripts/uat-search-knowledge.ts
    - .planning/phases/06-mcp-read-tools/06-CONTEXT.md (D-02, UAT criterion)
    - .planning/ROADMAP.md (Phase 6 success criteria #5)
  </read_first>
  <files>
    scripts/uat-read-around.ts
    package.json
  </files>
  <acceptance_criteria>
    - test -f scripts/uat-read-around.ts
    - grep -v '^#' scripts/uat-read-around.ts | grep -c 'search_knowledge' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' scripts/uat-read-around.ts | grep -c 'read_around' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' scripts/uat-read-around.ts | grep -c 'document_id' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' package.json | grep -c 'uat:read-around' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Create **`scripts/uat-read-around.ts`** modeled on uat-search-knowledge.ts:
    1. Spawn stdio transport to `apps/mcp-server/dist/stdio.js` (build required — document in SUMMARY)
    2. Call `search_knowledge` with query `"sample"` (or env `UAT_QUERY` override)
    3. Parse first hit from structuredContent.results[0] — require `documentId` and `chunkIndex`
    4. Call `read_around` with snake_case `{ document_id: hit.documentId, chunk_index: hit.chunkIndex }`
    5. Assert response is not error; structuredContent or parsed JSON has `chunks` array length >= 1
    6. Assert first chunk has `filename` and `chunkIndex` fields
    7. Print PASS with JSON summary; exit 1 on failure/timeout (30s)

    Add root **`package.json`** script: `"uat:read-around": "tsx scripts/uat-read-around.ts"` (or match existing uat script runner pattern in repo).

    Prerequisites note in script header comment: `pnpm dev` or Chroma+backend+ingested sample doc running; run `pnpm --filter @kb/mcp-server build` first.
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server build 2>&1</automated>
  </verify>
  <done>UAT script exists and is invocable via pnpm uat:read-around</done>
</task>

<task type="checkpoint" autonomous="false">
  <name>Task 3: Human UAT — search → read_around on live stack</name>
  <read_first>
    - .planning/phases/06-mcp-read-tools/06-CONTEXT.md
    - .planning/ROADMAP.md (Phase 6 success criteria)
    - scripts/uat-read-around.ts
  </read_first>
  <what-built>
    MCP read tools on stdio + HTTP with ContextService wiring. Automated unit/integration tests pass. UAT script ready for live corpus.
  </what-built>
  <how-to-verify>
    1. Ensure stack running: `pnpm dev` (Chroma, backend, MCP built)
    2. Confirm at least one ingested document (web 文档 tab or CLI ingest)
    3. Build MCP: `pnpm --filter @kb/mcp-server build`
    4. Run: `pnpm uat:read-around`
    5. Expect stdout PASS and read_around chunks with filename + chunkIndex
    6. Optional: Cursor MCP config — search a topic, then verify agent can call read_around on a hit
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms uat:read-around exits 0 on live stack
    - Operator confirms read_around returned neighboring chunk text (not 500-char search snippet only)
  </acceptance_criteria>
  <action>
    Run the UAT script against live stack. Record PASS/FAIL and any blockers in 06-02-SUMMARY.md under ## UAT Results.
  </action>
  <verify>
    <automated>pnpm uat:read-around 2>&1; echo exit=$?</automated>
  </verify>
  <done>Human UAT approved or blockers documented for follow-up</done>
</task>

</tasks>

<verification>
- `pnpm --filter @kb/mcp-server test` passes (all mcp-server tests)
- `pnpm uat:read-around` passes on live stack with ingested corpus
</verification>

<success_criteria>
- MCP-10: identical tool names on stdio unit tests and HTTP tools/list
- ROADMAP criterion 5: search → read_around workflow passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-mcp-read-tools/06-02-SUMMARY.md`
</output>
