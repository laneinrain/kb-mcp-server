---
phase: 04-admin-surfaces-security
plan: 04
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - apps/backend/package.json
  - apps/backend/src/static.ts
  - apps/backend/src/index.ts
  - package.json
  - turbo.json
autonomous: false
requirements: []
must_haves:
  truths:
    - "Prod: backend serves apps/web/dist via @fastify/static with SPA fallback"
    - "pnpm dev starts @kb/web alongside backend and MCP"
    - "turbo build ensures @kb/web builds before @kb/backend when backend depends on web dist"
    - "Operator confirms full Phase 4 admin trifecta via human E2E"
  artifacts:
    - path: "apps/backend/src/static.ts"
      provides: "Production SPA static serve + fallback"
      contains: "fastifyStatic"
    - path: "apps/backend/src/index.ts"
      provides: "registerWebStatic after API routes"
      contains: "registerWebStatic"
    - path: "turbo.json"
      provides: "build dependency @kb/backend on @kb/web#build"
      contains: "@kb/web"
  key_links:
    - from: "apps/backend/src/index.ts"
      to: "apps/web/dist"
      via: "registerWebStatic in production"
      pattern: "web/dist"
    - from: "package.json"
      to: "apps/web"
      via: "dev script includes @kb/web dev"
      pattern: "@kb/web"
---

<objective>
Wire production static serving, dev orchestration with Vite, turbo build ordering, and human E2E verification of the complete Phase 4 admin trifecta.

Purpose: Same-origin prod delivery and integrated dev stack; validate web + CLI + auth parity end-to-end.
Output: Backend static integration, updated root dev scripts, E2E checkpoint passed.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-surfaces-security/04-CONTEXT.md
@.planning/phases/04-admin-surfaces-security/04-RESEARCH.md
@.planning/ROADMAP.md (Phase 4 Success Criteria)
@apps/backend/src/index.ts
@package.json
@turbo.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static serve and dev orchestration</name>
  <read_first>
    - .planning/phases/04-admin-surfaces-security/04-RESEARCH.md (Pattern 3 static, dev proxy)
    - apps/backend/src/index.ts
    - package.json (root dev script)
    - turbo.json
  </read_first>
  <files>
    apps/backend/package.json
    apps/backend/src/static.ts
    apps/backend/src/index.ts
    package.json
    turbo.json
  </files>
  <acceptance_criteria>
    - grep -v '^#' apps/backend/package.json | grep -c '@fastify/static' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/static.ts | grep -c 'fastifyStatic' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/index.ts | grep -c 'registerWebStatic' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' package.json | grep -c '@kb/web' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' turbo.json | grep -c '@kb/web' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Create **`apps/backend/src/static.ts`** per RESEARCH Pattern 3:
    - `registerWebStatic(app)`: `@fastify/static` root `join(import.meta.dirname, "../../web/dist")`, `wildcard: false`
    - Register **after** API routes in `index.ts`
    - `setNotFoundHandler`: `/api/*` → JSON 404; other paths → `sendFile("index.html")` for SPA
    - Register when `NODE_ENV === "production"` OR `SERVE_WEB=true` for local prod smoke

    Install **`@fastify/static@9.1.3`** on backend.

    Update root **`package.json`**:
    - Extend `dev` to include `@kb/web` dev server (concurrently alongside backend/mcp/chroma)
    - Add `"dev:web": "pnpm --filter @kb/web dev"` if helpful

    Update **`turbo.json`**: add explicit build dependency so `@kb/backend#build` depends on `@kb/web#build` (ensures `apps/web/dist` exists before prod static serve).
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1 && pnpm --filter @kb/backend test 2>&1</automated>
  </verify>
  <done>Static serve wired; dev script starts web; turbo enforces web-before-backend build order</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Admin trifecta E2E verification</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 Success Criteria)
    - .planning/phases/02-rest-backend-search/02-03-PLAN.md (E2E checkpoint format)
  </read_first>
  <acceptance_criteria>
    - Operator confirms upload → list → search → delete in browser
    - Operator confirms pnpm ingest and kb list reflect same corpus as web
    - With AUTH_ENABLED=true: unauthenticated API 401; web modal + CLI Bearer work
  </acceptance_criteria>
  <what-built>
    Complete Phase 4 admin trifecta: React web admin, CLI ingest/list/delete, optional bearer auth, Vite dev proxy, production static serve from backend.
  </what-built>
  <how-to-verify>
    **Dev flow (auth off — default):**
    1. Ensure `.env` has valid `CHERRYIN_API_KEY`; run `pnpm dev` from repo root
    2. Open `http://127.0.0.1:5173` — Documents tab loads
    3. Upload `scripts/fixtures/sample.txt` via drag-drop or Choose file — **Indexing…** then indexed row
    4. Search tab — query sample content, topK 5, **Run search** — score, snippet, metadata
    5. Delete via confirm dialog — row removed
    6. `pnpm ingest scripts/fixtures/sample.txt` then CLI `list` — same document
    7. CLI `delete <id>` — gone from web after refresh

    **Auth enabled:**
    8. `AUTH_ENABLED=true`, `API_KEY=test-secret-key`; restart
    9. `curl http://127.0.0.1:3000/api/v1/documents` → 401; `/health` → ok
    10. Web modal + CLI with Bearer succeed

    **Prod static (optional):**
    11. `pnpm --filter @kb/web build && SERVE_WEB=true pnpm --filter @kb/backend dev` — SPA at `:3000`

    **NOT tested:** MCP HTTP auth, chunk settings UI
  </how-to-verify>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1 && pnpm --filter @kb/backend test 2>&1</automated>
  </verify>
  <done>Operator confirms Phase 4 success criteria 1–5</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @kb/web build` succeeds
- `pnpm --filter @kb/backend test` passes
- Human checkpoint confirms full admin workflow
</verification>

<success_criteria>
- Phase 4 criteria 4–5: auth parity and REST corpus consistency across web/CLI
- Prod same-origin SPA delivery works
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-surfaces-security/04-04-SUMMARY.md`
</output>
