---
phase: 04-admin-surfaces-security
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/vite.config.ts
  - apps/web/index.html
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - apps/web/src/App.css
  - apps/web/src/api/client.ts
  - apps/web/src/api/documents.ts
  - apps/web/src/api/search.ts
  - apps/web/src/components/AppShell.tsx
  - apps/web/src/components/UploadPanel.tsx
  - apps/web/src/components/DocumentTable.tsx
  - apps/web/src/components/SearchPanel.tsx
  - apps/web/src/components/ApiKeyModal.tsx
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - WEB-03
  - WEB-04
must_haves:
  truths:
    - "Operator can upload .txt/.md/.markdown/.pdf via web UI drag-drop or file picker (WEB-01)"
    - "Operator sees document list with filename, status, chunkCount, collection, updatedAt (WEB-02)"
    - "Operator can delete a document after confirm dialog; list refreshes (WEB-03)"
    - "Operator can run test search with topK 1–10 and see ranked results with score, text, documentId, filename, chunkIndex (WEB-04)"
    - "Dev: Vite :5173 proxies /api to backend :3000 (vite.config.ts)"
    - "On 401, ApiKeyModal prompts; key stored in sessionStorage; Bearer sent on retry (CONF-03 web UX)"
    - "Web uses REST only — no @kb/core in browser bundle"
  artifacts:
    - path: "apps/web/src/components/UploadPanel.tsx"
      provides: "Multipart upload to POST /api/v1/documents"
      contains: "FormData"
    - path: "apps/web/src/components/DocumentTable.tsx"
      provides: "Document list and delete action"
      contains: "Delete"
    - path: "apps/web/src/components/SearchPanel.tsx"
      provides: "Test search form and results"
      contains: "Run search"
    - path: "apps/web/src/components/ApiKeyModal.tsx"
      provides: "401-triggered API key prompt"
      contains: "sessionStorage"
    - path: "apps/web/vite.config.ts"
      provides: "Dev proxy /api → 127.0.0.1:3000"
      contains: "proxy"
  key_links:
    - from: "apps/web/src/api/client.ts"
      to: "/api/v1/*"
      via: "relative fetch with optional Bearer"
      pattern: "fetch\\("
    - from: "apps/web/vite.config.ts"
      to: "http://127.0.0.1:3000"
      via: "server.proxy /api"
      pattern: "127\\.0\\.0\\.1:3000"
    - from: "apps/web/src/components/UploadPanel.tsx"
      to: "POST /api/v1/documents"
      via: "TanStack Query mutation"
      pattern: "/api/v1/documents"
---

<objective>
Scaffold and implement the Vite + React admin SPA (WEB-01–WEB-04) per UI-SPEC with TanStack Query REST client.

Purpose: Deliver operator-facing web UI with upload, list, delete, and test search — REST-only data path, no @kb/core in browser.
Output: `apps/web` SPA with all UI components; static serve and E2E in Plan 04-04.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-admin-surfaces-security/04-CONTEXT.md
@.planning/phases/04-admin-surfaces-security/04-RESEARCH.md
@.planning/phases/04-admin-surfaces-security/04-PATTERNS.md
@.planning/phases/04-admin-surfaces-security/04-UI-SPEC.md
@apps/backend/src/index.ts
@packages/core/src/registry/types.ts

<interfaces>
Document list item (API response — no sourcePath):
```typescript
export interface DocumentRecord {
  id: string;
  filename: string;
  mimeType: string;
  status: "pending" | "processing" | "indexed" | "failed";
  chunkCount: number;
  collection: string;
  createdAt: string;
  updatedAt: string;
}
```

Search response (POST /api/v1/search):
```typescript
{ results: Array<{
  score: number;
  text: string;
  documentId: string;
  filename: string;
  chunkIndex: number;
}> }
```

Upload success: `{ documentId, chunkCount, collection, status: "indexed" }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/web and shared API layer</name>
  <read_first>
    - .planning/phases/04-admin-surfaces-security/04-UI-SPEC.md (Design System, ApiClient)
    - .planning/phases/04-admin-surfaces-security/04-RESEARCH.md (Pattern 2, Pattern 5)
    - .planning/phases/04-admin-surfaces-security/04-PATTERNS.md (web package, api client)
    - apps/mcp-server/package.json
  </read_first>
  <files>
    apps/web/package.json
    apps/web/tsconfig.json
    apps/web/vite.config.ts
    apps/web/index.html
    apps/web/src/main.tsx
    apps/web/src/App.tsx
    apps/web/src/App.css
    apps/web/src/api/client.ts
    apps/web/src/api/documents.ts
    apps/web/src/api/search.ts
  </files>
  <acceptance_criteria>
    - grep -v '^#' apps/web/package.json | grep -c '"name": "@kb/web"' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/package.json | grep -c '@tanstack/react-query' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/vite.config.ts | grep -c 'proxy' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/src/api/client.ts | grep -c 'sessionStorage' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/src/api/client.ts | grep -c '@kb/core' | awk '{exit ($1==0)?0:1}'
    - grep -v '^#' apps/web/src/App.tsx | grep -c 'QueryClientProvider' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Create **`apps/web/`** per RESEARCH Standard Stack:
    - Dependencies: `react@19.2.7`, `react-dom@19.2.7`, `@tanstack/react-query@5.101.2`
    - DevDependencies: `vite@8.1.3`, `@vitejs/plugin-react@6.0.3`, TypeScript, `@types/react`, `@types/react-dom`
    - **No** shadcn, Tailwind, or `@kb/core` in web package

    **`vite.config.ts`**: plugin-react; `server.port: 5173`; proxy `/api` → `http://127.0.0.1:3000`, `changeOrigin: true`.

    **`index.html`**, **`main.tsx`**: standard Vite React entry.

    **`App.css`**: minimal CSS per UI-SPEC — spacing scale, colors (`#f4f4f5` bg, `#2563eb` accent), typography (14px body, system fonts). Single file — no component library.

    **`api/client.ts`** per CONTEXT + UI-SPEC:
    - `getApiKey()` / `setApiKey(key)` using `sessionStorage` key `kb_api_key`
    - **Never** use `import.meta.env.VITE_*` for API key
    - `apiRequest(path, init)`: relative URLs only (`/api/v1/...`); attach `Authorization: Bearer` when key present
    - On 401: export callback hook `onUnauthorized` for ApiKeyModal wiring (set from App)
    - Parse error JSON `{ error, message }` on failure

    **`api/documents.ts`**: `uploadFile(file)`, `listDocuments()`, `deleteDocument(id)` wrapping apiRequest.

    **`api/search.ts`**: `searchDocuments(query, topK)` → POST body `{ query, topK }`.

    **`App.tsx`**: QueryClientProvider shell; placeholder for AppShell (Task 2). **Do NOT** add chunk settings UI — deferred per CONTEXT.
  </action>
  <verify>
    <automated>pnpm install 2>&1 && pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>apps/web builds; API layer uses relative /api paths and sessionStorage auth pattern</done>
</task>

<task type="auto">
  <name>Task 2: UI components per UI-SPEC (WEB-01–WEB-04)</name>
  <read_first>
    - .planning/phases/04-admin-surfaces-security/04-UI-SPEC.md (full component inventory, copywriting)
    - .planning/phases/04-admin-surfaces-security/04-CONTEXT.md (list fields, delete confirm, search fields)
    - apps/backend/src/routes/documents.ts
    - apps/backend/src/routes/search.ts
  </read_first>
  <files>
    apps/web/src/App.tsx
    apps/web/src/App.css
    apps/web/src/components/AppShell.tsx
    apps/web/src/components/UploadPanel.tsx
    apps/web/src/components/DocumentTable.tsx
    apps/web/src/components/SearchPanel.tsx
    apps/web/src/components/ApiKeyModal.tsx
  </files>
  <acceptance_criteria>
    - grep -v '^#' apps/web/src/components/UploadPanel.tsx | grep -c 'accept=' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/src/components/DocumentTable.tsx | grep -c 'chunkCount' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/src/components/DocumentTable.tsx | grep -c 'Delete' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/src/components/SearchPanel.tsx | grep -c 'chunkIndex' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/src/components/ApiKeyModal.tsx | grep -c 'kb_api_key' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/src/components/AppShell.tsx | grep -c 'tablist' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Implement components per **04-UI-SPEC.md** (native HTML + App.css only):

    **`AppShell`**: header "kb admin"; tab bar Documents | Search (`role="tablist"`); max-width 960px centered; default tab Documents.

    **`UploadPanel`** (WEB-01):
    - Hidden file input `accept=".txt,.md,.markdown,.pdf"`; visible **Choose file** button per UI-SPEC triggers input click
    - Drop zone with drag-over accent border
    - Auto-upload on select/drop via `useMutation` → `uploadFile`
    - Pending state: **Indexing…** + disable zone; success invalidates `['documents']` query
    - Error banners: 415 uses UI-SPEC copy; generic API errors show `message`

    **`DocumentTable`** (WEB-02, WEB-03):
    - `useQuery(['documents'], listDocuments)` — loading/error/empty states per UI-SPEC
    - Columns: filename (truncate 40ch), status badge colors, chunkCount, collection, updatedAt formatted
    - **Delete** button → `<dialog>` confirm with destructive copy naming `{filename}` → `useMutation` delete → invalidate list
    - No `window.confirm`

    **`SearchPanel`** (WEB-04):
    - Query field (required, max 2000); topK select default 5, options 1–10
    - **Run search** mutation → render result cards: score (3 decimals monospace), filename · chunk N, text snippet pre-wrap max-height 120px, documentId truncated

    **`ApiKeyModal`** (CONF-03 web UX):
    - Opens on first 401 via client `onUnauthorized` callback
    - Password input; **Save key** → sessionStorage → close → retry failed request once
    - Wrong key: reopen with **Invalid API key** inline error

    Wire all in **`App.tsx`**. Use UI-SPEC copywriting verbatim for CTAs, empty states, errors.

    **Out of scope:** chunk settings panel, collection picker on upload, dark mode, MCP auth UI.
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>WEB-01–WEB-04 UI implemented per UI-SPEC; TanStack Query drives all REST calls</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/v1/* | Untrusted file uploads and queries; optional sessionStorage API key |
| Static SPA → operator | No secrets in Vite bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | I | API key in Vite bundle | mitigate | sessionStorage only; no VITE_* secrets |
| T-04-11 | T | Unauthorized web upload | mitigate | Backend bearer gate when AUTH_ENABLED; modal collects key |
| T-04-12 | T | XSS stealing sessionStorage key | accept | Localhost dev tool; single-operator scaffold |
| T-04-13 | E | Static catch-all swallows /api | mitigate | Register static after API routes; not-found handler checks /api/ prefix |
| T-04-14 | I | Parallel @kb/core in browser | mitigate | grep gate: no @kb/core import in apps/web |
</threat_model>

<verification>
- `pnpm --filter @kb/web build` succeeds
- Dev: Vite proxy configured for /api → backend :3000
- No @kb/core imports in apps/web
- No MCP HTTP auth changes; no chunk settings UI
</verification>

<success_criteria>
- WEB-01: upload txt, md, pdf via web UI
- WEB-02: view document list with ingestion status
- WEB-03: delete with confirmation
- WEB-04: test search with ranked results
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-surfaces-security/04-03-SUMMARY.md`
</output>
