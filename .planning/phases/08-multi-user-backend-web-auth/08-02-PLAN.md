---
phase: 08-multi-user-backend-web-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - apps/backend/src/auth.ts
  - apps/backend/src/types.ts
  - apps/backend/src/index.ts
  - apps/backend/src/services.ts
  - apps/backend/src/routes/documents.ts
  - apps/backend/src/routes/search.ts
  - apps/backend/src/routes/settings.ts
  - packages/core/src/search/search-service.ts
  - apps/backend/src/routes/documents.multi-user.test.ts
  - apps/backend/src/routes/search.multi-user.test.ts
  - .env.example
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05
  - USER-02
  - USER-04
must_haves:
  truths:
    - "When USER_AUTH_ENABLED, /api/v1/documents and /search require JWT or API_KEY (D-06, D-12)"
    - "JWT user sees own docs + system legacy docs; cannot get/delete other users' docs (D-15, D-16, D-18)"
    - "API_KEY service mode sees all documents (D-11, D-21)"
    - "401 shape unchanged for missing/invalid auth (AUTH-05, D-07)"
    - "Upload sets user_id to JWT authUser.id (D-19)"
    - "JWT path sets authUser and scopes list/search/upload/delete (D-10)"
    - "USER_AUTH_ENABLED and AUTH_ENABLED remain independent flags (D-09)"
    - "Health, /docs, POST /api/v1/auth/login stay public — no preHandler (D-08)"
    - "MCP HTTP unchanged — no MCP auth changes this phase (D-20)"
  artifacts:
    - path: "apps/backend/src/auth.ts"
      provides: "composite preHandler"
      contains: "createProtectedRouteOpts"
    - path: "apps/backend/src/routes/documents.multi-user.test.ts"
      provides: "user isolation tests"
      contains: "not_found"
  key_links:
    - from: "apps/backend/src/index.ts"
      to: "apps/backend/src/auth.ts"
      via: "protectedRouteOpts"
      pattern: "USER_AUTH_ENABLED"
---

<objective>
Wire **composite JWT + API_KEY auth** and **user-scoped document/search** routes (AUTH-04, AUTH-05, USER-02, USER-04).

Depends on 08-01 user_id migration and system user.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-multi-user-backend-web-auth/08-CONTEXT.md
@.planning/phases/08-multi-user-backend-web-auth/08-RESEARCH.md
@apps/backend/src/auth.ts
@packages/auth/src/fastify.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| IDOR via documentId | High | get/delete check ownership; 404 cross-user (D-16) |
| JWT bypass via API_KEY off | Medium | USER_AUTH_ENABLED requires JWT unless AUTH_ENABLED service key |
| Settings tampering | Medium | Protect settings routes when USER_AUTH_ENABLED (D-61 discretion) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Composite protectedRouteOpts (JWT or API_KEY)</name>
  <read_first>
    - apps/backend/src/auth.ts
    - packages/auth/src/fastify.ts
    - .planning/phases/08-multi-user-backend-web-auth/08-CONTEXT.md (D-09–D-12)
  </read_first>
  <files>
    apps/backend/src/auth.ts
    apps/backend/src/types.ts
  </files>
  <acceptance_criteria>
    - grep -c "authMode" apps/backend/src/types.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "protectedRouteOpts" apps/backend/src/auth.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "createJwtPreHandler" apps/backend/src/auth.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Extend Fastify request type: `authUser?: AuthUser`, `authMode?: 'user' | 'service'`.

    Add **`protectedRouteOpts(config, app, authProvider)`**:
    - If `!config.USER_AUTH_ENABLED`: return existing `apiRouteOpts` behavior (API_KEY only when AUTH_ENABLED).
    - If `USER_AUTH_ENABLED`: preHandler tries Bearer token — validate JWT via `createJwtPreHandler` logic; on success `authMode='user'`. Else if `AUTH_ENABLED` try `verifyBearerAuth` → `authMode='service'`. Else 401 `{ error: 'unauthorized', message: '...' }`.

    Keep health/docs/login public (no opts).
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend build 2>&1</automated>
  </verify>
  <done>Single preHandler accepts JWT or service API_KEY</done>
</task>

<task type="auto">
  <name>Task 2: Wire services and index to system user + protected opts</name>
  <read_first>
    - apps/backend/src/services.ts
    - apps/backend/src/index.ts
    - packages/auth/src/user-store.ts
  </read_first>
  <files>
    apps/backend/src/services.ts
    apps/backend/src/index.ts
    .env.example
  </files>
  <acceptance_criteria>
    - grep -c "systemUserId" apps/backend/src/services.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "protectedRouteOpts" apps/backend/src/index.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "USER_AUTH_ENABLED" .env.example | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **`createAppServices`**: when auth enabled, open auth DB, `ensureSystemUser()`, run registry migration with systemUserId, expose `systemUserId` on AppServices.

    **`index.ts`**: use `protectedRouteOpts(config, app, authProvider)` for documents/search/settings.

    Document in **`.env.example`**: when using CLI with USER_AUTH, set `AUTH_ENABLED=true` + `API_KEY`.
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend build 2>&1</automated>
  </verify>
  <done>Backend boots with migration + composite auth</done>
</task>

<task type="auto">
  <name>Task 3: Scope document and search routes</name>
  <read_first>
    - apps/backend/src/routes/documents.ts
    - apps/backend/src/routes/search.ts
    - packages/core/src/registry/document-registry.ts
    - packages/core/src/search/search-service.ts
  </read_first>
  <files>
    apps/backend/src/routes/documents.ts
    apps/backend/src/routes/search.ts
    apps/backend/src/routes/settings.ts
    packages/core/src/search/search-service.ts
    apps/backend/src/routes/documents.multi-user.test.ts
    apps/backend/src/routes/search.multi-user.test.ts
  </files>
  <acceptance_criteria>
    - grep -c "listDocumentsForUser" apps/backend/src/routes/documents.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "authMode" apps/backend/src/routes/documents.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/backend test -- multi-user exits 0
  </acceptance_criteria>
  <action>
    **GET /documents**: JWT → `listDocumentsForUser(authUser.id, systemUserId)`; service → `listDocuments()`.

    **GET/DELETE /documents/:id**: JWT → allow if doc.userId is authUser.id OR systemUserId (read); delete only if doc.userId === authUser.id (system docs: 404 for JWT). Service → full access.

    **POST /documents**: JWT → pass `authUser.id` to ingest; service → assign systemUserId OR reject service upload (prefer: service uses systemUserId for CLI bulk — document in code comment).

    **POST /search**: JWT → search then filter hits to visible document IDs; service → unfiltered.

    Add integration tests: user A token cannot GET user B doc by id (404); both see system doc; API_KEY sees all.
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend test 2>&1</automated>
  </verify>
  <done>USER-02 isolation enforced at API layer</done>
</task>

</tasks>

<verification>
- [ ] Unauthenticated POST /api/v1/documents → 401 when USER_AUTH_ENABLED
- [ ] User A cannot delete User B document
- [ ] Both users list includes legacy system-owned doc
</verification>
