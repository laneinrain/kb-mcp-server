---
phase: 08-multi-user-backend-web-auth
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - apps/web/src/api/client.ts
  - apps/web/src/App.tsx
  - apps/web/src/components/AppShell.tsx
  - apps/web/src/App.css
  - apps/web/src/components/DocumentTable.tsx
  - apps/cli/src/commands/ingest.ts
  - apps/cli/src/commands/ingest.test.ts
  - README.md
  - packages/auth/README.md
autonomous: true
requirements:
  - WEB-03
  - WEB-04
must_haves:
  truths:
    - "Web uses JWT only — no ApiKeyModal / sessionStorage API key (D-01)"
    - "401 redirects to /login with token cleared (D-02)"
    - "Header 退出登录 clears kb_access_token and redirects (D-04, WEB-04)"
    - "WEB-03 complete — admin requires login (D-03)"
    - "CLI direct core path blocked when USER_AUTH_ENABLED (D-13)"
    - "CLI continues global API_KEY when AUTH_ENABLED — no per-user CLI login (D-21)"
    - "WEB-02 deferred — no register page (D-05)"
  artifacts:
    - path: "apps/web/src/components/AppShell.tsx"
      provides: "logout button"
      contains: "退出登录"
    - path: "apps/web/src/App.tsx"
      provides: "no ApiKeyModal"
      contains: "AppShell"
  key_links:
    - from: "apps/web/src/api/client.ts"
      to: "apps/web/src/lib/auth-token.ts"
      via: "getAccessToken only"
      pattern: "kb_access_token"
---

<objective>
Complete Web auth UX (JWT-only, logout, 401 → login) and CLI guard when multi-user enabled (WEB-03, WEB-04, D-01–D-04, D-13). WEB-02 explicitly **not** implemented (D-05).

Depends on 08-02 protected API.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-multi-user-backend-web-auth/08-CONTEXT.md
@apps/web/src/App.tsx
@apps/web/src/api/client.ts
@apps/cli/src/commands/ingest.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| API key in sessionStorage | Medium | Remove modal and sessionStorage key paths (D-01) |
| CLI bypass user scope | High | Fail fast when USER_AUTH_ENABLED without REST+API_KEY (D-13) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Web JWT-only client and 401 redirect</name>
  <read_first>
    - apps/web/src/api/client.ts
    - apps/web/src/lib/auth-token.ts
    - apps/web/src/App.tsx
    - .planning/phases/08-multi-user-backend-web-auth/08-CONTEXT.md (D-01, D-02)
  </read_first>
  <files>
    apps/web/src/api/client.ts
    apps/web/src/App.tsx
  </files>
  <acceptance_criteria>
    - grep -c "ApiKeyModal" apps/web/src/App.tsx | awk '{exit ($1==0)?0:1}'
    - grep -c "sessionStorage" apps/web/src/api/client.ts | awk '{exit ($1==0)?0:1}'
    - grep -c "/login" apps/web/src/api/client.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/web build exits 0
  </acceptance_criteria>
  <action>
    Remove **`ApiKeyModal`**, `setUnauthorizedHandler`, sessionStorage API key helpers from Web flow.

    **`apiRequest`**: use only `getAccessToken()` for Bearer; on 401 call `clearAccessToken()` and `window.location.replace('/login')`.

    Remove unused imports from **`App.tsx`**.
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>Human Web flow is JWT-only</done>
</task>

<task type="auto">
  <name>Task 2: Header logout and legacy doc delete UX</name>
  <read_first>
    - apps/web/src/components/AppShell.tsx
    - apps/web/src/components/DocumentTable.tsx
    - apps/web/src/App.css
  </read_first>
  <files>
    apps/web/src/components/AppShell.tsx
    apps/web/src/components/DocumentTable.tsx
    apps/web/src/App.css
  </files>
  <acceptance_criteria>
    - grep -c "退出登录" apps/web/src/components/AppShell.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c "clearAccessToken" apps/web/src/components/AppShell.tsx | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/web build exits 0
  </acceptance_criteria>
  <action>
    **`AppShell`**: add header action **「退出登录」** → `clearAccessToken()`, `window.location.href = '/login'`.

    **`DocumentTable`**: hide or disable delete for documents where `userId === systemUserId` (expose userId in API response when USER_AUTH enabled) — shared legacy read-only in Web (D-18).

    Style logout button in **`App.css`** to match header.
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>WEB-04 logout + legacy delete guard in UI</done>
</task>

<task type="auto">
  <name>Task 3: CLI guard and README auth matrix</name>
  <read_first>
    - apps/cli/src/commands/ingest.ts
    - README.md
    - packages/auth/README.md
    - .planning/phases/08-multi-user-backend-web-auth/08-CONTEXT.md (D-13, D-21)
  </read_first>
  <files>
    apps/cli/src/commands/ingest.ts
    apps/cli/src/commands/ingest.test.ts
    README.md
    packages/auth/README.md
  </files>
  <acceptance_criteria>
    - grep -c "USER_AUTH_ENABLED" apps/cli/src/commands/ingest.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "AUTH_ENABLED" README.md | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/cli test exits 0
  </acceptance_criteria>
  <action>
    **`ingest.ts`**: if `config.USER_AUTH_ENABLED && !config.AUTH_ENABLED`, exit with message to set AUTH_ENABLED + API_KEY for CLI REST ingest.

    Update **README** and **`packages/auth/README.md`** with flag matrix: Web=JWT, CLI=API_KEY global, MCP unchanged.

    Note WEB-02 deferred (JIT register).
  </action>
  <verify>
    <automated>pnpm --filter @kb/cli test 2>&1</automated>
  </verify>
  <done>D-13 CLI guard documented</done>
</task>

</tasks>

<verification>
- [ ] Manual: login → upload doc → logout → redirect login
- [ ] Manual: two users both see pre-migration doc in list
- [ ] `pnpm --filter @kb/web build` passes
</verification>
