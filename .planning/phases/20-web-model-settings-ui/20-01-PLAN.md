---
phase: 20-web-model-settings-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/api/settings.ts
  - apps/web/src/components/SettingsPanel.tsx
autonomous: true
requirements:
  - CONF-07
must_haves:
  truths:
    - "Web SettingsResponse includes models + embeddingDimensions (D-01)"
    - "SettingsPanel has Embedding/Rerank form with separate save mutation (D-02–D-06)"
    - "Client validates model strings and candidates 1–50 before PATCH"
  artifacts:
    - path: "apps/web/src/api/settings.ts"
      provides: "updateModelSettings"
      contains: "settings/models"
    - path: "apps/web/src/components/SettingsPanel.tsx"
      provides: "Embedding / Rerank section"
      contains: "rerankEnabled"
  key_links:
    - from: "apps/web/src/components/SettingsPanel.tsx"
      to: "apps/web/src/api/settings.ts"
      via: "updateModelSettings"
      pattern: "updateModelSettings"
---

<objective>
Wire Web API types and add **Embedding / Rerank** settings form that PATCHes `/api/v1/settings/models` (CONF-07).

Output: `@kb/web` build passes. CONF-08 warning/dimensions can be stubbed or deferred to 20-02 if cleaner — prefer include read-only dimensions placeholder in same section if trivial.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-web-model-settings-ui/20-CONTEXT.md
@.planning/phases/20-web-model-settings-ui/20-RESEARCH.md
@apps/web/src/api/settings.ts
@apps/web/src/components/SettingsPanel.tsx
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Empty model string sent | Medium | Client trim + min length; server Zod already |
| Context + model mutations race banners | Low | Separate success messages |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend settings API client</name>
  <read_first>
    - apps/web/src/api/settings.ts
    - apps/backend/src/routes/settings.ts
  </read_first>
  <files>
    apps/web/src/api/settings.ts
  </files>
  <acceptance_criteria>
    - grep -c "ModelSettings" apps/web/src/api/settings.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "settings/models" apps/web/src/api/settings.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c "embeddingDimensions" apps/web/src/api/settings.ts | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
  Add `ModelSettings`, extend `SettingsResponse`, implement `updateModelSettings` PATCH helper (D-01).
  </action>
  <verify>
    <automated>pnpm --filter @kb/web exec tsc -p tsconfig.json --noEmit 2>&1</automated>
  </verify>
  <done>Client types match Phase 19 API</done>
</task>

<task type="auto">
  <name>Task 2: SettingsPanel Embedding/Rerank form</name>
  <read_first>
    - apps/web/src/components/SettingsPanel.tsx
    - apps/web/src/App.css
  </read_first>
  <files>
    apps/web/src/components/SettingsPanel.tsx
  </files>
  <acceptance_criteria>
    - grep -c "保存模型设置" apps/web/src/components/SettingsPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c "updateModelSettings" apps/web/src/components/SettingsPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/web build exits 0
  </acceptance_criteria>
  <action>
  Add section「Embedding / Rerank」above context form (D-02–D-06): local state synced from query; checkbox + text/number fields; separate mutation; client validation; success/error banners for model save.
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>Operators can save model settings from Web</done>
</task>

</tasks>

<verification>
  <automated>pnpm --filter @kb/web build 2>&1</automated>
</verification>

<success_criteria>
- Form loads models from GET and persists via PATCH
</success_criteria>

<output>
After completion, create `.planning/phases/20-web-model-settings-ui/20-01-SUMMARY.md`
</output>
