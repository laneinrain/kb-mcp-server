---
phase: 20-web-model-settings-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - apps/web/src/components/SettingsPanel.tsx
  - apps/web/src/App.css
autonomous: true
requirements:
  - CONF-08
must_haves:
  truths:
    - "embeddingDimensions shown read-only with env note (D-07)"
    - "Warning banner when embeddingModel differs from saved baseline (D-08, D-09)"
    - "pnpm --filter @kb/web build succeeds (D-11)"
  artifacts:
    - path: "apps/web/src/components/SettingsPanel.tsx"
      provides: "embedding change warning"
      contains: "重新上传"
  key_links:
    - from: "apps/web/src/components/SettingsPanel.tsx"
      to: "GET embeddingDimensions"
      via: "settingsQuery.data.embeddingDimensions"
      pattern: "embeddingDimensions"
---

<objective>
Complete CONF-08: read-only vector dimensions display + embedding-model change risk warning on SettingsPanel.

Output: Web build green; warning copy in 简体中文 per D-08.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-web-model-settings-ui/20-CONTEXT.md
@.planning/phases/20-web-model-settings-ui/20-RESEARCH.md
@apps/web/src/components/SettingsPanel.tsx
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| User ignores warning and breaks search | Medium | Persistent visible banner before save |
| Warning stuck after save | Low | Reset baseline on successful mutation |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Dimensions + embedding change warning</name>
  <read_first>
    - apps/web/src/components/SettingsPanel.tsx
    - apps/web/src/App.css
  </read_first>
  <files>
    apps/web/src/components/SettingsPanel.tsx
    apps/web/src/App.css
  </files>
  <acceptance_criteria>
    - grep -c "embeddingDimensions" apps/web/src/components/SettingsPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c "EMBEDDING_DIMENSIONS" apps/web/src/components/SettingsPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - grep -c "重新上传" apps/web/src/components/SettingsPanel.tsx | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/web build exits 0
  </acceptance_criteria>
  <action>
  Implement D-07–D-10: read-only dimensions field; track `savedEmbeddingModel`; show warning when form embedding model differs; clear baseline on successful model save. Optional `.banner-warning` style (amber) distinct from error.
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>CONF-08 UX complete</done>
</task>

</tasks>

<verification>
  <automated>pnpm --filter @kb/web build 2>&1</automated>
</verification>

<success_criteria>
- Users see dimensions as read-only and are warned before changing embedding model
</success_criteria>

<output>
After completion, create `.planning/phases/20-web-model-settings-ui/20-02-SUMMARY.md`
</output>
