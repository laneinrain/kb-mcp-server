---
phase: 17-scoped-mcp-tools
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - apps/mcp-server/src/server.test.ts
  - apps/mcp-server/src/auth/mcp-request-context.ts
autonomous: true
requirements:
  - PLAT-07
  - PLAT-08
  - PLAT-04
must_haves:
  truths:
    - "authMode=service leaves allowedDocumentIds undefined on search/read (D-10)"
    - "authMode=global leaves allowedDocumentIds undefined (D-10)"
    - "authMode=user with empty Set still passes empty Set (D-11)"
  artifacts:
    - path: "apps/mcp-server/src/server.test.ts"
      provides: "service/global/empty-set ACL tests"
      contains: "authMode"
  key_links:
    - from: "apps/mcp-server/src/server.test.ts"
      to: "apps/mcp-server/src/auth/mcp-request-context.ts"
      via: "enterMcpCallerContext"
      pattern: "enterMcpCallerContext"
---

<objective>
Verify **service-account global bypass** and **`USER_AUTH_ENABLED=false` / global** paths do not apply ACL; document empty-set user behavior (PLAT-07, PLAT-08, PLAT-04).

Depends on 17-01 and 17-02.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-scoped-mcp-tools/17-CONTEXT.md
@apps/mcp-server/src/server.test.ts
@apps/mcp-server/src/auth/mcp-auth-resolver.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Service mode accidentally scoped | High | Explicit test: service → undefined ACL |
| Empty Set treated as global | Medium | Test user + empty Set passes empty Set to search |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Service / global / empty-set tool tests</name>
  <read_first>
    - apps/mcp-server/src/server.test.ts
    - apps/mcp-server/src/auth/mcp-request-context.ts
  </read_first>
  <files>
    apps/mcp-server/src/server.test.ts
  </files>
  <acceptance_criteria>
    - grep -c 'authMode: "service"' apps/mcp-server/src/server.test.ts | awk '{exit ($1>=1)?0:1}'
    - grep -c 'authMode: "global"' apps/mcp-server/src/server.test.ts | awk '{exit ($1>=1)?0:1}'
    - pnpm --filter @kb/mcp-server test -- server exits 0
  </acceptance_criteria>
  <action>
  Extend **`server.test.ts`** (InMemory client pattern):

  1. `enterMcpCallerContext({ authMode: "service" })` → `search` called **without** `allowedDocumentIds` (or with `undefined`)
  2. `enterMcpCallerContext({ authMode: "global" })` → same
  3. `enterMcpCallerContext({ authMode: "user", allowedDocumentIds: new Set() })` → search called with empty Set (not undefined)

  Optionally add a tiny unit test file for `getToolAllowedDocumentIds` if clearer than only server integration.

  No production code changes expected unless a bug is found — then fix in place.
  </action>
  <verify>
    <automated>pnpm --filter @kb/mcp-server test 2>&1</automated>
  </verify>
  <done>Service/global bypass and empty-set semantics verified</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @kb/mcp-server test`
2. `pnpm --filter @kb/core test -- context-service`
</verification>

## PLAN COMPLETE
