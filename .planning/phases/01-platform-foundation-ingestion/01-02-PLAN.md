---
phase: 01-platform-foundation-ingestion
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - packages/core/package.json
  - packages/core/src/index.ts
  - packages/core/src/registry/schema.sql
  - packages/core/src/registry/document-registry.ts
  - packages/core/src/registry/settings-store.ts
  - packages/core/src/registry/types.ts
  - packages/core/src/vector-store/chroma-store.ts
  - packages/core/src/embeddings/embedding-client.ts
  - packages/core/src/ingestion/chunker.ts
  - packages/core/src/ingestion/types.ts
  - packages/core/src/registry/settings-store.test.ts
  - packages/core/src/ingestion/chunker.test.ts
  - packages/core/src/vector-store/chroma-store.test.ts
  - packages/core/src/embeddings/embedding-client.test.ts
autonomous: true
requirements:
  - INGE-05
  - INGE-06
  - INGE-07
  - INGE-09
user_setup:
  - service: cherryin
    why: "Live embedding smoke tests and EmbeddingClient.ping()"
    env_vars:
      - name: CHERRYIN_API_KEY
        source: "CherryIn dashboard"
must_haves:
  truths:
    - "ChunkConfig reads persisted SQLite settings with env bootstrap defaults (1024 tokens, 154 overlap)"
    - "EmbeddingClient embeds document text via CherryIn qwen/qwen3-embedding-8b"
    - "ChromaVectorStore upserts precomputed embeddings and survives sidecar restart"
    - "Default collection 'default' is used; optional collection param accepted in store API"
  artifacts:
    - path: "packages/core/src/registry/settings-store.ts"
      provides: "ChunkConfig persistence per D-06/D-07"
      contains: "getChunkConfig"
    - path: "packages/core/src/embeddings/embedding-client.ts"
      provides: "CherryIn embedDocuments + embedQuery + ping"
      contains: "embedDocuments"
    - path: "packages/core/src/vector-store/chroma-store.ts"
      provides: "Chroma HTTP adapter with explicit embeddings"
      contains: "upsertChunks"
    - path: "packages/core/src/ingestion/chunker.ts"
      provides: "TokenTextSplitter wrapper reading ChunkConfig"
      contains: "TokenTextSplitter"
  key_links:
    - from: "packages/core/src/ingestion/chunker.ts"
      to: "settings-store.getChunkConfig"
      via: "runtime chunk size/overlap"
      pattern: "getChunkConfig|chunkSize"
    - from: "packages/core/src/vector-store/chroma-store.ts"
      to: "ChromaClient"
      via: "HTTP upsert with embeddings array"
      pattern: "upsert|embeddings"
    - from: "packages/core/src/embeddings/embedding-client.ts"
      to: "open.cherryin.cc"
      via: "openai SDK baseURL"
      pattern: "embeddings\\.create"
---

<objective>
Implement core storage and embedding services: SQLite document registry + ChunkConfig, CherryIn embedding client, Chroma vector store adapter, and token-based chunker.

Purpose: IngestionService (Plan 03) orchestrates against these contracts — no inline Chroma or embed logic in parsers.
Output: Tested `@kb/core` modules for registry, settings, embeddings, vector store, chunker.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-platform-foundation-ingestion/01-CONTEXT.md
@.planning/phases/01-platform-foundation-ingestion/01-RESEARCH.md
@packages/config/src/env.ts

<interfaces>
From @kb/config (Plan 01):
```typescript
export function loadConfig(): AppConfig;
export const DEFAULT_COLLECTION: string;
export const EMBEDDING_MODEL: string;
export const EMBEDDING_DIMENSIONS: number;
```

ChunkConfig shape (implement in registry/types.ts):
```typescript
export interface ChunkConfig {
  chunkSize: number;      // default 1024 (D-01)
  chunkOverlap: number;   // default 154 (D-02, 15% of 1024)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQLite registry and ChunkConfig store</name>
  <read_first>
    - .planning/phases/01-platform-foundation-ingestion/01-CONTEXT.md (D-04, D-05, D-06, D-07)
    - .planning/phases/01-platform-foundation-ingestion/01-RESEARCH.md (Pattern 4, Architectural Responsibility Map)
    - packages/config/src/env.ts
  </read_first>
  <files>
    packages/core/src/registry/schema.sql
    packages/core/src/registry/types.ts
    packages/core/src/registry/settings-store.ts
    packages/core/src/registry/document-registry.ts
    packages/core/src/registry/settings-store.test.ts
    packages/core/package.json
  </files>
  <behavior>
    - Test 1: First boot with empty DB seeds settings row from env CHUNK_SIZE=1024, CHUNK_OVERLAP=154
    - Test 2: getChunkConfig() returns persisted values after seed
    - Test 3: DocumentRegistry upserts document metadata and tracks chunk IDs
    - Test 4: DocumentRegistry deleteByDocumentId removes metadata row
  </behavior>
  <acceptance_criteria>
    - grep -c 'getChunkConfig' packages/core/src/registry/settings-store.ts returns at least 1
    - grep -c 'chunk_size' packages/core/src/registry/schema.sql returns at least 1
    - grep -c 'documents' packages/core/src/registry/schema.sql returns at least 1
    - grep -c 'better-sqlite3' packages/core/package.json returns at least 1
    - grep -c '1024' packages/core/src/registry/settings-store.test.ts returns at least 1
  </acceptance_criteria>
  <action>
    Add deps to `@kb/core`: `better-sqlite3`, `@types/better-sqlite3` (dev).

    **`schema.sql`**: tables `settings` (id=1, chunk_size, chunk_overlap), `documents` (id UUID, filename, source_path, mime_type, status, chunk_count, collection, created_at, updated_at), `document_chunks` (document_id, chunk_index, chroma_id).

    **`settings-store.ts`** per D-06/D-07:
    - On init: create tables if missing; if no settings row, INSERT from `loadConfig()` env defaults (1024, 154)
    - Export `getChunkConfig(): ChunkConfig`, `getSettingsStore(db)` factory
    - Ensure parent dir for SQLITE_PATH exists (mkdir -p)

    **`document-registry.ts`**:
    - `registerDocument(meta)`, `updateStatus(id, status, chunkCount?)`, `getDocument(id)`, `listDocuments()`, `deleteDocument(id)`
    - `trackChunkIds(documentId, chromaIds[])`, `getChunkIds(documentId)`

    Write tests first (TDD), then implement. Use temp SQLite file in tests.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test -- settings-store 2>&1</automated>
  </verify>
  <done>ChunkConfig persisted in SQLite with env bootstrap; DocumentRegistry CRUD works in unit tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EmbeddingClient for CherryIn</name>
  <read_first>
    - .planning/phases/01-platform-foundation-ingestion/01-RESEARCH.md (Pattern 5, CherryIn batch, Qwen3 prefix)
    - packages/config/src/constants.ts
  </read_first>
  <files>
    packages/core/src/embeddings/embedding-client.ts
    packages/core/src/embeddings/embedding-client.test.ts
  </files>
  <behavior>
    - Test 1: embedDocuments([]) returns []
    - Test 2: embedDocuments batches inputs (mock client — max 64 per call)
    - Test 3: embedQuery prepends Qwen3 instruction prefix (unit test string format)
    - Test 4: ping() calls embedDocuments with single short string (mock)
  </behavior>
  <acceptance_criteria>
    - grep -c 'embedDocuments' packages/core/src/embeddings/embedding-client.ts returns at least 1
    - grep -c 'embedQuery' packages/core/src/embeddings/embedding-client.ts returns at least 1
    - grep -c 'qwen/qwen3-embedding-8b' packages/core/src/embeddings/embedding-client.ts returns at least 1
    - grep -c 'Instruct:' packages/core/src/embeddings/embedding-client.ts returns at least 1
    - grep -c 'dimensions' packages/core/src/embeddings/embedding-client.ts returns at least 1
    - grep -c 'openai' packages/core/package.json returns at least 1
  </acceptance_criteria>
  <action>
    Add `openai` dep to `@kb/core`.

    **`embedding-client.ts`** per INGE-06 and RESEARCH Pattern 5:
    - Construct OpenAI client with `apiKey: config.CHERRYIN_API_KEY`, `baseURL: config.CHERRYIN_BASE_URL`
    - `embedDocuments(texts: string[])`: raw text, model `qwen/qwen3-embedding-8b`, `dimensions: 1024`, batch ≤64 with retry on 429 (exponential backoff, max 3)
    - `embedQuery(text: string)`: prepend `Instruct: Given a user question, retrieve relevant passages from the knowledge base\nQuery:${text}`
    - `ping()`: embedDocuments(['health-check']) — used by API-05 in Plan 03
    - Never log API keys or full request bodies

    Unit tests with mocked OpenAI client. Optional integration test skipped when `CHERRYIN_API_KEY` unset.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test -- embedding-client 2>&1</automated>
  </verify>
  <done>EmbeddingClient passes unit tests; embedDocuments uses CherryIn model with 1024 dimensions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ChromaVectorStore and token chunker</name>
  <read_first>
    - .planning/phases/01-platform-foundation-ingestion/01-RESEARCH.md (Pattern 1, 2, 3; Chroma client init)
    - .planning/phases/01-platform-foundation-ingestion/01-CONTEXT.md (D-01, D-03)
  </read_first>
  <files>
    packages/core/src/vector-store/chroma-store.ts
    packages/core/src/ingestion/chunker.ts
    packages/core/src/ingestion/types.ts
    packages/core/src/ingestion/chunker.test.ts
    packages/core/src/index.ts
    packages/core/package.json
  </files>
  <behavior>
    - Test 1: chunkText splits long input into multiple chunks with overlap from getChunkConfig
    - Test 2: chunkText uses TokenTextSplitter not RecursiveCharacterTextSplitter
    - Test 3: ChromaVectorStore.buildChunkId returns `{documentId}:{chunkIndex}` format
    - Test 4: deleteByDocumentId calls collection.delete with document_id filter (mock)
  </behavior>
  <acceptance_criteria>
    - grep -c 'TokenTextSplitter' packages/core/src/ingestion/chunker.ts returns at least 1
    - grep -c 'RecursiveCharacterTextSplitter' packages/core/src/ingestion/chunker.ts returns 0
    - grep -c 'deleteByDocumentId' packages/core/src/vector-store/chroma-store.ts returns at least 1
    - grep -c 'upsertChunks' packages/core/src/vector-store/chroma-store.ts returns at least 1
    - grep -c 'DEFAULT_COLLECTION' packages/core/src/vector-store/chroma-store.ts returns at least 1
    - grep -c 'collection\\?' packages/core/src/vector-store/chroma-store.ts returns at least 1 OR grep -c 'collection?:' packages/core/src/vector-store/chroma-store.ts returns at least 1
    - grep -c 'chromadb' packages/core/package.json returns at least 1
    - grep -c '@langchain/textsplitters' packages/core/package.json returns at least 1
  </acceptance_criteria>
  <action>
    Add deps: `chromadb`, `@langchain/textsplitters`.

    **`chunker.ts`** per INGE-05, D-01/D-03:
    - `chunkText(text: string, config?: ChunkConfig)`: use `TokenTextSplitter` with `encodingName: "cl100k_base"`, read sizes from arg or `getChunkConfig()`
    - Same sliding window for txt/md/pdf (D-03)

    **`chroma-store.ts`** per INGE-07, INGE-09:
    - Init `ChromaClient` from config host/port; `heartbeat()` on connect
    - `getOrCreateCollection(name?)` — default `DEFAULT_COLLECTION`; accept optional `collection` param (INGE-09)
    - Collection metadata: `embedding_model`, `embedding_dim: 1024`, `hnsw:space: cosine`
    - `upsertChunks({ documentId, filename, chunks, embeddings, collection? })` — ids `{documentId}:{index}`, metadatas include document_id, filename, chunk_index
    - `deleteByDocumentId(documentId, collection?)` — `collection.delete({ where: { document_id } })` per INGE-08 prep
    - All writes require explicit `embeddings` array (no default embed fn)

    Export all public APIs from `packages/core/src/index.ts`.

    Chunker unit tests required. **`chroma-store.test.ts`** must cover Behavior Tests 3–4 with mocked collection (buildChunkId format, deleteByDocumentId filter). Add `@vitest` skip integration test comment for live Chroma (Plan 03 verifies restart).
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test -- chunker chroma-store 2>&1</automated>
  </verify>
  <done>TokenTextSplitter chunker and ChromaVectorStore with delete/upsert contracts exported from @kb/core</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core → CherryIn API | Outbound HTTPS with API key |
| core → Chroma HTTP | Localhost sidecar |
| core → SQLite file | Local filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | I | embedding-client logs | mitigate | Never log keys or embed request bodies |
| T-01-04 | T | Chroma upsert without embeddings | mitigate | Type wrapper requires embeddings on upsert |
| T-01-05 | R | Partial embed batch failure | mitigate | Retry 429; fail entire batch before registry commit (Plan 03) |
</threat_model>

<verification>
- All @kb/core unit tests pass
- getChunkConfig returns 1024/154 on fresh DB with default env
- embedQuery output includes Instruct prefix (unit test)
</verification>

<success_criteria>
- INGE-05: configurable chunk size/overlap via SQLite + env bootstrap
- INGE-06: CherryIn embedding client with batching and retry
- INGE-07: Chroma adapter with explicit embeddings and heartbeat
- INGE-09: default collection with optional collection parameter in API
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-ingestion/01-02-SUMMARY.md`
</output>
