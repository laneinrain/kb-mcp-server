---
phase: 01-platform-foundation-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - .gitignore
  - .env.example
  - .nvmrc
  - packages/config/package.json
  - packages/config/tsconfig.json
  - packages/config/src/env.ts
  - packages/config/src/constants.ts
  - packages/config/src/index.ts
  - packages/core/package.json
  - packages/core/tsconfig.json
  - packages/core/src/index.ts
  - scripts/wait-for-chroma.ts
  - vitest.config.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
user_setup:
  - service: pnpm
    why: "Monorepo workspace manager required before any package install"
    env_vars: []
    dashboard_config: []
  - service: cherryin
    why: "Embedding API key needed in later plans; document now in .env.example"
    env_vars:
      - name: CHERRYIN_API_KEY
        source: "CherryIn dashboard — API key for open.cherryin.cc"
must_haves:
  truths:
    - "Operator can copy .env.example to .env and all required vars are documented"
    - "Invalid or missing env vars fail fast at loadConfig() with clear Zod errors"
    - "No secrets are committed — .env and data/ are gitignored"
    - "pnpm install succeeds and turbo can resolve @kb/config and @kb/core workspaces"
  artifacts:
    - path: "packages/config/src/env.ts"
      provides: "Zod-validated loadConfig() for all services"
      contains: "loadConfig"
    - path: ".env.example"
      provides: "Documented env template without secrets"
      contains: "CHERRYIN_API_KEY"
    - path: "pnpm-workspace.yaml"
      provides: "Monorepo workspace roots"
      contains: "packages/*"
    - path: "scripts/wait-for-chroma.ts"
      provides: "Chroma sidecar readiness probe for pnpm dev"
      contains: "heartbeat"
  key_links:
    - from: "packages/config/src/env.ts"
      to: "process.env"
      via: "Zod parse after dotenv in dev"
      pattern: "dotenv|process\\.env"
    - from: "scripts/wait-for-chroma.ts"
      to: "ChromaClient.heartbeat"
      via: "poll until success or timeout"
      pattern: "heartbeat"
---

<objective>
Scaffold the TypeScript monorepo and shared configuration layer so all downstream packages read secrets and service URLs from validated environment variables — never from committed files.

Purpose: Greenfield foundation; every later plan imports `@kb/config` instead of ad-hoc `process.env` reads.
Output: pnpm + Turborepo workspace, `@kb/config` with Zod schema, `.env.example`, Chroma wait script, `@kb/core` package skeleton.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-platform-foundation-ingestion/01-CONTEXT.md
@.planning/phases/01-platform-foundation-ingestion/01-RESEARCH.md

## Locked Decisions (implement exactly)
- **D-06/D-07:** Env vars `CHUNK_SIZE`, `CHUNK_OVERLAP` are bootstrap defaults (1024 / 154 per D-01/D-02) — schema must include them; SQLite seeding happens in Plan 02
- **CONF-01:** All secrets via env / `.env`; never import secrets in committed source
- **CONF-02:** `.env.example` documents all required vars with placeholder values only

## Package naming
- `@kb/config` — packages/config
- `@kb/core` — packages/core (skeleton only this plan)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monorepo root scaffold</name>
  <read_first>
    - .planning/phases/01-platform-foundation-ingestion/01-RESEARCH.md (Recommended Project Structure, Standard Stack)
    - .planning/PROJECT.md (Constraints)
  </read_first>
  <files>
    package.json
    pnpm-workspace.yaml
    turbo.json
    tsconfig.base.json
    .gitignore
    .nvmrc
    vitest.config.ts
    packages/core/package.json
    packages/core/tsconfig.json
    packages/core/src/index.ts
  </files>
  <acceptance_criteria>
    - grep -E '"packages/\*"|"apps/\*"' pnpm-workspace.yaml returns at least 1 match
    - grep -c '"@kb/core"' packages/core/package.json | grep -v '^#' | grep -qv '^0$' OR grep -c '"name".*"@kb/core"' packages/core/package.json returns 1
    - grep -c '\.env' .gitignore | grep -v '^#' | grep -qv '^0$'
    - grep -c 'data/' .gitignore | grep -v '^#' | grep -qv '^0$'
    - grep -c '"turbo"' package.json returns 1
    - grep -c '"vitest"' package.json returns 1
  </acceptance_criteria>
  <action>
    Create greenfield pnpm + Turborepo monorepo per RESEARCH.md layout:

    1. Root `package.json`: `"private": true`, `"packageManager": "pnpm@11.x"`, `"engines": { "node": ">=24 <25" }`, devDeps `turbo`, `typescript`, `tsx`, `vitest`, `concurrently`, `@types/node`, scripts stubs `"build": "turbo run build"`, `"test": "turbo run test"`, `"dev": "echo 'wired in plan 03'"`.

    2. `pnpm-workspace.yaml`: `packages/*` and `apps/*`.

    3. `turbo.json`: pipeline with `build` (`dependsOn: ["^build"]`, outputs `dist/**`), `test` (`dependsOn: ["^build"]`), `dev` (`cache: false`, `persistent: true`).

    4. `tsconfig.base.json`: strict ES2022, `"module": "NodeNext"`, `"moduleResolution": "NodeNext"`, `"declaration": true`.

    5. `.gitignore`: `node_modules/`, `.env`, `.env.local`, `data/`, `dist/`, `.turbo/`, `*.log`.

    6. `.nvmrc`: `24`.

    7. `packages/core`: minimal package `"name": "@kb/core"`, `"type": "module"`, exports `"."` → `./src/index.ts` (dev) / `./dist/index.js` (build), empty `src/index.ts` with `export {}`.

    8. `vitest.config.ts` at root with workspace/projects pointing at packages.

    Do NOT add apps/backend yet (Plan 03). Do NOT commit `.env`.
  </action>
  <verify>
    <automated>pnpm install && pnpm exec turbo run build --filter=@kb/core 2>&1 | head -20</automated>
  </verify>
  <done>pnpm install succeeds; turbo resolves @kb/core workspace; .gitignore excludes .env and data/</done>
</task>

<task type="auto">
  <name>Task 2: Zod env schema and .env.example</name>
  <read_first>
    - .planning/phases/01-platform-foundation-ingestion/01-CONTEXT.md (D-01, D-02, D-06, D-07)
    - .planning/phases/01-platform-foundation-ingestion/01-RESEARCH.md (phase_requirements CONF-01/02, Pattern 4)
    - packages/config/ (create fresh)
  </read_first>
  <files>
    packages/config/package.json
    packages/config/tsconfig.json
    packages/config/src/env.ts
    packages/config/src/constants.ts
    packages/config/src/index.ts
    .env.example
  </files>
  <acceptance_criteria>
    - grep -c 'loadConfig' packages/config/src/env.ts returns at least 1
    - grep -c 'CHERRYIN_API_KEY' .env.example returns at least 1
    - grep -c 'CHUNK_SIZE' .env.example returns at least 1
    - grep -c 'CHUNK_OVERLAP' .env.example returns at least 1
    - grep -c 'DEFAULT_COLLECTION' packages/config/src/constants.ts returns at least 1
    - grep -c '1024' packages/config/src/env.ts returns at least 1
    - grep -c 'z\\.object' packages/config/src/env.ts returns at least 1
    - test -f .env.example && ! grep -E 'sk-[a-zA-Z0-9]{20,}' .env.example
  </acceptance_criteria>
  <action>
    Create `@kb/config` package per CONF-01/CONF-02 and RESEARCH env template:

    **`packages/config/src/env.ts`** — Zod schema + `loadConfig()`:
    - `CHERRYIN_API_KEY` (string, min 1)
    - `CHERRYIN_BASE_URL` (url, default `https://open.cherryin.cc`)
    - `CHROMA_HOST` (default `localhost`)
    - `CHROMA_PORT` (coerce number, default 8000)
    - `CHUNK_SIZE` (coerce number, default **1024** per D-01)
    - `CHUNK_OVERLAP` (coerce number, default **154** = 15% of 1024 per D-02)
    - `SQLITE_PATH` (default `./data/sqlite/registry.db`)
    - `DATA_DIR` (default `./data`)
    - `BACKEND_HOST` (default `127.0.0.1` — CONF-04 prep)
    - `BACKEND_PORT` (coerce number, default 3000)
    - `DEFAULT_COLLECTION` (default `default`)
    - `EMBEDDING_MODEL` (default `qwen/qwen3-embedding-8b`)
    - `EMBEDDING_DIMENSIONS` (coerce number, default 1024)

    Call `dotenv.config()` at top when `NODE_ENV !== 'production'`. On parse failure, log Zod issues and `process.exit(1)`.

    **`packages/config/src/constants.ts`** — export `DEFAULT_COLLECTION = "default"`, `EMBEDDING_MODEL`, `EMBEDDING_DIMENSIONS`.

    **`.env.example`** — all vars above with placeholders (`CHERRYIN_API_KEY=your_key_here`), comments explaining each. No real secrets.

    Export `loadConfig`, `AppConfig` type, constants from `index.ts`.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/config 2>&1 || pnpm --filter @kb/config test 2>&1 || node -e "import('./packages/config/src/env.ts').then(m=>{try{m.loadConfig()}catch(e){console.log('expected without .env')}})" 2>&1 | head -5</automated>
  </verify>
  <done>loadConfig() validates env with typed output; .env.example documents every required var without secrets</done>
</task>

<task type="auto">
  <name>Task 3: Chroma readiness probe script</name>
  <read_first>
    - .planning/phases/01-platform-foundation-ingestion/01-RESEARCH.md (Dev Orchestration, Chroma Client)
    - packages/config/src/env.ts
  </read_first>
  <files>
    scripts/wait-for-chroma.ts
    package.json
  </files>
  <acceptance_criteria>
    - grep -c 'heartbeat' scripts/wait-for-chroma.ts returns at least 1
    - grep -c 'ChromaClient' scripts/wait-for-chroma.ts returns at least 1
    - grep -c 'wait-for-chroma' package.json returns at least 1
    - grep -c '@kb/config' scripts/wait-for-chroma.ts returns at least 1
  </acceptance_criteria>
  <action>
    Add `chromadb` as root devDependency (or `@kb/core` dep in plan 02 — for now add to root devDeps for wait script only).

    Create **`scripts/wait-for-chroma.ts`**:
    - Import `loadConfig` from `@kb/config` and `ChromaClient` from `chromadb`
    - Poll `client.heartbeat()` every 500ms for up to 60s
    - On success: log `Chroma ready` to stderr, exit 0
    - On timeout: log error to stderr, exit 1

    Add root script `"wait:chroma": "tsx scripts/wait-for-chroma.ts"`.

    This script is consumed by `pnpm dev` in Plan 03 — do not wire full dev orchestration here.
  </action>
  <verify>
    <automated>pnpm exec tsx --version 2>&1 | head -1</automated>
  </verify>
  <done>wait-for-chroma.ts exists, uses loadConfig + ChromaClient.heartbeat, registered in package.json scripts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env file → loadConfig | Untrusted local file; must never be committed |
| dev scripts → Chroma HTTP | Localhost-only sidecar probe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I | `.env` in git | mitigate | `.gitignore` `.env`; `.env.example` placeholders only |
| T-01-02 | I | loadConfig error messages | mitigate | Zod errors must not echo secret values |
| T-01-03 | D | wait-for-chroma infinite hang | mitigate | 60s timeout with exit 1 |
</threat_model>

<verification>
- `pnpm install` succeeds from clean clone
- Copy `.env.example` → `.env`, fill dummy values, `loadConfig()` returns typed config
- `.env` absent from `git status`
</verification>

<success_criteria>
- Monorepo installs and builds @kb/core skeleton
- CONF-01: secrets load from env only; CONF-02: .env.example complete
- wait-for-chroma script ready for Plan 03 dev orchestration
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-ingestion/01-01-SUMMARY.md`
</output>
