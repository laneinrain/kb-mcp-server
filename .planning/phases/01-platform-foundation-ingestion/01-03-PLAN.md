---
phase: 01-platform-foundation-ingestion
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - packages/core/src/ingestion/parsers/txt-parser.ts
  - packages/core/src/ingestion/parsers/md-parser.ts
  - packages/core/src/ingestion/parsers/pdf-parser.ts
  - packages/core/src/ingestion/parsers/index.ts
  - packages/core/src/ingestion/ingestion-service.ts
  - packages/core/src/ingestion/ingestion-service.test.ts
  - packages/core/src/ingestion/parsers/pdf-parser.test.ts
  - packages/core/src/index.ts
  - apps/backend/package.json
  - apps/backend/tsconfig.json
  - apps/backend/src/index.ts
  - apps/backend/src/routes/health.ts
  - scripts/ingest.ts
  - scripts/fixtures/sample.txt
  - scripts/fixtures/sample.md
  - package.json
  - turbo.json
autonomous: false
requirements:
  - INGE-01
  - INGE-02
  - INGE-03
  - INGE-04
  - INGE-08
  - API-05
  - CONF-04
user_setup:
  - service: cherryin
    why: "Required for live ingest and /health/embeddings"
    env_vars:
      - name: CHERRYIN_API_KEY
        source: "CherryIn dashboard"
must_haves:
  truths:
    - "Operator can ingest txt, markdown, and text-layer PDF via pnpm ingest script"
    - "Scanned/empty PDFs rejected with clear English error before embedding"
    - "Re-ingesting a file replaces vectors without orphan chunks in Chroma"
    - "Vectors persist after Chroma sidecar restart"
    - "GET /health, /health/chroma, /health/embeddings return ok when stack is up"
    - "Backend binds to 127.0.0.1 by default (CONF-04)"
  artifacts:
    - path: "packages/core/src/ingestion/ingestion-service.ts"
      provides: "Full parse → chunk → embed → store orchestration"
      contains: "ingest"
    - path: "scripts/ingest.ts"
      provides: "Dev CLI entrypoint for operator ingest"
      contains: "IngestionService"
    - path: "apps/backend/src/routes/health.ts"
      provides: "API-05 health endpoints"
      contains: "/health/chroma"
    - path: "packages/core/src/ingestion/parsers/pdf-parser.ts"
      provides: "PDF text extraction with rejection threshold"
      contains: "insufficient text"
  key_links:
    - from: "scripts/ingest.ts"
      to: "IngestionService.ingest"
      via: "CLI file path argument"
      pattern: "ingest\\("
    - from: "IngestionService"
      to: "ChromaVectorStore.deleteByDocumentId"
      via: "re-ingest path before upsert"
      pattern: "deleteByDocumentId"
    - from: "apps/backend/src/routes/health.ts"
      to: "EmbeddingClient.ping"
      via: "GET /health/embeddings"
      pattern: "ping\\(\\)|embeddings"
---

<objective>
Deliver the complete ingestion pipeline (txt/md/PDF parsers, IngestionService with re-ingest semantics), dev CLI, health-only Fastify backend, and pnpm dev orchestration — proving end-to-end ingest into persistent Chroma.

Purpose: Phase 1 exit criteria — operator can run local stack, ingest documents, and verify health without REST CRUD (Phase 2).
Output: Working `pnpm dev`, `pnpm ingest <file>`, health endpoints, integration-verified persistence.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-platform-foundation-ingestion/01-CONTEXT.md
@.planning/phases/01-platform-foundation-ingestion/01-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
From @kb/core (Plan 02 exports):
```typescript
// settings-store
export function getChunkConfig(): ChunkConfig;

// embedding-client
export class EmbeddingClient {
  embedDocuments(texts: string[]): Promise<number[][]>;
  ping(): Promise<void>;
}

// chroma-store
export class ChromaVectorStore {
  upsertChunks(params: UpsertParams): Promise<string[]>;
  deleteByDocumentId(documentId: string, collection?: string): Promise<void>;
  heartbeat(): Promise<void>;
}

// chunker
export function chunkText(text: string, config?: ChunkConfig): Promise<string[]>;

// document-registry
export class DocumentRegistry { /* register, updateStatus, delete, trackChunkIds */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Document parsers (txt, md, pdf)</name>
  <read_first>
    - .planning/phases/01-platform-foundation-ingestion/01-CONTEXT.md (PDF threshold discretion)
    - .planning/phases/01-platform-foundation-ingestion/01-RESEARCH.md (PDF validation, gray-matter, pdf-parse)
    - packages/core/src/ingestion/chunker.ts
  </read_first>
  <files>
    packages/core/src/ingestion/parsers/txt-parser.ts
    packages/core/src/ingestion/parsers/md-parser.ts
    packages/core/src/ingestion/parsers/pdf-parser.ts
    packages/core/src/ingestion/parsers/index.ts
    packages/core/src/ingestion/parsers/pdf-parser.test.ts
    scripts/fixtures/sample.txt
    scripts/fixtures/sample.md
    packages/core/package.json
  </files>
  <behavior>
    - Test 1: parseTxt returns file contents as string
    - Test 2: parseMd strips YAML frontmatter via gray-matter
    - Test 3: parsePdf rejects text with length below max(50, ceil(pageCount * 0.1)) threshold
    - Test 4: parsePdf accepts text above threshold (mock pdf-parse)
    - Test 5: Unsupported extension throws clear error
  </behavior>
  <acceptance_criteria>
    - grep -c 'parseTxt' packages/core/src/ingestion/parsers/txt-parser.ts returns at least 1
    - grep -c 'gray-matter' packages/core/src/ingestion/parsers/md-parser.ts returns at least 1
    - grep -c 'pdf-parse' packages/core/src/ingestion/parsers/pdf-parser.ts returns at least 1
    - grep -c 'insufficient text' packages/core/src/ingestion/parsers/pdf-parser.ts returns at least 1 OR grep -ci 'scanned PDF' packages/core/src/ingestion/parsers/pdf-parser.ts returns at least 1
    - grep -c '50' packages/core/src/ingestion/parsers/pdf-parser.ts returns at least 1
    - grep -c 'pdf-parser.test' packages/core/src/ingestion/parsers/pdf-parser.test.ts returns at least 1
    - grep -c 'gray-matter' packages/core/package.json returns at least 1
    - grep -c 'pdf-parse' packages/core/package.json returns at least 1
  </acceptance_criteria>
  <action>
    Add deps: `gray-matter`, `pdf-parse`.

    **`parsers/index.ts`**: export `parseDocument(filePath: string): Promise<{ text: string; mimeType: string; filename: string }>` — route by extension `.txt`, `.md`, `.markdown`, `.pdf`.

    **`txt-parser.ts`** (INGE-01): read UTF-8 file.

    **`md-parser.ts`** (INGE-02): read file, `gray-matter` strip frontmatter, return content only (D-03: no heading-aware chunking).

    **`pdf-parser.ts`** (INGE-03, INGE-04):
    - Use `pdf-parse` for text-layer extraction
    - Validate per CONTEXT: reject if `trimmed.length < max(50, Math.ceil(pageCount * 0.1))`
    - Error message: `"No sufficient text layer detected — scanned PDFs are not supported in v1."`
    - Wrap parser in try/catch; enforce max file size 50MB at parse entry

    Add fixture files `scripts/fixtures/sample.txt` and `sample.md` for manual ingest testing.

    Allowlist extensions only — reject others with English error.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test -- pdf-parser 2>&1</automated>
  </verify>
  <done>Parsers handle txt/md/pdf; PDF rejection threshold enforced with tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IngestionService with re-ingest semantics</name>
  <read_first>
    - .planning/phases/01-platform-foundation-ingestion/01-RESEARCH.md (Pattern 2 delete-before-upsert, Pitfall 5)
    - packages/core/src/vector-store/chroma-store.ts
    - packages/core/src/registry/document-registry.ts
  </read_first>
  <files>
    packages/core/src/ingestion/ingestion-service.ts
    packages/core/src/ingestion/ingestion-service.test.ts
    packages/core/src/index.ts
  </files>
  <behavior>
    - Test 1: ingest() calls parseDocument → chunkText → embedDocuments → upsertChunks
    - Test 2: re-ingest same path calls deleteByDocumentId before upsert (mock verify call order)
    - Test 3: stable document_id derived from content hash or path (consistent across re-ingest)
    - Test 4: registry status transitions pending → complete only after full embed succeeds
    - Test 5: ingest(path, { collection?: string }) passes collection to ChromaVectorStore (INGE-09)
  </behavior>
  <acceptance_criteria>
    - grep -c 'class IngestionService' packages/core/src/ingestion/ingestion-service.ts returns at least 1
    - grep -c 'deleteByDocumentId' packages/core/src/ingestion/ingestion-service.ts returns at least 1
    - grep -c 'embedDocuments' packages/core/src/ingestion/ingestion-service.ts returns at least 1
    - grep -c 'collection\\?' packages/core/src/ingestion/ingestion-service.ts returns at least 1 OR grep -c 'collection?:' packages/core/src/ingestion/ingestion-service.ts returns at least 1
    - grep -c 'IngestionService' packages/core/src/index.ts returns at least 1
  </acceptance_criteria>
  <action>
    **`ingestion-service.ts`** orchestrates INGE-01–08:

    Constructor injects: DocumentRegistry, ChromaVectorStore, EmbeddingClient, optional SettingsStore.

    `ingest(filePath: string, options?: { collection?: string })`:
    1. Resolve absolute path; reject path traversal (must be under cwd or explicit allowed dir)
    2. Compute stable `document_id` (sha256 of normalized absolute path — same file re-ingest replaces)
    3. `parseDocument` → `chunkText` (reads ChunkConfig from settings per D-05: runtime config at ingest time)
    4. If document exists: `deleteByDocumentId(document_id, collection)` in Chroma (INGE-08)
    5. `embedDocuments(chunks)` — all chunks must succeed before any Chroma write (Pitfall 5)
    6. `upsertChunks` with stable ids `{document_id}:{chunk_index}`
    7. Update DocumentRegistry metadata and chunk ID tracking
    8. Return `{ documentId, chunkCount, collection }`

    Export from index.ts.

    Unit tests with mocked deps verifying delete-before-upsert ordering.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test -- ingestion-service 2>&1</automated>
  </verify>
  <done>IngestionService implements full pipeline with delete-then-upsert re-ingest per INGE-08</done>
</task>

<task type="auto">
  <name>Task 3: Health backend, ingest CLI, and dev orchestration</name>
  <read_first>
    - .planning/phases/01-platform-foundation-ingestion/01-RESEARCH.md (Health Endpoint Shape, Dev Orchestration, API-05)
    - .planning/REQUIREMENTS.md (API-05, CONF-04)
    - scripts/wait-for-chroma.ts
  </read_first>
  <files>
    apps/backend/package.json
    apps/backend/tsconfig.json
    apps/backend/src/index.ts
    apps/backend/src/routes/health.ts
    scripts/ingest.ts
    package.json
    turbo.json
  </files>
  <acceptance_criteria>
    - grep -c '/health/chroma' apps/backend/src/routes/health.ts returns at least 1
    - grep -c '/health/embeddings' apps/backend/src/routes/health.ts returns at least 1
    - grep -c '127.0.0.1' apps/backend/src/index.ts returns at least 1 OR grep -c 'BACKEND_HOST' apps/backend/src/index.ts returns at least 1
    - grep -c 'IngestionService' scripts/ingest.ts returns at least 1
    - grep -c 'concurrently' package.json returns at least 1
    - grep -c 'chroma run' package.json returns at least 1
    - grep -c '@kb/backend' apps/backend/package.json returns at least 1
  </acceptance_criteria>
  <action>
    **`apps/backend`** (API-05, CONF-04):
    - Package `@kb/backend`, deps: `fastify`, `@kb/core`, `@kb/config`
    - `index.ts`: `loadConfig()`, Fastify listen on `config.BACKEND_HOST` (default `127.0.0.1`) and `config.BACKEND_PORT` — CONF-04
    - **`routes/health.ts`**:
      - `GET /health` → `{ status: "ok" }`
      - `GET /health/chroma` → heartbeat via ChromaVectorStore
      - `GET /health/embeddings` → EmbeddingClient.ping(), return model name
    - No CRUD routes (Phase 2 scope)

    **`scripts/ingest.ts`**:
    - Usage: `tsx scripts/ingest.ts &lt;file-path&gt; [--collection name]`
    - Wire IngestionService with real deps; load dotenv via loadConfig
    - Print `{ documentId, chunkCount }` on success; exit 1 on error with message to stderr

    **Root `package.json` dev orchestration**:
    ```json
    "dev": "concurrently -k \"npx chroma run --path ./data/chroma\" \"pnpm wait:chroma && turbo run dev --filter=@kb/backend\"",
    "ingest": "tsx scripts/ingest.ts"
    ```
    Add `@kb/backend` dev script: `"dev": "tsx watch src/index.ts"`

    Update turbo.json if needed for backend dev task.

    **Persistence verification script** (inline in task or separate): document in SUMMARY steps to ingest fixture, restart Chroma, verify collection count unchanged (INGE-07 manual/automated checkpoint).
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend build 2>&1 || pnpm exec tsc -p apps/backend/tsconfig.json --noEmit 2>&1</automated>
  </verify>
  <done>Health backend on 127.0.0.1, ingest CLI wired, pnpm dev starts Chroma + backend</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: End-to-end stack verification</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 Success Criteria)
    - scripts/fixtures/sample.txt
    - apps/backend/src/routes/health.ts
  </read_first>
  <files>N/A — verification only</files>
  <acceptance_criteria>
    - curl http://127.0.0.1:3000/health returns status ok
    - curl http://127.0.0.1:3000/health/chroma returns status ok
    - curl http://127.0.0.1:3000/health/embeddings returns status ok with model name
    - pnpm ingest scripts/fixtures/sample.txt reports chunkCount greater than 0
    - Re-ingest same file succeeds without duplicate-vector error
    - Vectors persist after Chroma sidecar restart
  </acceptance_criteria>
  <action>
    Pause for human verification of the full Phase 1 stack after Tasks 1–3 complete. No code changes unless issues found — operator runs live ingest against CherryIn and Chroma.
  </action>
  <what-built>
    Full Phase 1 stack: pnpm dev (Chroma + health backend), pnpm ingest for txt/md/pdf, health endpoints, re-ingest and persistence behavior.
  </what-built>
  <how-to-verify>
    1. Copy `.env.example` → `.env`, set real `CHERRYIN_API_KEY`
    2. Run `pnpm dev` — wait for Chroma + backend ready
    3. `curl http://127.0.0.1:3000/health` → `{ "status": "ok" }`
    4. `curl http://127.0.0.1:3000/health/chroma` → ok
    5. `curl http://127.0.0.1:3000/health/embeddings` → ok with model name
    6. `pnpm ingest scripts/fixtures/sample.txt` → reports chunkCount &gt; 0
    7. Re-run same ingest command → succeeds (replaces, no duplicate error)
    8. Stop `pnpm dev`, restart — vectors from step 6 still present (INGE-07)
    9. Attempt ingest on scanned PDF (if available) → clear rejection error (INGE-04)
  </how-to-verify>
  <verify>
    <automated>echo "Human checkpoint — run steps in how-to-verify"</automated>
  </verify>
  <done>Operator confirms all Phase 1 success criteria pass live with CherryIn API key configured</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI file path → filesystem | Untrusted path input from operator |
| HTTP health → localhost | Must not bind 0.0.0.0 in dev (CONF-04) |
| PDF bytes → pdf-parse | Potentially malicious file content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | T | scripts/ingest.ts path args | mitigate | Resolve paths; reject traversal outside cwd |
| T-01-07 | E | pdf-parse on untrusted PDF | mitigate | 50MB cap; try/catch; allowlist .pdf only |
| T-01-08 | I | Health on 0.0.0.0 | mitigate | Bind Fastify to 127.0.0.1 via BACKEND_HOST default |
| T-01-09 | I | Health endpoints leak internals | accept | Localhost-only dev; minimal response bodies |
</threat_model>

<verification>
- All @kb/core tests pass: `pnpm test`
- Backend typecheck passes
- Human checkpoint confirms live CherryIn + Chroma ingest cycle
</verification>

<success_criteria>
- INGE-01/02/03: txt, md, PDF ingest via CLI
- INGE-04: empty/scanned PDF rejected
- INGE-08: re-ingest replaces without orphans
- INGE-07: vectors survive Chroma restart
- API-05: three health endpoints functional
- CONF-04: backend on 127.0.0.1
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-ingestion/01-03-SUMMARY.md`
</output>
