---
phase: 05-context-retrieval-core
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - packages/core/src/context/types.ts
  - packages/core/src/context/errors.ts
  - packages/core/src/context/context-service.ts
  - packages/core/src/context/context-service.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-02
must_haves:
  truths:
    - "readAround returns symmetric ±N window around chunk_index with full Chroma text (D-01, D-07)"
    - "Window above max is silently clamped; response includes windowRequested and windowApplied (D-03)"
    - "At document boundaries window shrinks; chunkRange reflects actual indices (D-04)"
    - "Chunks ordered by chunk_index ascending; center marked isCenter: true (D-05, D-06)"
    - "read_around char cap truncates from far end of window first; center preserved (D-08)"
    - "readFile returns all chunks in chunk_index order with chunk/file bounds from settings"
    - "Unknown document_id or out-of-range chunk_index throws ContextError with no partial chunks (CORE-02)"
    - "ContextService reads getContextConfig() on every call — no cached settings (D-13)"
  artifacts:
    - path: "packages/core/src/context/context-service.ts"
      provides: "readAround and readFile orchestration"
      contains: "readAround"
    - path: "packages/core/src/context/errors.ts"
      provides: "ContextError with document_not_found and chunk_index_out_of_range"
      contains: "ContextError"
    - path: "packages/core/src/context/types.ts"
      provides: "ReadAroundResult, ReadFileResult, ContextChunk interfaces"
      contains: "ReadAroundResult"
    - path: "packages/core/src/context/context-service.test.ts"
      provides: "Unit tests for bounds, clamp, shrink, truncation, errors"
      contains: "document_not_found"
  key_links:
    - from: "packages/core/src/context/context-service.ts"
      to: "packages/core/src/registry/document-registry.ts"
      via: "getDocument + getChunkIds before Chroma fetch"
      pattern: "getChunkIds"
    - from: "packages/core/src/context/context-service.ts"
      to: "packages/core/src/vector-store/chroma-store.ts"
      via: "getByIds with sliced chroma IDs"
      pattern: "getByIds"
    - from: "packages/core/src/context/context-service.ts"
      to: "packages/core/src/registry/settings-store.ts"
      via: "getContextConfig per call"
      pattern: "getContextConfig"
---

<objective>
Implement ContextService with readAround/readFile, structured ContextError, and bounded retrieval algorithms (D-01–D-08, CORE-01, CORE-02).

Purpose: Phase 6 MCP tools and future REST will consume this shared service; validation and bounds logic must be correct before any transport wiring.
Output: `context-service.ts`, `types.ts`, `errors.ts`, comprehensive unit tests with mocked registry/Chroma/settings.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-context-retrieval-core/05-CONTEXT.md
@.planning/phases/05-context-retrieval-core/05-RESEARCH.md
@.planning/phases/05-context-retrieval-core/05-PATTERNS.md
@packages/core/src/search/search-service.ts
@packages/core/src/registry/document-registry.ts
@.planning/phases/05-context-retrieval-core/05-01-SUMMARY.md

<interfaces>
From 05-01 (Chroma + Settings):
```typescript
export interface ChunkHit {
  documentId: string;
  filename: string;
  chunkIndex: number;
  text: string;
}
export interface GetByIdsParams {
  ids: string[];
  collection?: string;
}
async getByIds(params: GetByIdsParams): Promise<ChunkHit[]>;

export interface ContextConfig {
  readAroundWindowDefault: number;
  readAroundWindowMax: number;
  readAroundMaxChars: number;
  readFileMaxChunks: number;
  readFileMaxChars: number;
}
getContextConfig(): ContextConfig;
```

From `document-registry.ts`:
```typescript
export interface DocumentRegistry {
  getDocument(documentId: string): DocumentRecord | undefined;
  getChunkIds(documentId: string): string[];
}
export interface DocumentRecord {
  id: string;
  filename: string;
  collection: string;
  // ...
}
```

From `search-service.ts` factory pattern:
```typescript
static create(config: AppConfig, deps: { ... }): SearchService;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Context types and ContextError (CORE-02)</name>
  <read_first>
    - .planning/phases/05-context-retrieval-core/05-CONTEXT.md (Claude's Discretion — Errors)
    - .planning/phases/05-context-retrieval-core/05-RESEARCH.md (Pattern 5)
    - .planning/phases/05-context-retrieval-core/05-PATTERNS.md (types.ts, errors sections)
    - packages/core/src/search/types.ts
    - apps/backend/src/lib/errors.ts
  </read_first>
  <files>
    packages/core/src/context/types.ts
    packages/core/src/context/errors.ts
    packages/core/src/index.ts
  </files>
  <behavior>
    - Test 1: ContextError has code document_not_found and correct name
    - Test 2: ContextError has code chunk_index_out_of_range
    - Test 3: Types export ReadAroundResult with windowRequested, windowApplied, chunkRange fields
  </behavior>
  <acceptance_criteria>
    - grep -v '^#' packages/core/src/context/errors.ts | grep -c 'ContextError' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' packages/core/src/context/errors.ts | grep -c 'document_not_found' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' packages/core/src/context/errors.ts | grep -c 'chunk_index_out_of_range' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' packages/core/src/context/types.ts | grep -c 'ReadAroundResult' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' packages/core/src/context/types.ts | grep -c 'isCenter' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' packages/core/src/index.ts | grep -c 'ContextError\|ReadAroundResult' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Create **`packages/core/src/context/types.ts`** per RESEARCH Pattern 3:
    - `ContextChunk` — documentId, filename, chunkIndex, text, optional `isCenter`
    - `ReadAroundOptions` — optional `window`, `collection`
    - `ReadFileOptions` — optional `collection`
    - `ReadAroundResult` — documentId, filename, collection, chunkRange `{ start, end }`, windowRequested, windowApplied, optional truncated/totalChars, chunks[]
    - `ReadFileResult` — documentId, filename, collection, chunkCount, returnedChunks, optional truncated, chunks[]

    Create **`packages/core/src/context/errors.ts`** (CORE-02):
    - `ContextErrorCode = "document_not_found" | "chunk_index_out_of_range"`
    - `class ContextError extends Error` with `code` field and `name = "ContextError"`

    Export all types + `ContextError` from **`packages/core/src/index.ts`**.

    Add minimal **`errors.test.ts`** or test cases in context-service.test.ts for error construction.

    Match backend `{ error, message }` mapping intent — codes are stable for Phase 6 MCP JSON.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test -- context 2>&1</automated>
  </verify>
  <done>Context types and ContextError exported; error codes match CORE-02 contract</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContextService readAround/readFile with bounds and unit tests</name>
  <read_first>
    - .planning/phases/05-context-retrieval-core/05-CONTEXT.md (D-01 through D-08, D-13)
    - .planning/phases/05-context-retrieval-core/05-RESEARCH.md (Patterns 3, 4)
    - .planning/phases/05-context-retrieval-core/05-PATTERNS.md (context-service section)
    - packages/core/src/search/search-service.ts
    - packages/core/src/search/search-service.test.ts
  </read_first>
  <files>
    packages/core/src/context/context-service.ts
    packages/core/src/context/context-service.test.ts
    packages/core/src/index.ts
  </files>
  <behavior>
    - Test 1: readAround center=4 window=2 → fetches indices 2–6 (D-01)
    - Test 2: window=5 with max=3 → windowApplied=3, no error (D-03)
    - Test 3: center=0 window=2 → chunkRange shrinks to {0,2} (D-04)
    - Test 4: chunks sorted ascending; center has isCenter: true (D-05, D-06)
    - Test 5: char cap removes furthest-from-center first; center always kept (D-08)
    - Test 6: center-only exceeds cap → returns full center, truncated: true (RESEARCH open Q2)
    - Test 7: unknown document_id → ContextError document_not_found; getByIds NOT called (CORE-02)
    - Test 8: chunk_index out of range → ContextError; getByIds NOT called (CORE-02)
    - Test 9: readFile applies max chunks then max chars; truncates from tail
    - Test 10: collection resolves as options?.collection ?? doc.collection ?? defaultCollection
    - Test 11: getContextConfig called on each readAround/readFile (D-13)
    - Test 12: ContextService.create factory mirrors SearchService.create
  </behavior>
  <acceptance_criteria>
    - grep -v '^#' packages/core/src/context/context-service.ts | grep -c 'readAround' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' packages/core/src/context/context-service.ts | grep -c 'readFile' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' packages/core/src/context/context-service.ts | grep -c 'getContextConfig' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' packages/core/src/context/context-service.ts | grep -c 'getChunkIds' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' packages/core/src/context/context-service.ts | grep -c 'getByIds' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' packages/core/src/index.ts | grep -c 'ContextService' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    Create **`context-service.ts`** per PATTERNS + RESEARCH Pattern 4:

    **Class + factory** (mirror SearchService + IngestionService settings dep):
    ```typescript
    constructor(registry, vectorStore, settingsStore, defaultCollection)
    static create(config, { registry, vectorStore, settingsStore })
    ```

    **`readAround(documentId, chunkIndex, options?)`** — D-01–D-08, D-13:
    1. `settings = this.settingsStore.getContextConfig()` — **every call**, no cache (D-13)
    2. Validate: `getDocument` → missing throws `ContextError("document_not_found")` **before** Chroma (CORE-02)
    3. `chromaIds = getChunkIds(documentId)`; if `chunkIndex &lt; 0 || chunkIndex >= chromaIds.length` → `chunk_index_out_of_range` **before** Chroma (CORE-02)
    4. Collection: `options?.collection ?? doc.collection ?? defaultCollection`
    5. Window: `windowRequested = options?.window ?? settings.readAroundWindowDefault`; `windowApplied = Math.min(windowRequested, settings.readAroundWindowMax)` (D-02, D-03)
    6. Shrink: `start = max(0, center - windowApplied)`, `end = min(len-1, center + windowApplied)` (D-04)
    7. Fetch: `getByIds({ ids: chromaIds.slice(start, end+1), collection })` — full text, no 500-char truncate (D-07, CORE-01)
    8. Sort by chunk_index ascending; set `isCenter: true` on center chunk (D-05, D-06)
    9. Truncate: `truncateAroundCenter(chunks, chunkIndex, settings.readAroundMaxChars)` — remove furthest-from-center first; tie-break higher index; never remove center until alone (D-08)
    10. Return `ReadAroundResult` with windowRequested, windowApplied, chunkRange, truncated metadata

    **`readFile(documentId, options?)`**:
    1. Same validation (document exists; no chunk_index check)
    2. Live settings for `readFileMaxChunks`, `readFileMaxChars`
    3. Slice chromaIds to chunk cap, fetch, truncate chars from tail (highest indices)
    4. Return chunkCount (total in doc), returnedChunks, truncated flag

    Implement **`truncateAroundCenter`** and **`truncateFromEnd`** as private module functions or class privates per RESEARCH algorithms.

    **`context-service.test.ts`**: Vitest mocks for registry, vectorStore, settingsStore — follow `search-service.test.ts` pattern. Assert getByIds call order and error paths with `expect(getByIds).not.toHaveBeenCalled()` on validation failures.

    Export **`ContextService`** from `packages/core/src/index.ts`.

    **Do NOT** register MCP tools. **Do NOT** truncate chunk text to snippets. **Do NOT** return partial chunks on validation failure.
  </action>
  <verify>
    <automated>pnpm --filter @kb/core test -- context-service 2>&1</automated>
  </verify>
  <done>readAround/readFile satisfy D-01–D-08 and CORE-01/CORE-02; all context unit tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → ContextService | Untrusted documentId/chunkIndex/window; validate before storage fetch |
| ContextService → Chroma | Only fetches after registry validation; bounded ID slices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | I | Partial data leak on bad index | mitigate | Fail fast before getByIds; CORE-02 tests assert no fetch |
| T-05-05 | D | Unbounded read_file response | mitigate | readFileMaxChunks + readFileMaxChars from live settings |
| T-05-06 | T | Wrong collection fetch | mitigate | Resolve collection from doc.record then options then default |
</threat_model>

<verification>
- `pnpm --filter @kb/core test -- context` passes
- All D-01–D-08 behaviors covered by automated tests
- CORE-02: error paths never call getByIds
</verification>

<success_criteria>
- CORE-01: ContextService reads from same registry + Chroma via getByIds as SearchService corpus
- CORE-02: Structured ContextError, no partial chunks on invalid doc/index
- D-13: Live settings read per call
</success_criteria>

<output>
After completion, create `.planning/phases/05-context-retrieval-core/05-02-SUMMARY.md`
</output>
