---
phase: 05-context-retrieval-core
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - apps/backend/src/routes/settings.ts
  - apps/backend/src/routes/settings.test.ts
  - apps/backend/src/lib/errors.ts
  - apps/backend/src/services.ts
  - apps/backend/src/index.ts
  - apps/web/src/api/settings.ts
  - apps/web/src/components/SettingsPanel.tsx
  - apps/web/src/components/AppShell.tsx
  - apps/web/src/App.tsx
autonomous: false
requirements:
  - CORE-01
must_haves:
  truths:
    - "GET /api/v1/settings returns chunk (read-only) and context groups"
    - "PATCH /api/v1/settings/context updates context limits with Zod validation (D-12)"
    - "Settings routes use routeOpts Bearer gate when AUTH_ENABLED"
    - "ContextService wired in createAppServices alongside SearchService"
    - "Web admin has 设置 tab with 分块 (read-only) and 上下文检索 (editable) sections (D-09)"
    - "Saving context settings persists to SQLite; ContextService reads live values on next call (D-13)"
    - "Client validates readAroundWindowMax >= readAroundWindowDefault before submit"
  artifacts:
    - path: "apps/backend/src/routes/settings.ts"
      provides: "GET /api/v1/settings and PATCH /api/v1/settings/context"
      contains: "/api/v1/settings"
    - path: "apps/backend/src/services.ts"
      provides: "settingsStore and contextService on AppServices"
      contains: "contextService"
    - path: "apps/web/src/components/SettingsPanel.tsx"
      provides: "Grouped 简体中文 settings form"
      contains: "上下文检索"
    - path: "apps/web/src/components/AppShell.tsx"
      provides: "设置 tab in navigation"
      contains: "设置"
  key_links:
    - from: "apps/web/src/components/SettingsPanel.tsx"
      to: "/api/v1/settings/context"
      via: "updateContextSettings PATCH mutation"
      pattern: "settings/context"
    - from: "apps/backend/src/routes/settings.ts"
      to: "packages/core/src/registry/settings-store.ts"
      via: "updateContextConfig on PATCH"
      pattern: "updateContextConfig"
    - from: "apps/backend/src/services.ts"
      to: "packages/core/src/context/context-service.ts"
      via: "ContextService.create with settingsStore"
      pattern: "ContextService.create"
    - from: "apps/backend/src/index.ts"
      to: "apps/backend/src/routes/settings.ts"
      via: "registerSettingsRoutes with routeOpts"
      pattern: "registerSettingsRoutes"
---

<objective>
Expose context settings via backend API, wire ContextService in backend DI, and add Web admin 「设置」 tab with grouped configuration (D-09–D-13).

Purpose: Operators configure read_around/read_file bounds without redeploy; ContextService must read live SQLite values immediately after admin save.
Output: settings routes + tests, services wiring, SettingsPanel + API client, AppShell tab.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-context-retrieval-core/05-CONTEXT.md
@.planning/phases/05-context-retrieval-core/05-RESEARCH.md
@.planning/phases/05-context-retrieval-core/05-PATTERNS.md
@apps/backend/src/routes/search.ts
@apps/backend/src/routes/documents.ts
@apps/backend/src/auth.ts
@apps/web/src/components/SearchPanel.tsx
@.planning/phases/05-context-retrieval-core/05-01-SUMMARY.md
@.planning/phases/05-context-retrieval-core/05-02-SUMMARY.md

<interfaces>
From backend auth (Phase 4):
```typescript
export function apiRouteOpts(config: AppConfig, app: FastifyInstance): ApiRouteOpts;
// Returns { preHandler: [app.verifyBearerAuth] } when AUTH_ENABLED
```

From 05-01 SettingsStore:
```typescript
getChunkConfig(): ChunkConfig;
getContextConfig(): ContextConfig;
updateContextConfig(patch: Partial<ContextConfig>): ContextConfig;
```

From 05-02:
```typescript
export class ContextService {
  static create(config: AppConfig, deps: {
    registry: DocumentRegistry;
    vectorStore: ChromaVectorStore;
    settingsStore: SettingsStore;
  }): ContextService;
}
```

From web client:
```typescript
// apps/web/src/api/client.ts — apiRequest with Bearer + 401 retry
export async function apiRequest<T>(path: string, init?: RequestInit): Promise<T>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend settings routes, error mapper, services wiring</name>
  <read_first>
    - .planning/phases/05-context-retrieval-core/05-CONTEXT.md (D-12, D-13)
    - .planning/phases/05-context-retrieval-core/05-RESEARCH.md (Patterns 5, 6)
    - .planning/phases/05-context-retrieval-core/05-PATTERNS.md (settings.ts, errors.ts, services.ts, index.ts)
    - apps/backend/src/routes/search.ts
    - apps/backend/src/routes/search.test.ts
    - apps/backend/src/lib/errors.ts
  </read_first>
  <files>
    apps/backend/src/routes/settings.ts
    apps/backend/src/routes/settings.test.ts
    apps/backend/src/lib/errors.ts
    apps/backend/src/services.ts
    apps/backend/src/index.ts
  </files>
  <behavior>
    - Test 1: GET /api/v1/settings returns { chunk, context } shape
    - Test 2: PATCH /api/v1/settings/context with valid body returns updated context
    - Test 3: PATCH with readAroundWindowMax &lt; readAroundWindowDefault → 400 validation_error
    - Test 4: createAppServices exposes settingsStore and contextService
  </behavior>
  <acceptance_criteria>
    - grep -v '^#' apps/backend/src/routes/settings.ts | grep -c 'registerSettingsRoutes' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/routes/settings.ts | grep -c '/api/v1/settings' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/routes/settings.ts | grep -c 'ContextSettingsSchema' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/services.ts | grep -c 'contextService' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/services.ts | grep -c 'settingsStore' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/index.ts | grep -c 'registerSettingsRoutes' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/backend/src/lib/errors.ts | grep -c 'mapContextError' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **D-12 per CONTEXT** — backend settings API:

    1. **`apps/backend/src/routes/settings.ts`** (mirror search.ts Zod + routeOpts):
       - `ContextSettingsSchema` with zod/v4: all fields positive integers; `.refine(max >= default)` on window fields; sensible mins (chars ≥ 1000)
       - `GET /api/v1/settings` → `{ chunk: getChunkConfig(), context: getContextConfig() }`
       - `PATCH /api/v1/settings/context` → `updateContextConfig(request.body)` → `{ context: updated }`
       - Pass `routeOpts` from deps (Bearer when AUTH_ENABLED)
       - Export `registerSettingsRoutes(app, { settingsStore, routeOpts })`

    2. **`apps/backend/src/lib/errors.ts`**:
       - Add `mapContextError` for future REST (404 document_not_found, 400 chunk_index_out_of_range)
       - Add `mapContextSettingsError` for store validation failures

    3. **`apps/backend/src/services.ts`** (CORE-01 wiring):
       - Import `ContextService`, `initSettingsStore` already present
       - Extend `AppServices` with `settingsStore: SettingsStore` and `contextService: ContextService`
       - `ContextService.create(config, { registry, vectorStore, settingsStore })`
       - Return both in `createAppServices` result

    4. **`apps/backend/src/index.ts`**:
       - After search routes: `registerSettingsRoutes(app, { settingsStore: services.settingsStore, routeOpts })`

    5. **`settings.test.ts`**: Fastify inject harness per search.test.ts — mock settingsStore, assert GET/PATCH responses and validation 400.

    **Do NOT** add REST endpoints for readAround/readFile (API-06 deferred). **Do NOT** expose PATCH for chunk settings in Phase 5 (read-only chunk group per RESEARCH).
  </action>
  <verify>
    <automated>pnpm --filter @kb/backend test -- settings 2>&1</automated>
  </verify>
  <done>Settings API live; ContextService wired; backend tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Web 设置 tab and SettingsPanel (D-09–D-13)</name>
  <read_first>
    - .planning/phases/05-context-retrieval-core/05-CONTEXT.md (D-09, D-11, D-13)
    - .planning/phases/05-context-retrieval-core/05-RESEARCH.md (Pattern 7)
    - .planning/phases/05-context-retrieval-core/05-PATTERNS.md (SettingsPanel, AppShell, api/settings)
    - apps/web/src/components/SearchPanel.tsx
    - apps/web/src/components/UploadPanel.tsx
    - apps/web/src/App.tsx
    - apps/web/src/components/AppShell.tsx
  </read_first>
  <files>
    apps/web/src/api/settings.ts
    apps/web/src/components/SettingsPanel.tsx
    apps/web/src/components/AppShell.tsx
    apps/web/src/App.tsx
  </files>
  <acceptance_criteria>
    - grep -v '^#' apps/web/src/components/AppShell.tsx | grep -c 'settings' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/src/components/AppShell.tsx | grep -c '设置' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/src/components/SettingsPanel.tsx | grep -c '上下文检索' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/src/components/SettingsPanel.tsx | grep -c '分块' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/src/api/settings.ts | grep -c 'fetchSettings' | awk '{exit ($1>=1)?0:1}'
    - grep -v '^#' apps/web/src/App.tsx | grep -c 'SettingsPanel' | awk '{exit ($1>=1)?0:1}'
  </acceptance_criteria>
  <action>
    **D-09 per CONTEXT** — Web admin grouped settings (简体中文):

    1. **`apps/web/src/api/settings.ts`**:
       - `fetchSettings()` → GET `/api/v1/settings`
       - `updateContextSettings(patch)` → PATCH `/api/v1/settings/context`
       - Types for `ContextSettings` and grouped response

    2. **`apps/web/src/components/SettingsPanel.tsx`** (SearchPanel + UploadPanel patterns):
       - `useQuery({ queryKey: ["settings"], queryFn: fetchSettings })`
       - `useMutation` for save with success banner 「已保存上下文检索设置」
       - **Section 「分块」**: read-only display of `chunkSize` / `chunkOverlap` from GET (D-09 separation)
       - **Section 「上下文检索」**: editable number inputs for all five D-11 fields with 简体中文 labels per RESEARCH table
       - Client validation: if `readAroundWindowMax &lt; readAroundWindowDefault` → error 「最大窗口必须大于或等于默认窗口」
       - Reuse `panel-stack`, `field`, `btn btn-primary`, `banner-error`, `banner-success` classes

    3. **`AppShell.tsx`**: Add `settings` to `AppTab`; add tab `{ id: "settings", label: "设置" }` between search and help

    4. **`App.tsx`**: Import SettingsPanel; render when `activeTab === "settings"`

    **Do NOT** add MCP tools. **Do NOT** make chunk settings editable in Phase 5. Use existing `apiRequest` auth — no client.ts changes unless type imports needed.
  </action>
  <verify>
    <automated>pnpm --filter @kb/web build 2>&1</automated>
  </verify>
  <done>设置 tab shows grouped 分块 + 上下文检索; save persists via PATCH; web build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify admin settings end-to-end</name>
  <what-built>GET/PATCH settings API, ContextService wiring, Web 设置 tab with 上下文检索 form</what-built>
  <how-to-verify>
    1. Start stack: `pnpm dev` (or backend + web separately)
    2. Open Web admin → click **设置** tab
    3. Confirm **分块** section shows chunk_size/chunk_overlap (read-only)
    4. Confirm **上下文检索** section shows five numeric fields with 简体中文 labels
    5. Change `read_around_max_chars` (e.g. 32000 → 30000) → click **保存设置** → success banner appears
    6. Refresh page → value persists
    7. If AUTH_ENABLED: verify save works with valid API key; 401 without key
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PATCH /api/v1/settings/context | Untrusted numeric input; Zod + client validation |
| HTTP client → /api/v1/settings | Optional Bearer when AUTH_ENABLED (D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | E | Unauthenticated settings tampering | mitigate | routeOpts preHandler on GET/PATCH when AUTH_ENABLED |
| T-05-08 | T | Invalid window max &lt; default persisted | mitigate | Zod .refine() + SettingsPanel client check + store guard |
| T-05-09 | D | Extreme limit values | mitigate | Zod int min/max bounds on PATCH schema |
</threat_model>

<verification>
- `pnpm --filter @kb/backend test -- settings` passes
- `pnpm --filter @kb/web build` passes
- Human checkpoint: settings save + reload persists
</verification>

<success_criteria>
- D-09–D-13: Operator-configurable context limits via Web admin; live SQLite reads in ContextService
- D-12: GET/PATCH settings with auth gate
- CORE-01: ContextService wired to same registry/vectorStore/settingsStore as search path
</success_criteria>

<output>
After completion, create `.planning/phases/05-context-retrieval-core/05-03-SUMMARY.md`
</output>
