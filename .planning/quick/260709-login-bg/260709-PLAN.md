# Quick Task 260709: Login page beach background

**Goal:** Use `apps/web/png/login_in_backgrand.png` as full-screen login/register background with frosted-glass card.

## Tasks

1. Copy asset to `apps/web/public/` for Vite static serving
2. Update `.login-page` / `.login-card` styles (cover background + glassmorphism)
3. Verify `pnpm --filter @kb/web build` copies image to `dist/`
