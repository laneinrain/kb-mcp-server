# Milestone v1.2: Multi-User Auth & Hash Upload

**Status:** ✅ SHIPPED 2026-07-05  
**Phases:** 7–9  
**Total Plans:** 9

## Overview

Replace single shared API key with JWT multi-user auth (Mock CAS → production swap path), per-user document isolation, and content-hash dedup on `(user_id, filename)` uploads.

## Phases

### Phase 7: Auth Center Module

**Goal:** Independent `@kb/auth` with MockCasAuthProvider, 工号 login API, and 简体中文 login page.

**Requirements:** AUTH-01, AUTH-02, AUTH-03, AUTH-06, WEB-01

**Plans:** 3/3 complete

- [x] 07-01: `@kb/auth` + MockCasAuthProvider + employeeId store + config
- [x] 07-02: Login API + README swap documentation
- [x] 07-03: Web LoginPage + JWT client

---

### Phase 8: Multi-User Backend & Web Auth

**Goal:** JWT-protected API, per-user document isolation, logout.

**Requirements:** AUTH-04, AUTH-05, USER-01–04, WEB-03, WEB-04 (WEB-02 deferred)

**Plans:** 3/3 complete

- [x] 08-01: Registry `user_id` + system user migration + Chroma metadata
- [x] 08-02: Composite JWT/API_KEY auth + scoped document/search routes
- [x] 08-03: Web JWT-only + logout + CLI guard

**Cross-cutting constraints:**
- JWT user sees own docs + system legacy shared docs
- API_KEY service path retains global access
- WEB-02 deferred — JIT login only

---

### Phase 9: Filename Content-Hash Dedup

**Goal:** Same-filename uploads skip re-embedding when content unchanged; replace when content differs.

**Requirements:** INGE-10, INGE-11, INGE-12, INGE-13

**Plans:** 3/3 complete

- [x] 09-01: Registry `content_hash` + `findByUserAndFilename` + hash helpers
- [x] 09-02: IngestionService created/unchanged/replaced outcomes
- [x] 09-03: REST/Web/CLI outcome surfacing

**Cross-cutting constraints:**
- Dedup key `(user_id, filename)`; hash on normalized parsed text
- Legacy documents keep existing ids until re-upload
- MCP unchanged (global corpus)

---

## Milestone Summary

**Key Decisions:**
- `@kb/auth` as swappable module with MockCasAuthProvider (production → unified CAS)
- Composite JWT + API_KEY preHandler for Web vs service CLI paths
- Stable `documentId` = `sha256(userId + '\0' + filename)` for dedup
- `outcome`: created | unchanged | replaced with HTTP 201/200 mapping

**Issues Resolved:**
- Web upload path validation (DATA_DIR vs cwd) in Phase 8
- Path-based document IDs broken for UUID temp uploads — replaced in Phase 9

**Issues Deferred:**
- WEB-02 register page (JIT login only)
- Per-user MCP auth (PLAT-04)
- Live Web dedup UAT without USER_AUTH (integration tests cover behavior)

**Technical Debt:**
- CLI direct ingest uses empty userId when auth disabled (pre-existing)
- ApiKeyModal.tsx file remains but is unreferenced

---

_For current project status, see .planning/ROADMAP.md_
