# Milestone v1.6: Web Usability — Model Settings & MCP User Guide

**Status:** ✅ SHIPPED  
**Shipped:** 2026-07-20  
**Phases:** 19–21  
**Total Plans:** 8/8

## Overview

Enhance the Web admin console: runtime **embedding / rerank** configuration on the Settings page (resolves v1.4 deferral of Web rerank toggle), and a detailed **MCP User Guide** for Cursor and CodeBuddy under JWT auth (minimal slice of v1.5 deferral “Web MCP token management UI” via copy-current-JWT).

## Phases

### Phase 19: Model Settings Core

**Goal:** Persist model config in `SettingsStore`; expose REST; wire search/ingest clients to read runtime settings.

**Requirements:** CONF-04, CONF-05, CONF-06

**Plans:** 3/3 complete

- [x] 19-01: Schema migration + `getModelConfig` / `updateModelConfig` (seed from env)
- [x] 19-02: `GET /api/v1/settings` include `models`; `PATCH /api/v1/settings/models`
- [x] 19-03: Wire `SearchService` / embedding + rerank paths to settings (not frozen env only)

---

### Phase 20: Web Model Settings UI

**Goal:** Settings panel section for embedding/rerank with validation and embedding-change warnings.

**Requirements:** CONF-07, CONF-08

**Plans:** 2/2 complete

- [x] 20-01: Web API client + Settings form (model IDs, toggle, candidates)
- [x] 20-02: Embedding risk banner + read-only dimensions display; build smoke

---

### Phase 21: MCP User Guide (Help)

**Goal:** Expand Help into an actionable User Guide; Cursor + CodeBuddy JWT recipes; copy JWT helper.

**Requirements:** DOCS-01, DOCS-02, DOCS-03, DOCS-04, DOCS-05

**Plans:** 3/3 complete

- [x] 21-01: Restructure HelpPanel User Guide (中文 sections)
- [x] 21-02: Cursor + CodeBuddy MCP JWT config blocks + auth matrix
- [x] 21-03: Copy current JWT control + security/expiry notes

---

## Milestone Summary (shipped)

**Resolves from prior milestones:**
- Web rerank / model settings (deferred in v1.4)
- Partial “Web MCP token UI” (deferred in v1.5) — copy JWT only

**Key Decisions:**
- SQLite settings for models; dimensions stay env-only
- Runtime read of model config after PATCH
- Help-hosted User Guide for Cursor & CodeBuddy
- Embedding + Rerank PATCH/UI admin-only when `USER_AUTH_ENABLED`

**Issues Deferred:**
- RETR-01 hybrid BM25
- Online dimension change / collection rebuild
- Long-lived MCP token management product

---

_For current project status, see .planning/ROADMAP.md_
